# Implementation Plan

**Last updated:** 2026-05-28 — curator audit pass + housekeeping: completed `[x]` items pruned to the *Completed Items* reference list (foot of file); inline per-pass audit summaries collapsed into the Audit History table (full detail in git history + `docs/appendix/changelog.md`).
**Package version:** 0.2.16 in `pyproject.toml` — but plan bullet P8.38 (line ~237) and Completed-by-version claim that SFT FCCM "FIXED v0.2.17". The SFT FCCM branch (`engine/ccr/sft_fccm.py`) IS landed and wired (`pipeline_adapter.py:150` calls `sft_rows_to_exposures`), but `pyproject.toml` was not bumped to 0.2.17 — version-string drift to reconcile at next release. New work should baseline from 0.2.16.
**Source TODO/FIXME/HACK count:** 2 (both in `src/rwa_calc/engine/ccf.py:347` and `:417` — Art. 166D(5)(b) and CRE32.27 0.5 floor multipliers awaiting hoist into `data/tables/airb_floors.py`; tracked under P6.32 below)

_Per-pass audit narrative now lives in git history (`git log -p IMPLEMENTATION_PLAN.md`), `docs/appendix/changelog.md`, and the Audit History table near the foot of this file; the verbose inline summaries were removed 2026-05-28 to keep this file readable._

---

## Status Legend

- [ ] Not started
- [~] Partial / needs rework
- [x] Complete (pruned to the reference list at the end of this file)

---

## Remaining Work — Prioritized Bullet List

Items are sorted by priority. Each item notes its ID, status, and effort estimate (S/M/L).

### Tier 1 — Calculation Correctness (must fix for regulatory accuracy)

- **P1.94** [~] **PARTIAL — sub-items (a) FIXED v0.2.27, (b) FIXED v0.2.15 (batch 20260526-1735), (d) FIXED v0.2.33 (batch 20260529-2340), (e) FIXED v0.2.38 (batch 20260530-0311), (f) FIXED v0.2.30** — B31 currency mismatch 1.5x: 150% cap fixed v0.1.192; auto-detection wired; sub-item (a) `is_hedged` flag now gates the multiplier per Art. 123B(2) hedge exemption. Sub-item (b) v0.2.15 wires the 90%-coverage hedge gate per Art. 123B(2): new `hedge_coverage_ratio: pl.Float64` column on `LOAN_SCHEMA` (default 0.0) + new `B31_CURRENCY_MISMATCH_HEDGE_COVERAGE_FLOOR = Decimal("0.90")` constant in `data/tables/b31_risk_weights.py`; `engine/sa/namespace.py::apply_currency_mismatch_multiplier` hedge waiver gate extended to `is_hedged OR hedge_coverage_ratio >= 0.90`. Also added `"natural_person"` as alias entity_type per CRR Art. 112(1)(h) (in `data/tables/entity_class_mapping.py` SA + IRB maps and `VALID_ENTITY_TYPES`). Pinned by `tests/acceptance/basel31/test_p1_94b_hedge_coverage_ratio_gates_currency_mismatch.py` (2 tests: LOAN_SCHEMA contract; B31 retail_other GBP-income/EUR-loan with hedge_coverage_ratio=0.85 → RW=1.125 RWA=112_500 multiplier fires). Sub-item (f) v0.2.30 narrows `apply_currency_mismatch_multiplier` scope from substring-match (`_upper_class.str.contains("RETAIL"|"MORTGAGE"|"RESIDENTIAL"|"COMMERCIAL"|"CRE")`) which over-matched COMMERCIAL_MORTGAGE, to exact `pl.col("exposure_class").is_in(["retail_other","retail_qrre","retail_mortgage","residential_mortgage"])` per Art. 123B(1). Pinned by `tests/acceptance/basel31/test_p1_94f_currency_mismatch_scope_residential_re.py` (10 tests across retail in-scope + CRE out-of-scope + corporate sanity + cross-arm scope-boundary checks; load-bearing anti-assertion `RW != 1.50` for commercial_mortgage). Sub-item (d) v0.2.33 wires Art. 123B(2A): `engine/sa/namespace.py::apply_currency_mismatch_multiplier` now rescales the 90%-hedge-coverage test against the fully-drawn base (`max(drawn_amount, facility_limit)`) for `is_revolving` rows (effective coverage = `hedge_coverage_ratio × drawn / full_draw_base`), so a facility 90%-hedged on its current drawing fails the waiver once measured against the committed limit; non-revolving rows unchanged, defensive column-absence defaults preserve production frames (no new floor literal — reuses `B31_CURRENCY_MISMATCH_HEDGE_COVERAGE_FLOOR`). Pinned by `tests/acceptance/basel31/test_p1_94d_art_123b_2a_revolving_instalment_rule.py` (11 tests: revolving in-scope RW 1.125/RWA 112_500; non-revolving + fully-drawn-revolving controls RW 0.75). Sub-item (e) v0.2.38 wires Art. 123B(3) transitional: new `B31_EFFECTIVE_DATE = date(2027, 1, 1)` in `data/tables/b31_risk_weights.py` + a strict `if config.reporting_date < B31_EFFECTIVE_DATE: return self._lf.with_columns(currency_mismatch_multiplier_applied=False)` short-circuit at the top of `apply_currency_mismatch_multiplier` (after the existing `is_basel_3_1` guard), so a Basel-3.1 run with a pre-2027 reporting date suppresses the multiplier (boundary 1 Jan 2027 in scope, strict `<`); `@cites("PS1/26, paragraph 123B.3")`. Pinned by `tests/acceptance/basel31/test_p1_94e_pre_2027_fallback.py` (7 tests: Run A 2026-12-31 → RW 0.75/RWA 75_000 multiplier suppressed; Run B 2027-01-01 → RW 1.125/RWA 112_500 multiplier fires). **Sub-items remaining:** (g) CR5 pre-multiplier RW reporting + OF 02.00 row 0380 memo. **Effort: S** | Ref: PRA PS1/26 Art. 123B
- **P1.108** [~] **DISPUTED (file:line re-scoped 2026-05-28)** — CRR 1.06 scaling applied to retail IRB. `formulas.py:383-384` (`apply_scaling = config.is_crr; scaling_factor = 1.06 if apply_scaling else 1.0`, consumed at `:478-486`) and `namespace.py:720` + `:887` (`scaling_factor = 1.06 if config.is_crr else 1.0`) apply 1.06 to ALL CRR classes with no retail carve-out. BCBS CRE31.23 retail has NO 1.06, but UK-onshored CRR Art. 154(1) (CRR PDF page 148-149, references the Art. 153(1) RW formula which carries `× 1.06`) includes the factor. If UK text authoritative, code is correct. Needs PRA legal clarification — kept DISPUTED, not a confirmed bug. B31 unaffected (1.06 removed under PS1/26). **Effort: S (if confirmed bug)** | Ref: CRR Art. 153(1)/154(1), BCBS CRE31.23
- **P1.130** [x] **FIXED v0.2.32 (batch 20260529-2258)** — Aggregator summaries now reflect post-floor RWA when the output floor binds. The defect: `summary_by_class` / `summary_by_approach` (and `post_crm_detailed` / `post_crm_summary`) were generated at `aggregator.py:131-134` from the **pre-floor** `combined` frame, before the portfolio-level output-floor block reassigns `combined` to the floored frame at `:198`; `_summaries.py` computes `total_rwa` as `(reporting_ead × reporting_rw).sum()` and the floor never recomputes `reporting_rw`, so reported per-class/per-approach RWA understated the floored total (Art. 92(2A)) whenever the floor bound. **FIX (2 files):** (a) `engine/aggregator/aggregator.py` — moved `post_crm_detailed`/`post_crm_summary`/`summary_by_class`/`summary_by_approach` generation to **after** the floor block (built from the possibly-floored `combined`); `pre_crm_summary` left in place; bundle return-arg order unchanged. (b) `engine/aggregator/_summaries.py` — new private `_floor_addon_expr(cols, ead_col)` folds the per-row `floor_impact_rwa` add-on (allocated by `reporting_ead / ead_final` share so guarantee-split rows don't double-count; no-op `pl.lit(0.0)` when the floor didn't run/bind) into the reporting-path `total_rwa` for both `_build_class_agg_exprs` and `_build_approach_agg_exprs`. The Art. 62(d) EL/T2-credit-cap carve-out (`aggregator.py:136-156`, un-floored IRB RWA) is left byte-for-byte identical; the non-binding path stays correct. Pinned by `tests/acceptance/basel31/test_p1_130_summaries_reflect_post_floor.py` (6 tests: floor-binding + S-TREA≈200m guards; `summary_by_approach`/`summary_by_class` totals reconcile to `output_floor_summary.total_rwa_post_floor`; `results.rwa_final` reconciliation; shortfall reflected — relationship-framed, robust to IRB-K drift). **Effort: S** | Ref: PRA PS1/26 Art. 92(2A)
- **P1.30(e)** [x] **FIXED v0.2.33 (batch 20260529-2340)** — Art. 234 partial protection tranching now modelled. New optional `attachment_amount`/`detachment_amount` columns on `GUARANTEE_SCHEMA` (`data/schemas.py`); `engine/crm/guarantees.py::_build_remainder_sub_rows` (now `@cites("CRR Art. 234")`) emits a three-row split when `attachment_amount > 0` — first-loss retained `[0,a)` (`__REM_FL`, obligor RW), mezzanine protected `[a,d)` (guarantor-substituted), senior retained `[d,EAD]` (`__REM_SEN`, obligor RW); null attachment preserves the legacy first-loss single `__REM` row. `redistribute_non_beneficial` remainder predicate loosened `ends_with("__REM")`→`contains("__REM")` so both retained rows are still classified as remainders. Pinned by `tests/acceptance/crr/test_p1_30e_art_234_partial_protection_tranching.py` (11 tests: three-row structure, per-tranche EAD 200k/400k/400k, ΣEAD=1,000,000 conservation, exactly-one-guarantor-row, blended RW 0.80). **Effort: L** | Ref: PRA PS1/26 Art. 234, CRR Art. 234
- **P1.10** [ ] Unfunded credit protection transitional (PRA Rule 4.11) — narrow eligibility carve-out for legacy contracts during 1 Jan 2027–30 Jun 2028. Requires Art. 213 eligibility validation first. **Effort: M** | Ref: PRA PS1/26 Rule 4.11
- **P1.153** [x] **FIXED v0.2.33 (batch 20260529-2340)** — Art. 155(3) PD/LGD equity approach implemented. New `EquityApproach.PD_LGD` enum (`domain/enums.py`); new `data/tables/crr_equity_pd_lgd.py` carrying the Art. 165 PD floors (0.09%/0.09%/0.40%/1.25%), LGD 90% (65% diversified-PE), M=5y, and the Art. 155(3) 1.5× no-default-info scaling (all `Decimal`, data-layer); new `equity_pd_lgd: bool = False` flag on `CalculationConfig`; new `has_default_definition_info` `pl.Boolean` column on `EQUITY_EXPOSURE_SCHEMA`. `engine/equity/calculator.py::_apply_equity_weights_pd_lgd` reuses the shared corporate IRB primitives (`engine/irb/formulas.py` correlation/K/MA), computes `RWEA = K×12.5×1.06×MA×EAD`, the per-exposure cap `min(RWEA, EAD×12.5 − EL×12.5)`, and the 1.5× factor when default-definition info is absent; `_determine_approach` routes CRR+IRB+flag → PD_LGD (still returns SA under Basel 3.1 — flag ignored, IRB equity removed). Pinned by `tests/acceptance/crr/test_p1_153_art_155_3_pdlgd_equity.py` (14 tests; worked exchange-traded exposure EAD £1m → RW 1.918731, RWA £1,918,731, K 0.0736714). Golden corrected from the scenario's misrounded `(1-R)^-0.5` (1.92154→1.918731) after independent stdlib `NormalDist` verification. **Effort: L** | Ref: CRR Art. 155(3), Art. 165

<!-- ### Migrated from DOCS_IMPLEMENTATION_PLAN.md on 2026-05-03 -->
<!-- D3.1, D3.4, D3.6, D3.11–D3.23, D3.27, D3.29, D3.36–D3.39, D3.43 → P1.163–P1.185. -->
<!-- These items are code-side only; the docs side has already been corrected. -->

- **P1.173** [~] **Tracked as P2.17 (closed v0.2.29)** — CRR retail payroll/pension 35% RW now applied per Art. 123 second subparagraph (P2.17 detail below). Sub-gap remaining (cosmetic, see P1.175): comments at `b31_risk_weights.py:212` and `schemas.py:80,101,642` cite "Art. 123(3)(a-b)" — should be Art. 123(4) for the CRR side. **Effort: S** | Ref: CRR Art. 123(4)
- **P1.178** [~] **Tracked as P1.122 (open sub-claim (b))** — B31 guarantee substitution framework branching. P1.95 and P1.110 closed; remaining gap (unrated B31 institution guarantor SCRA via IRB SA-fallback path) is folded into P1.122(b). **Effort: M**
- **P1.183** [ ] **NEW** — CRR Art. 164(4) portfolio-level LGD floors not implemented. `LGDFloors.crr()` returns zeros; Art. 164(4) (CRR2) imposes portfolio-level minimum LGD: ≥10% retail residential RE, ≥15% retail commercial RE. Per-exposure architecture can't enforce directly. **Capital impact: understatement.** Add post-aggregation validation step computing EW avg LGD per retail RE sub-class; emit error or re-floor. **Effort: M** | Ref: UK CRR Art. 164(4) — **NOTE (batch 20260530-0032): must run SINGLE-STREAM.** A scenario-architect pass (batch 20260530-0032) confirmed the only architecturally sound enforcement of the EW-average floor is a post-aggregation hook whose call site lands in `engine/aggregator/aggregator.py` (a hard-excluded shared file); emitting it mid-IRB-stage would force a LazyFrame-contract-violating eager collect. Pick it alone via `/next-scenario` or `/implement-scenario P1.183`, not in a parallel `/next-items` batch. Proposal interpretation: WARNING-only (code `IRB007`), not a per-exposure re-floor; new scalars `CRR_RETAIL_RRE_PORTFOLIO_LGD_FLOOR=0.10` / `CRR_RETAIL_CRE_PORTFOLIO_LGD_FLOOR=0.15` belong in `data/tables/firb_lgd.py`.

#### Tier 1b — Source-code docstring corrections (cosmetic, no calculation impact)

These items are docstring / code-comment / structural-organisation corrections only. The calculations they describe are correct; the bugs are in the prose surrounding them. Tagged Effort: S.

- **P1.175** [ ] **NEW (comment refs)** — Art. 114 para errors in comments across `calculator.py`/`classifier.py`/`eu_sovereign.py`/`guarantee.py` cite "Art. 114(3)/(4)" for domestic currency. UK = 114(4); EU = 114(7). Bundle with P1.173 sub-gap. **Effort: S** | Ref: CRR Art. 114(4)/(7), Art. 123(4)
- **P1.188** [ ] **NEW (cosmetic, docstring/comment-only — 2026-05-16 audit; file:line re-scoped 2026-05-26)** — Six stale `PS9/24` regulatory-instrument citations in source-code docstrings and metadata: `contracts/config.py:512` (`PostModelAdjustmentConfig` class docstring); `data/schemas.py:1904` (POST-MODEL ADJUSTMENTS comment — original bullet cited `:1446`, file has since grown); `engine/irb/adjustments.py:9, 14, 133` (module docstring + class docstring); `pyproject.toml:4` description string says "compliant with PRA PS9/24". Final instrument is **PS1/26** (effective 1 Jan 2027). PS9/24 was the near-final consultation paper from PRA in 2024; numbering was reassigned to PS1/26 on publication of the final policy statement. No calculation impact. (Picked P1.188 to avoid collision with P6.15's existing reference to "P1.186" as the `liquidation_period` config tracker.) **Effort: S** | Ref: PRA PS1/26 (final), PS9/24 (consultation — superseded)

- **P6.9** [~] Provision pro-rata weight uses pre-CCF approximation (`drawn + interest + nominal`) instead of spec's `ead_gross`. Reasonable but diverges from spec. **Effort: S** | Ref: CRM spec
- **P6.15** [~] 3 missing schema fields: `protection_inception_date` (P1.10), `contractual_termination_date` (P1.20 revolving maturity), `liquidation_period` as config (P1.39 dependency). Note: prior plan iteration cross-referenced "P1.186" here, but that ID was used and closed v0.2.13 for an unrelated CRR Art. 224(2)(a) Hfx default fix — the `liquidation_period` config gap is its own residual sub-claim of P1.39 and not currently tracked under any separate P-code. **Effort: S**

### Tier 2 — Test Coverage Gaps (no code changes, but essential for confidence)

- **P5.11** [ ] **NEW** — Missing acceptance tests for secondary SA exposure classes. Covered bonds, PSE, RGLA, MDB, high-risk items, and other items (Art. 134) have 60-120 unit tests each but ZERO end-to-end acceptance tests. Need CRR and B31 acceptance scenarios. **Effort: M**
- **P5.12** [ ] **NEW** — Missing B31 acceptance tests for Art. 129A covered bond changes (SCRA-derived unrated RW). Only unit tests exist. **Effort: S**
- **P5.13** [ ] **NEW** — Missing acceptance tests for Art. 124A-124L RE treatment scenarios under B31 (income-dependent/non-dependent, commercial sub-types, Other RE Art. 124J). Only unit tests. **Effort: S**
- **P5.14** [ ] **NEW** — No integration/acceptance tests for COREP or Pillar III reporting generators. All 663+ COREP tests and 197+ Pillar III tests are unit tests with synthetic data. No test validates full pipeline → reporting output. **Effort: M**
- **P5.15** [x] **FIXED v0.2.34 (batch 20260530-0032)** — Art. 123A(1)(b)(ii) 0.2% retail granularity sub-condition now enforced. New `B31_RETAIL_GRANULARITY_LIMIT = Decimal("0.002")` in `data/tables/b31_risk_weights.py`; `engine/classifier.py::_build_qualifies_as_retail_expr` gains a Basel-3.1-only granularity limb (inserted after the SME auto-qualify + GBP 880k threshold branches): a natural-person candidate-retail obligor whose per-obligor aggregate exceeds 0.2% of the total candidate-retail portfolio (denominator de-dups one contribution per obligor via `pl.len().over("counterparty_reference")` wrapped in `partition_by_nullable`, with a `portfolio_total > 0` guard) fails `qualifies_as_retail` and re-routes to CORPORATE (RW 0.75→1.00). CRR branch stays threshold-only. Pinned by `tests/acceptance/basel31/test_p5_15_art_123a_granularity.py` (19 tests: breach obligor → CORPORATE RW 1.00/RWA 2,000 with load-bearing anti-confound `lending_group_adjusted_exposure < 880k`; control-pass obligor stays retail RW 0.75/RWA 750). **Effort: M**
- **P5.16** [ ] **NEW (2026-05-16 audit — test-side companion to P1.30(e))** — No acceptance scenario covering Art. 234 partial protection / tranching (CRR Art. 234, PRA PS1/26 Art. 234). Required once P1.30(e) is implemented; tracking now so the implementer doesn't ship without an end-to-end acceptance pin. Block on P1.30(e). **Effort: M** | Ref: CRR / PRA PS1/26 Art. 234
- **P5.17** [ ] **NEW (2026-05-28 audit — test-side companion to P8.53 + P8.39)** — No end-to-end acceptance scenario exercises the WWR LGD=100% override (P8.53) or the QCCP 2%/4% RW pin (P8.39) **through the orchestrator**. Both engine functions (`apply_wwr_gate`, `apply_ccp_risk_weight`) have thorough unit coverage (`tests/unit/ccr/test_wwr.py` 13 tests; `tests/unit/ccr/test_ccp.py` 8 tests) but those tests call the free functions directly — they do not prove the pipeline routes through them, which is exactly the gap P8.53/P8.39 describe. Track two acceptance scenarios so the implementer who wires the orchestrator call sites cannot ship without an integration pin: (a) **CCR-WWR-1** — a netting set with one `is_specific_wwr=True` trade + one non-WWR trade run through the full `PipelineOrchestrator`, asserting the WWR trade lands in a `<ns>__wwr__<trade>` synthetic NS with `wwr_lgd_override=1.0` while the non-WWR trade stays in the parent NS; (b) **CCR-CCP-1/2** — direct-cleared and client-cleared QCCP trades run end-to-end, asserting `risk_weight == 0.02` / `0.04` overrides the standard SA-Institution ladder (the load-bearing anti-degenerate: RW must NOT equal the CQS-2 institution 50%). Block on P8.53 and P8.39 respectively. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 291(5)(c), Art. 306(1); `tests/acceptance/ccr/`

### Tier 3 — COREP Reporting Completeness

- **P2.1** [~] COREP template rework — see detailed section below.
- **P2.5** [~] COREP missing row structure — OF 02.00 sub-class breakdown rows (0295-0297, 0355-0356, 0382-0385) need `cp_is_fse`/`is_sme` pipeline columns. OF 08.01 revolving row 0017 needs pipeline column. **Effort: M**
- **P2.7** [~] COREP pre-credit-derivative RWEA (row 0310) — approximated as total RWEA. Accurate split requires per-exposure pre/post CD tracking in CRM pipeline. **Effort: M**
- **P2.16** [ ] **NEW** — Output floor edge cases: (a) GCRA cap not enforced when S-TREA=0 (`_floor.py:77` passes GCRA uncapped; should be 0.0 per "1.25% of S-TREA"); (b) S-TREA fallback to `rwa_final` when `sa_rwa` absent (`aggregator.py:138`) skews GCRA cap on audit path. **Effort: S** | Ref: PRA PS1/26 Art. 92(2A)
- **P2.15** [x] **FIXED v0.2.39 (batch 20260530-0426)** — Equity transitional irrevocable opt-out election (PRA PS1/26 Rules 4.9-4.10) now wired. New `opt_out: bool = False` field on `EquityTransitionalConfig` (`contracts/config.py`); when set it gates BOTH transitional gates per Rule 4.9's joint election — `engine/equity/calculator.py::_equity_holding_higher_of_rw` returns `None` (CIU look-through underlying reverts from the 370% legacy Art. 155(2) higher-of to `_DEFAULT_HOLDING_RW`=100%) and `_apply_transitional_floor` early-returns unchanged (direct equity keeps its end-state assigned RW). `@cites("PS1/26, paragraph 4.9")`. The CIU higher-of mechanic itself shipped under P1.139 (v0.2.31). Pinned by `tests/acceptance/basel31/test_p2_15_equity_transitional_optout.py` (8 cases: CIU look-through RW 3.70/RWA 3,700,000 at opt_out=False → RW 1.00/RWA 1,000,000 at opt_out=True; direct LISTED control RW 2.50/RWA 2,500,000 invariant under both). **Effort: S** | Ref: PRA PS1/26 Rules 4.9-4.10
- **P2.19** [x] **FIXED v0.2.30 (batch 20260528-2220)** — Higher-risk equity Art. 133(4) definition relies on pre-classified `equity_type` instead of evaluating regulatory conditions dynamically. **FIXED (scope-corrected):** the bullet's proposed `is_held_for_short_term_resale`/`is_derived_from_derivative` flags are **BCBS CRE60.20, not PRA** (already corrected in-repo as D1.38/D3.37) and were deliberately NOT added; the genuine gap was the Art. 133(4) 400% higher-risk test being gated on `equity_type ∈ {private_equity, private_equity_diversified}` at `engine/equity/calculator.py`. Generalised the gate to any unlisted (non-exchange-traded) equity with **evidenced** business age <5y (null age stays 250% for non-PE), reusing the existing 4.00 scalar — no schema change. Pinned by `tests/acceptance/basel31/test_p2_19_unlisted_young_equity_higher_risk.py`. **Effort: S** | Ref: PRA PS1/26 Art. 133(4)
- **P2.21** [ ] **NEW (file:line re-scoped 2026-05-28)** — Pipeline error conversion drops ALL `CalculationError` metadata (not just `regulatory_reference`). Three `getattr` blocks in `pipeline.py` (`:510, 512` resolution-error path / `:645, 647` classification-error path / `:683, 684` equity-error path — earlier `:509,511,644,646,682` reference drifted by one line) use `getattr(error, "error_type", "unknown")` and `getattr(error, "context", {})` but `CalculationError` (`contracts/errors.py:47-56`) is `@dataclass(frozen=True)` with only `code/message/severity/category/exposure_reference/counterparty_reference/regulatory_reference/field_name/expected_value/actual_value` — no `error_type` and no `context`. Both `getattr` calls always return the default. Audit trail severely degraded. (Note: the CRM/RE-split paths at `:721, 757` instead use `error.code` directly, which DOES resolve — so this is specifically the three `getattr` sites above.) **Effort: S**
- **P1.139** [x] **FIXED v0.2.31 (batch 20260528-2308)** — Equity transitional Rule 4.7-4.8 CIU higher-of now implemented. The fix lands in `_resolve_look_through_rw` (`engine/equity/calculator.py`), not `_apply_transitional_floor`: an EQUITY-class CIU look-through/mandate-based underlying whose CQS join misses (was falling back to `_DEFAULT_HOLDING_RW`=100%) now takes `max(legacy Art. 155(2) "other equity" simple RW = 370%, Rule 4.2/4.3 transitional SA RW)` when the B31 equity-transitional regime is active (gated on the existing `equity_transitional.enabled` + `reporting_date` surface — no new config fields); the CIU wrapper itself remains excluded from the floor (`_apply_transitional_floor` unchanged). New helper `_equity_holding_higher_of_rw` reuses `IRB_SIMPLE_EQUITY_RISK_WEIGHTS` (no new engine-scope scalar). Pinned by `tests/acceptance/basel31/test_p1_139_ciu_transitional_higher_of.py` (3 tests: look-through RW 2.52, RWA 2,520,000 vs buggy 900,000, wrapper-not-floored). Remaining out-of-scope follow-ons: mandate-based 1.2× variant, opt-out election (P2.15), no-IRB-permission negative control. **Effort: M** | Ref: PRA PS1/26 Rules 4.7-4.8; CRR Art. 155(2)
- **P1.141** [x] **FIXED v0.2.35 (batch 20260530-0114)** — Art. 124(4) mixed-RE **all-or-nothing qualifying gate** implemented. (Re-scoped: the pro-rata-by-collateral-value split was already shipped end-to-end; the genuine residual gap was the gate — if either property component fails Art. 124A, BOTH must drop to Art. 124J, no partial preference.) `engine/hierarchy.py` now aggregates a per-exposure `re_collateral_non_qualifying` flag (any RE collateral with `is_qualifying_re=False`) and preserves UNCAPPED residential/commercial collateral values before the retail-threshold cap (the cap was distorting split shares when total collateral > exposure); `engine/classifier.py` emits a Basel-3.1-only `re_split_force_other_re = is_mixed & re_collateral_non_qualifying` (new Bool on `CLASSIFIER_OUTPUT_SCHEMA`, `@cites("PS1/26, paragraph 124.4")`), CRR path `pl.lit(False)`; `engine/re_splitter.py` then routes BOTH secured rows through Art. 124J (`b31_other_re_rw_expr`: RESI→cp_rw, CRE→max(60%,cp_rw)) at full pro-rata EAD (drops the 0.55×V cap → residual 0). No new scalar (reuses `B31_OTHER_RE_CRE_FLOOR_RW=0.60`). Pinned by `tests/acceptance/basel31/test_p1_141_art_124_4_all_or_nothing_gate.py` (9 tests: unrated-corporate mixed exposure with failing commercial component → both legs RW 1.00, secured_rre/cre EAD 1,200,000/800,000, parent RWA 2,000,000 vs pre-fix 1,340,000, residual EAD 0). **Effort: M** | Ref: PRA PS1/26 Art. 124(4)
- **P1.142** [x] **FIXED v0.2.31 (batch 20260528-2308)** — Art. 124E three-property limit now automated. New `qualifying_property_count: ColumnSpec(pl.Int32, required=False)` on `COUNTERPARTY_SCHEMA` (`data/schemas.py`) + `B31_RRE_THREE_PROPERTY_LIMIT = 3` in `data/tables/b31_risk_weights.py`; `engine/classifier.py` derives the materially-dependent flag `cp_is_natural_person AND (cp_qualifying_property_count > 3)` (strict `>3`) with `coalesce` precedence so an explicit caller-supplied income flag still wins, Basel-3.1-only guard (CRR routing untouched). The derived flag overrides `has_income_cover` so a 4+-property natural-person obligor routes onto the Art. 124G income-producing whole-loan track (70-80% LTV → RW 0.50) instead of the Art. 124F loan-split. Pinned by `tests/acceptance/basel31/test_p1_142_three_property_income_dependent.py` (6 tests: breach count=4 → RW 0.50/RWA 100,000; control count=3 → loan-split RW 0.34667/RWA 69,333.33; strict-`>3` boundary). **Effort: M** | Ref: PRA PS1/26 Art. 124E(2), Art. 124G
- **P1.143** [ ] **NEW (Phase 9)** — Rule 4.11 unfunded credit protection grandfathering window (1 Jan 2027 – 30 Jun 2028) not honoured. Distinct from P1.10 (which is the underlying Art. 213 eligibility check; P1.143 is the transitional date-gated relaxation that depends on it). **Effort: M** | Ref: PRA PS1/26 Rule 4.11
- **P2.25** [ ] **NEW (Phase 9)** — CR5 special disclosure rules not implemented. `reporting/pillar3/generator.py:325-359` omits: (a) currency mismatch multiplier exposures reported at pre-multiplier RW with post-multiplier RWEA; (b) regulatory RE not-materially-dependent split up to / above 55% LTV; (c) equity transitional reported at end-state RW. **Effort: M** | Ref: PRA PS1/26 Annex XX §CR5
- **P2.26** [x] **FIXED v0.2.35 (batch 20260530-0114)** — COREP Annex II §1.3 "(-)"-column sign convention now enforced. `reporting/corep/generator.py` adds module-level `_C07_NEGATIVE_COLS = {0030,0035,0050,0060,0070,0080,0090,0130,0140}` / `_C08_NEGATIVE_COLS = {0290}` and a `_negate_deduction_cols(values, negative_cols)` helper applied as a single negation pass at the END of `_compute_c07_values` and `_c08_memorandum_cols` — AFTER the reconciliation columns (0040/0110/0150/IRB-0090) consume the POSITIVE intermediates, so net-exposure arithmetic is byte-unchanged; negative-zero normalised to 0.0, null preserved. `@cites("PS1/26, paragraph 1.3")`. Pinned by `tests/unit/test_corep.py::TestSignConvention` (15 tests: 10 signed columns incl. CRR+B31, plus 4 over-negation invariants asserting 0040 stays +850 etc.); 17 pre-existing corep tests updated to the corrected sign (13 direct sign flips + 4 reconciliation re-derivations replaced with direct engine-value assertions). Full `tests/unit/test_corep.py`: 706 passed. **Effort: S** | Ref: COREP Annex II §1.3
- **P2.27** [ ] **NEW (Phase 9)** — OF 08.01 col 0275 uses IRB EAD not SA-equivalent. `reporting/corep/generator.py:3615`. Annex II §OF 08.01 col 0275 requires SA-equivalent post-CCF / post-CRM EAD. Using IRB EAD overstates the non-modelled exposure denominator for the output floor. **Effort: S** | Ref: COREP Annex II §OF 08.01 col 0275 — **DROPPED at design gate (batch 20260530-0213); re-scope to Effort M–L, NOT a generator one-liner.** A scenario-architect + reviewer pass confirmed there is NO SA-equivalent EAD column anywhere in `src/rwa_calc/`, and the only Effort-S derivation (`sa_equivalent_ead = sa_rwa / sa_risk_weight`) is algebraically circular: `engine/sa/calculator.py:205` builds `sa_rwa = ead_final × sa_risk_weight`, so dividing back out reproduces the IRB `ead_final` col 0275 already sums (a no-op). A correct fix is a dedicated engine batch that recomputes SA-basis post-CCF EAD (SA Table A1 CCFs applied to the IRB population) and re-bases BOTH col 0275 (SA-equivalent EAD) AND col 0276 (SA-equivalent RWA floor numerator — which today silently assumes EAD_SA == EAD_IRB) together. Pick alone / not as a parallel-batch Effort-S item.
- **P2.28** [ ] **NEW (Phase 9)** — CR9 row taxonomy incomplete. `reporting/pillar3/templates.py:565-580` missing AIRB RE 4-way sub-class split (resi SME, resi non-SME, comm SME, comm non-SME) and FIRB "financial / large corporates" sub-row. **Superseded by P2.49** which expresses the full row taxonomy. **Effort: S** | Ref: PRA PS1/26 Annex XXII §CR9
- **P2.29** [x] **FIXED v0.2.34 (batch 20260530-0032)** — OV1 equity sub-approach + output-floor rows now populated. `reporting/pillar3/generator.py` no longer hard-codes rows 11-14/26/27 to null (the catch-all `_OV1_EXPLICIT_NULL_REFS` is replaced by `_OV1_FLOOR_NO_SHIM_REFS={"26","27"}` + an `_OV1_EQUITY_SUBAPPROACH_REFS` discriminator map): rows 11-14 (IRB Transitional / look-through / mandate-based / fall-back) sum `rwa_final` over `approach_applied="equity"` + the row's discriminator (`equity_transitional_approach`/`ciu_approach`), col c = 8% × a; row 26 = first non-null `output_floor_pct`; row 27 = `OutputFloorSummary.of_adj` via a new optional `generate_from_lazyframe(output_floor_summary=...)` param (default `None` preserves all existing callers incl. the parquet-backed `generate()`; rows 26/27 excluded from the col-c shim). Memo "of which" rows 11-14 are NOT folded into the row-29 total. Pinned by `tests/unit/reporting/pillar3/test_p2_29_ov1_equity_subapproach.py` (15 tests; Python-only seeded-results fixture + `OutputFloorSummary`). **Effort: S** | Ref: PRA PS1/26 Annex XX §OV1
- **P2.30** [x] **FIXED v0.2.37 (batch 20260530-0238)** — CCF Annex I Row 3 / Row 4 discrimination. New `RiskType.MR_ISSUED = "medium_risk_issued"` (`domain/enums.py`) for Annex I Row 3 (other issued OBS items), distinct from `MR` (Row 4, NIFs/RUFs); both resolve to an **identical** 50% SA CCF (and matching F-IRB) so RWA/EAD are provably invariant — the change is identifier-separability for Annex I-faithful disclosure only. Wired through `data/schemas.py` (`VALID_RISK_TYPES_INPUT` + `RISK_TYPE_SYNONYMS` `mr_issued`/`medium_risk_issued`→`MR_ISSUED`) and `data/tables/ccf.py` (explicit `MR_ISSUED: 0.50` in `SA_CCF_CRR`/`SA_CCF_B31`/`FIRB_OBS_FALLBACK` + `is_mr_or_oc` membership). Pinned by `tests/unit/test_corep_annex1_row_discrimination.py` (11 tests: enum/constraint/synonym existence RED→GREEN + CCF 0.50 / EAD 500k invariance for both rows). P2.31 (concrete product→risk_type mapping table) and the optional COREP "of which" sub-row remain out of scope. **Effort: S** | Ref: CRR Annex I, Art. 111
- **P2.31** [x] **FIXED v0.2.38 (batch 20260530-0311)** — Annex I concrete-product→`risk_type` mapping now in code. New input-domain `obs_product` column on `FACILITY_SCHEMA`/`CONTINGENTS_SCHEMA` (`data/schemas.py`, with `VALID_OBS_PRODUCTS` + `OBS_PRODUCT_SYNONYMS` + `COLUMN_VALUE_CONSTRAINTS` registration); a **single framework-invariant** `ANNEX1_PRODUCT_RISK_TYPE` dict + `build_product_to_risk_type_expr` in `data/tables/ccf.py` (ACCEPTANCE→FR; PERFORMANCE_BOND/WARRANTY/TENDER_BOND/BID_BOND/DOCUMENTARY_CREDIT/TRADE_LC→MLR — all framework-invariant under SA Table A1, so **no** `_CRR`/`_B31` split; the framework split lives only in the untouched `SA_CCF_CRR`/`SA_CCF_B31`); `engine/ccf.py::_compute_ccf` fills `risk_type` from `obs_product` only when `risk_type` is null/empty (explicit wins), `@cites("CRR Art. 111")`. Reg note: performance bonds are SA-Annex-I **MLR/20%** (the 50% figure is the F-IRB Art. 166(10)(b) path, not SA). Pinned by `tests/acceptance/crr/test_p2_31_annex1_mapping.py` (10 tests: ACCEPTANCE→FR/CCF 1.00/EAD 2,000,000; PERFORMANCE_BOND & DOCUMENTARY_CREDIT→MLR/0.20/EAD 400,000; explicit-LR override stays LR). Full-pipeline `obs_product` threading through `hierarchy.py` is a documented follow-on (targeted test drives `CCFCalculator` directly). **Effort: M** | Ref: CRR Annex I, Art. 111
- **P2.32** [x] **FIXED v0.2.35 (batch 20260530-0114)** — CCF Art. 166E(5) purchased-receivables undrawn-commitment conversion factor implemented. New `is_purchased_receivable_commitment` Bool (`ColumnSpec(pl.Boolean, default=False, required=False)`) on `FACILITY_SCHEMA` + `CONTINGENTS_SCHEMA` (`data/schemas.py`), threaded through `engine/hierarchy.py` mirroring `is_uk_residential_mortgage_commitment`; a Basel-3.1-gated `_apply_purchased_receivable_ccf` in `engine/ccf.py::_compute_ccf` overrides a revolving purchase commitment's CCF to 40% (Table A1 Row 5 OC) or 10% (Row 7 UCC/LR), reusing `SA_CCF_B31["OC"]`/`["LR"]` (no new scalar), `@cites("PS1/26, paragraph 166.5")`; no-op under CRR. (Dilution/default split and Art. 166A(5) EAD netting remain out of scope — separate mechanisms.) Pinned by `tests/acceptance/basel31/test_p2_32_purchased_receivable_ccf.py` (7 tests: OC 40%/EAD 400k, LR 10%/EAD 100k, **load-bearing MR 40%/EAD 400k** overriding generic MR 50%, CRR control no-op). **Effort: M** | Ref: CRR Art. 166(5), PRA PS1/26 Art. 166E(5)
- **P2.33** [x] **FIXED v0.2.30 (batch 20260528-2220)** — CCF UK residential mortgage commitment Row 4(b) 50% PRA deviation not enforceable. **FIXED:** added `is_uk_residential_mortgage_commitment` flag (FACILITY/CONTINGENTS schema), threaded it through `HierarchyResolver`, and applied a Basel-3.1-gated Row 4(b) 50% CCF override (`SA_CCF_B31["MR"]`) bounded by the "not 10%/100%" carve-out — replacing the 40% OC fall-through; control (flag-off) row stays 40%. Pinned by `tests/acceptance/basel31/test_p2_33_uk_resi_mortgage_commitment_ccf.py`. **Effort: S** | Ref: PRA PS1/26 Annex I Row 4(b)
- **P2.37** [ ] **NEW** — Art. 155(4) Internal Models Approach (CRR equity) entirely absent. VaR-based equity capital with PRA-permission gate. Lower priority than P1.153 (PD/LGD approach). **Effort: L** | Ref: CRR Art. 155(4)
- **P2.38** [x] **FIXED v0.2.40 (batch 20260530-0519)** — Non-trading-book short-position netting (Art. 155(2)) now modelled. New `EQUITY_EXPOSURE_SCHEMA` columns `position_value` (signed +long/−short)/`issuer_reference`/`is_explicitly_hedged`; `engine/equity/calculator.py::_net_short_positions` (`@cites("CRR Art. 155(2)")`) nets signed positions per issuer for the IRB Simple method (CRR-only — B31 routes equity to SA), netted long = max(0, Σ position_value), absorbed short → EAD/RWA 0; hedge-tenor floor `CRR_EQUITY_NETTING_MIN_HEDGE_YEARS` in `data/tables/crr_equity_rw.py`. Pinned by `tests/acceptance/crr/test_p2_38_art_155_2_short_position_netting.py` (8 tests: netted EAD 600,000 × 290% = RWA 1,740,000 vs un-netted 4,060,000). **Effort: M** | Ref: CRR Art. 155(2)
- **P2.40** [ ] **NEW (re-scoped again 2026-05-26 — file:line drift)** — model_id precedence chain incomplete. `src/rwa_calc/engine/hierarchy.py:2211-2255` (`_attach_counterparty_rating` body — original 2026-05-16 pointer at `:~2297-2331` is stale, but the substantive claim still holds: `cp_select` at `:2225-2231` only pulls `internal_model_id` from `counterparty_lookup.counterparties`; on `:2250-2252` `model_id` is sourced exclusively from the counterparty's inherited rating row via `pl.col("internal_model_id").alias("model_id")`). Spec specifies an exposure → facility → loan → counterparty → model_permissions default precedence. Original 2026-04 bullet's `1288-1295` pointer pointed at the MOF allocation block (`_allocate_mof_facility_undrawn`) which is unrelated. **Effort: M** | Ref: model_permissions spec — **DROPPED at design gate (batch 20260530-0213); must be split + multi-file, NOT `hierarchy.py`-only.** A scenario-architect audit confirmed there is NO `model_id`/`internal_model_id` source column at facility/loan/exposure grain in any schema (`FACILITY_SCHEMA`/`LOAN_SCHEMA`/`CONTINGENTS_SCHEMA`/`RAW_EXPOSURE_SCHEMA`/`RESOLVED_HIERARCHY_SCHEMA`); `model_id` only enters via `RATINGS_SCHEMA` at counterparty grain (the existing `hierarchy.py:2316-2322` alias). The memory note "model_id on Facility/Loan/Contingent fixture dataclasses" is a test scaffold (`tests/fixtures/p1_126/p1_126.py:187-190`) that pre-propagates onto the exposure frame to *bypass* the production path — not a schema column. A `hierarchy.py`-only coalesce is a no-op without an upstream source. **Split into P2.40a** (schema+loader enablement: add `model_id: ColumnSpec(pl.String, required=False)` to FACILITY/LOAN/CONTINGENTS, thread through RAW_EXPOSURE/RESOLVED_HIERARCHY + the loader exposure-flattening — touches `data/schemas.py` + `engine/loader.py`, does NOT touch the 4 hard-excluded shared files) **then P2.40b** (the `hierarchy.py` `coalesce(pl.col("model_id"), pl.col("internal_model_id"))` precedence). Pick P2.40a first via `/implement-scenario`.
- **P2.41** [x] **FIXED v0.2.39 (batch 20260530-0426)** — `exposure_subclass` for the Art. 147A(1)(e)/(f) COREP corporate split now derived. New `ExposureSubclass` StrEnum (`domain/enums.py`: CORPORATE_FINANCIAL_LARGE / CORPORATE_OTHER / CORPORATE_SME) + classifier-derived `exposure_subclass` column (`ColumnSpec(pl.String, required=False)` on `CLASSIFIER_OUTPUT_SCHEMA`); `engine/classifier.py::_derive_exposure_subclass` (Basel-3.1-only, CRR→null; `@cites("PS1/26, paragraph 147A.1")`) routes corporate exposures to `corporate_financial_large` when FSE OR `cp_annual_revenue > config.thresholds.large_corporate_revenue_threshold` (GBP 440m, Art. 147A(1)(e)), `corporate_sme` when SME, else `corporate_other` (Art. 147A(1)(f)). `reporting/corep/generator.py::_c02_00_irb_sub_agg` now routes the C 02.00 corporate sub-rows (0295/0296/0297, 0355/0356) by `exposure_subclass` instead of the `is_fse = apply_fi_scalar OR cp_is_financial_sector_entity` heuristic (which misrouted large-corporates-by-revenue into 0297), with a graceful fallback when the column is absent (CRR/pre-classifier frames). Reporting-granularity only — RWA-conservation-neutral (full `tests/unit/test_corep.py` 706 green). Pinned by `tests/acceptance/basel31/test_p2_41_exposure_subclass_corep.py` (7 tests: row 0295 == RWA(FSE)+RWA(large-corp-by-revenue) strictly > RWA(FSE) alone, row 0297 residual 0, F-IRB conservation). **Effort: M** | Ref: PRA PS1/26 Art. 147A(1)(e)/(f)
- **P2.44** [x] **FIXED v0.2.30 (batch 20260528-2220)** — Art. 139(2B) ECAI rating-attribution disapplication for SA SL not enforced. **FIXED:** added `rating_is_issue_specific`/`rating_is_inferred` Bools (RATINGS_SCHEMA) + `external_rating_is_issue_specific` (HIERARCHY_OUTPUT_SCHEMA), threaded provenance through the rating-inheritance chain, and gated SA-SL routing (Art. 122B(1)) so an inferred-only rating is disapplied per Art. 139(2B) → exposure falls to unrated object-finance 100% RW instead of the rated-corporate CQS table (defaults preserve legacy rated-SL behaviour). Pinned by `tests/acceptance/basel31/test_p2_44_sa_sl_inferred_rating_disapplied.py`. **Effort: S–M** | Ref: PRA PS1/26 Art. 122B(1), 139(2)/(2A)/(2B)
- **P2.45** [ ] **NEW** — Art. 143(6) Overseas Model Approach (OMA) not represented. New B31 IRB permission for retail/SME-corp of equivalent-jurisdiction overseas subs, capped at 7.5% of group RWA / EV pre-output-floor. Need: OMA enum, jurisdiction-equivalence Bool, post-aggregation cap validation, Art. 143(6)(c)-(k) checks. **Effort: M** | Ref: PRA PS1/26 Art. 143(6)/(7)/(8)
- **P2.46** [x] **FIXED v0.2.40 (batch 20260530-0519)** — PPU provenance enum implemented: new `PpuReason` StrEnum (`art_150_1_a..j` + `art_148_rollout`, `domain/enums.py`); new `ppu_reason` column on `MODEL_PERMISSIONS_SCHEMA` + `CLASSIFIER_OUTPUT_SCHEMA` threaded via `engine/classifier.py::_resolve_model_permissions` onto the SA-precedence row; `reporting/corep/generator.py` routes C 07.00 row 0050 (`ppu_reason ∈ art_150_1_*`) / row 0060 (`art_148_rollout`) with graceful fallback when the column is absent; `"standardised"` added to `VALID_MODEL_PERMISSION_APPROACHES` (required sub-fix). Art. 150(2) equity materiality is an explicit non-goal. Pinned by `tests/acceptance/crr/test_p2_46_ppu_provenance_corep.py` (8 tests: rows 0050/0060/residual = 1m/1m/1m of total SA 3m). Was: CRR Art. 150(1) PPU provenance enum missing on `model_permissions`. COREP col 0050 ("PPU SA") indistinguishable from Art. 148 roll-out or no-permission SA. Add `ppu_reason` enum. Sub-finding: Art. 150(2) firm-level equity materiality (10%/5% own funds) not enforced — note as non-goal. **Effort: S–M** | Ref: CRR Art. 150(1)/(2); PRA Art. 150(1A)
- **P2.47** [~] **PARTIAL — Art. 137 OECD MEIP (Basel-3.1 arm) FIXED v0.2.39 (batch 20260530-0426); Art. 136 / 138 / 121 remain** — Art. 137 Table 9 MEIP-score→RW direct sovereign path was already shipped on the **CRR arm** under P1.100; v0.2.39 adds the missing **Basel-3.1 arm**. `engine/sa/namespace.py::_apply_b31_risk_weight_overrides` gains the `cp_eca_score` branch (mirroring the CRR sibling, ordered below the Art. 114(3)/(4) domestic-currency 0% override and above the unrated fallback) so an unrated B31 sovereign carrying an ECA/MEIP score gets the Art. 137 RW (e.g. score 2 → 20%) instead of the Art. 114 unrated 100% default — reusing the existing `ECA_MEIP_RISK_WEIGHTS` data table via `_eca_meip_rw_expr()` (no new scalar, no schema change), `@cites("CRR Art. 137")` (watchfire WHITELIST synced). Pinned by `tests/acceptance/basel31/test_p2_47_art137_meip_b31.py` (RW 0.20 / RWA 1,000,000 on EAD 5,000,000; anti-confound RW≠1.00). **Remaining sub-claims:** Art. 136 grade-string→CQS map; Art. 138 second-best across ECAI+ECA; Art. 121 sovereign-derived institution propagation. **Effort: M** | Ref: PRA PS1/26 Art. 136-138, 121
- **P2.48** [x] **FIXED v0.2.40 (batch 20260530-0519)** — CR8 RWEA flow opening + residual rows now populated. New optional `previous_period_results: pl.LazyFrame | None` param on `Pillar3Generator.generate`/`generate_from_lazyframe` (no `contracts/bundles.py` change); `_generate_cr8` fills row 1 (opening = prior IRB-non-slotting `rwa_final` sum via `_filter_irb_non_slotting` + `_col_sum`) and row 8 (Other = closing − opening, signed Float64, increases +/decreases −), rows 2-7 stay None; backwards-compatible (omitting the param leaves rows 1-8 None). Pinned by `tests/unit/reporting/pillar3/test_p2_48_cr8_rwea_flow.py` (9 tests: opening 1.0m, residual +150k, closing 1.15m, −150k decrease control). Per PRA PS1/26 Annex XXII §11 / CRR Art. 438(h). Was: Pillar III CR8 RWEA flow only populates closing row; rows 1 (opening) and 2-8 (flow drivers) emit `None` (`pillar3/generator.py::_generate_cr8:690-718` lacks prior-period data). Sign convention: increases positive, decreases negative. Need optional `previous_period_results` on reporting boundary; compute seven flow drivers; signed Decimals. **Effort: M** | Ref: PRA PS1/26 Annex XXII §11
- **P2.49** [x] **FIXED v0.2.38 (batch 20260530-0311)** — CR9 column-`a` taxonomy extended to the full PRA PS1/26 Annex XXII leaf set. `CR9_FIRB_CLASSES` 4→**5** leaves (added `corporate_financial_large` [Art. 147(2)(c)(ii), F-IRB-only], `corporate_other_non_sme`; dropped the collapsed `corporate` parent) and `CR9_AIRB_CLASSES` 6→**10** leaves (added `corporate_other_non_sme` + retail RRE/CRE × SME/non-SME + retail_other SME/non-SME; dropped `corporate`/`retail_mortgage`/`retail_other` parents). The row-definition tuple became a 3-element `(row_key, label, CR9ClassSpec)` form — `CR9ClassSpec` is a plain frozen dataclass so `templates.py` stays import-clean (no `pl`; arch_check-enforced data/engine boundary); the descriptor→`pl.Expr` resolution (`_cr9_class_predicate` over `exposure_class`/`is_sme`/`property_type`/`cp_is_financial_sector_entity`, graceful degradation when a discriminator is absent) lives in `generator.py`, applied to **both** `_generate_all_cr9` and `_generate_cr9_1`; `@cites("PS1/26, paragraph 147.2")`. **Supersedes P2.28.** Pinned by `tests/unit/reporting/pillar3/test_p2_49_cr9_taxonomy.py` (13 tests: 5/10 leaf counts, 15 expected keys, collapsed-parent absence, discriminator routing) + re-keyed `tests/unit/test_pillar3.py` (count/key/value tests, `_make_cr9_irb_data` gained the 3 discriminator cols); full pillar3 surface 256 passed. **Effort: M** | Ref: PRA PS1/26 Annex XXII pp.19-20, Art. 147(2)
- **P2.9** [~] **RE-SCOPED 2026-05-21** — OF 34.07 IRB CCR template now in scope under Tier 8 (P8.50). Becomes a sub-deliverable of P8.50 / P8.51 (COREP CCR template suite) rather than a known gap. Originally tracked as "CCR is out of scope; document as known gap" — now actively planned. **Effort: M** | See Tier 8.
- **P4.20** [ ] C 08.02 PD bands use fixed buckets instead of firm-specific internal rating grades. **Effort: M**

### Tier 4 — Pillar III Disclosure Gaps

- **P3.5** [x] **FIXED v0.2.37 (batch 20260530-0238)** — CR9.1 (ECAI-based PD back-testing) now callable end-to-end. The `CR9_1_COLUMNS` template existed but had no generator method; added `_generate_cr9_1` + `_generate_cr9_1_for_class` + `_cr9_1_schema` and a new `cr9_1: dict[str, pl.DataFrame]` field on the **reporting-side** `Pillar3TemplateBundle` (`reporting/pillar3/generator.py` — NOT `contracts/bundles.py`), wired into `generate_from_lazyframe`. Basel-3.1-only (CRR → `{}`); filters ECAI-mapped rows (`ecai_pd_mapping`, Art. 180(1)(f)), groups by `external_rating_equivalent`, reuses `_compute_cr9_values` for cols c–h. Pinned by `tests/unit/reporting/pillar3/test_p3_5_cr9_1_ecai_backtesting.py` (25 tests; seed `tests/fixtures/p3_5/`; key `"advanced_irb - corporate"`, height 3, ECAI "A"/"BBB", CRR-gate + CR9 regression). Multi-ECAI fan-out and Excel-sheet export remain out of scope. **Effort: S** | Ref: PRA PS1/26 Art. 452(h), Art. 180(1)(f), Annex XXII §15
- **P3.3** [~] Missing qualitative tables UKB CRD (SA qualitative, Art. 444(a-d)) and UKB CRE (IRB qualitative, Art. 452(a-f)). CR6 AIRB purchased receivables sub-row missing. CR5 rows 18-33 not detailed. **Effort: M**
- **P3.6** [ ] **NEW (Phase 9)** — CR5 / CR9 / OV1 disclosure gaps overlap with P2.25/P2.28/P2.29. Pillar III tables cannot faithfully reproduce Annex XX / XXII rows without: (a) pre-multiplier RW column, (b) equity transitional end-state RW column, (c) AIRB RE 4-way sub-class split, (d) FIRB financial/large-corp sub-rows, (e) OV1 equity sub-approach populations. **Effort: M** | Ref: PRA PS1/26 Annex XX §CR5/OV1, Annex XXII §CR9

### Tier 5 — Documentation & Consistency

- **P4.2** [~] Stale version numbers — `overview.md` says 0.1.37, `prd.md` says 0.1.28; current `pyproject.toml` is **0.2.20**. Plan header re-baselined; docs side still drifted. **Effort: S**
- **P4.3** [~] Stale `docs/plans/implementation-plan.md` — shows items as incomplete that are done. **Effort: S**
- **P4.4** [~] Stale PRD FR statuses. **Effort: S**
- **P4.8** [~] COREP template spec thin in `output-reporting.md` — detailed in `corep-reporting.md` feature doc. Cross-reference needed. **Effort: S**
- **P4.9** [~] Type checker inconsistency — docs disagree with CLAUDE.md (`mypy` vs `ty`). **Effort: S**
- **P4.10** [~] `model_permissions` not in architecture spec. **Effort: S**
- **P4.23** [ ] Stale test counts in `regulatory-compliance.md` (97 CRR / 116 B31; actual 169/212). **Effort: S**
- **P4.24** [ ] Stale NFR metrics in `nfr.md`: 1,844+ tests (actual 5,188); "6 stages" (actual 9 protocols); 8 namespaces (actual 2). **Effort: S**
- **P4.25** [ ] Stale acceptance test counts in `index.md`. **Effort: S**
- **P4.28** [ ] Stale C 07.00 column count in `reporting/corep/templates.py` docstring (says 24/22, actual 27/27). **Effort: S**
- **P4.29** [ ] Stale "Not Yet Implemented" in `docs/user-guide/regulatory/basel31.md:186-187, 196` (currency mismatch, defaulted — both done). **Effort: S**
<!-- P4.30 CLOSED 2026-05-26 (silent fix — see Completed Items): regulatory-compliance.md:80 / :99-101 now have CRR-J, B31-K, B31-L, B31-M; CRR-D enumeration at :73 covers D1–D6. -->

- **P4.31** [ ] Skill ref `slotting-changes.md` shows BCBS pre-op PF weights (80/100/120/350%) as PRA B31; PRA has no separate pre-op table. **Effort: S**
- **P4.32** [ ] Skill reference files (`.claude/skills/.../references/*.md`) need systematic BCBS-vs-PRA audit. **Effort: S**
- **P4.33** [ ] Stale BCBS output floor in docstrings: `comparison.py:18`, `bundles.py:489`, `ui/marimo/comparison_app.py:14-15` say "50% (2027) to 72.5% (2032+)". Runtime correct. **Effort: S**
- **P4.47** [ ] Informational: PS1/26 output-floor transitional is 3 years (60/65/70% 2027-2029 per Art. 92(5)); 2030+ → Art. 92(2A) 72.5%. Audit any cite of Art. 92(5)(d) (doesn't exist). **Effort: S**
- **P4.48** [ ] **File path re-scoped 2026-05-26** — Covered bond "Art. 161(1B)" comments at `src/rwa_calc/data/tables/firb_lgd.py:289, 296, 778, 797, 846, 874` (original bullet cited `irb/firb_lgd.py` which does not exist — the file lives in `data/tables/`, consistent with the data/engine separation enforced by `scripts/arch_check.py` checks 5/6). Article doesn't exist in CRR text; correct ref is Art. 161(1)(d). 11.25% value correct. **Effort: S**
<!-- P4.49 CLOSED 2026-05-26 (silent fix — see Completed Items): the cited file `irb/ccf.py` does not exist (consolidated into `engine/ccf.py`), and the live file already uses Art. 166(8) — e.g. `engine/ccf.py:6, 11, 40, 123, 136, 137, 138, 141, 167, 171` all cite Art. 166(8). No occurrence of `Art. 166(9)` remains in `engine/`. -->
- **P4.51** [ ] **File:line + scope re-scoped 2026-05-26** — Two `ExposureClass` docstring typos in `domain/enums.py`: `INSTITUTION` at `:52` cites Art. 112(d) (correct CRR ref is **(f)** — Art. 112(d) is MDBs); `HIGH_RISK` at `:106` (original bullet cited `:76`, file has grown) cites Art. 112(l) (correct CRR ref is **(k)**). The original bullet additionally claimed a third typo at `:91` (which it labelled DEFAULT) — that line is the PSE docstring citing Art. 112(c), which is actually correct CRR; line `:88` (DEFAULT) cites Art. 112(j) which is also CRR-correct (defaults are (j), high-risk is (k), institutions are (f)). Narrowing to the two real typos only. **Effort: S**
- **P4.52** [ ] `ciu_holdings` not in `docs/specifications/architecture.md` data-model section. **Effort: S**
- **P4.53** [ ] Undocumented CalculationConfig fields: `PostModelAdjustmentConfig`, `collect_engine`, `spill_dir`. **Effort: S**
- **P4.54** [ ] fastexcel vs xlsxwriter inconsistency between `overview.md` and `output-reporting.md`. **Effort: S**
- **P4.55** [ ] Document new B3.1 sovereign/institution PD floors (pairs with P2.36) in `firb-calculation.md`/`airb-calculation.md`. **Effort: S**

### Tier 6 — Code Quality

- **P6.15** [~] 3 missing schema fields — see Tier 1 above.
- **P6.17** [ ] **NEW (Phase 9)** — Pre-floor `rwa_final` naming collision. `engine/pipeline.py:487-490`. Consumers reading `sa_results` / `irb_results` see pre-floor values under the label `rwa_final`, creating ambiguity against the post-floor `rwa_final` on the aggregated bundle. **Effort: S**
- **P6.21** [ ] **NEW (Phase 9)** — `_compute_portfolio_waterfall` eagerly collects mid-pipeline. `engine/comparison.py:754-763, 387`. Breaks the LazyFrame-first contract. **Effort: S**
- **P6.23** [x] **FIXED v0.2.37 (batch 20260530-0238)** — `_TRANSITIONAL_REPORTING_DATES` (`engine/comparison.py`) corrected from mid-year `date(YYYY, 6, 30)` to `date(YYYY, 1, 1)` per PRA PS1/26 Art. 92(5) 1-January phase-in (60/65/70% from 1 Jan 2027/2028/2029) + Art. 92(2A) steady-state 72.5% from 1 Jan 2030; two stale "mid-year" docstrings corrected. The canonical `OutputFloorConfig.basel_3_1()` schedule was already on 1-Jan dates — only the comparison-runner constant was wrong. Pinned by `tests/unit/test_transitional_schedule.py` (`TestTransitionalDates::test_dates_are_first_of_january` inverted from the prior mid-year assertion + new default-path timeline `reporting_date` behavioural test; 21 tests green). **Effort: S** | Ref: PRA PS1/26 Art. 92(5)/(2A)
- **P6.24** [ ] **NEW** — `LazyFrameResult` mutability across stage boundaries. `src/rwa_calc/contracts/errors.py:86`. Not `frozen=True`; exposes `add_error`/`add_errors` mutation methods. Contradicts immutable-bundle architecture principle. **Effort: S**
- **P6.25** [x] **FIXED v0.2.36 (batch 20260530-0213)** — Factory-method asymmetry for CRM/A-IRB collateral knobs resolved. `CalculationConfig.crr()` now accepts `airb_collateral_method: AIRBCollateralMethod = AIRBCollateralMethod.LGD_MODELLING` and threads it into the returned config, matching `basel_3_1()`'s existing both-knob signature (CRR A-IRB own-LGD collateral recognition per CRR Art. 161/181; PS1/26 Art. 169A names this `LGD_MODELLING`; `FOUNDATION` would be the wrong F-IRB default). Behaviourally inert under CRR — every `engine/crm/collateral.py` read of the field is `is_basel_3_1`-gated, so RWA/EAD/LGD are unchanged; the dataclass field default at `contracts/config.py:946` stays `None` for raw-constructor semantics; `basel_3_1()` untouched. Pinned by `tests/unit/crm/test_art169_lgd_modelling.py::TestConfigAndEnum` (3 tests: crr() default == LGD_MODELLING, override pass-through == FOUNDATION, basel_3_1() symmetry; the stale `test_crr_config_no_airb_method` `is None` assertion was flipped). **Effort: S** | Ref: `src/rwa_calc/contracts/config.py`; CRR Art. 161/181; PRA PS1/26 Art. 169A
- **P6.27** [ ] **NEW (batch 20260508-0020 surfacing)** — Pre-existing failures in `tests/unit/crm/test_collateral_sequential_fill.py`: `TestSequentialFillAllCategories::test_all_five_categories_b31` (got 0.15015 vs 0.1595 expected) and `TestPerTypeMinThreshold::test_covered_bonds_no_threshold` (got 0.34595 vs 0.3655 expected). Both reproduce on the unmodified main `src/` per engine-implementer attestation. Numerical drift on multi-collateral sequential-fill blended LGD calculation. **Effort: S** | Ref: `src/rwa_calc/engine/crm/collateral.py`, `tests/unit/crm/test_collateral_sequential_fill.py:733,990`
- **P6.28** [ ] **NEW (batch 20260509-1100 surfacing)** — Pre-existing test-vs-engine drift in `tests/unit/test_sovereign_floor_institutions.py::TestTradeExemption::test_trade_lc_exempt_from_floor`. Test asserts B31 institution SCRA-A short-term trade-LC RW = 40% but engine returns 20% — driven by the SCRA short-term Art. 121(4) trade-finance extension shipped in P1.128 (v0.2.13) which routes `is_short_term_trade_lc=True` through `B31_ECRA_SHORT_TERM_ECAI_RISK_WEIGHTS`/SCRA short-term branch. Resolve by re-deriving the regulatorily correct value (per Art. 121(4) text the SCRA-A short-term ≤6m branch yields 20% — test expectation is stale). **Effort: S** | Ref: `tests/unit/test_sovereign_floor_institutions.py:252-270`
- **P6.29** [ ] **NEW (batch 20260509-1100 surfacing)** — Stress fixture under `tests/acceptance/stress/test_stress_pipeline.py` is stale: 56 errors with `ColumnNotFoundError: unable to find column "has_short_term_ecai"`. Stress fixture data was generated before P1.105 (v0.2.12) added the `has_short_term_ecai` field to `FACILITY_SCHEMA`. Regenerate the stress fixture or have the stress generator default-fill the column. Pre-existing across batches. **Effort: S** | Ref: `tests/acceptance/stress/`, `src/rwa_calc/data/schemas.py` `FACILITY_SCHEMA`, `_b31_append_institution_maturity_branches`
- **P6.30** [x] **FIXED v0.2.31 (batch 20260528-2308)** — Dead `compute_pfe_ir_singleton` stub removed. Deleted the `NotImplementedError`-raising function (and its `@cites("CRR Art. 278")`) from `engine/ccr/pfe.py`, the `pfe_ir_singleton` namespace shim + docstring bullet from `engine/ccr/namespace.py`, and the import + `__all__` entry from `engine/ccr/__init__.py`; updated the stale module scaffold comment to state the SA-CCR formula bodies are implemented. Watchfire-safe: production `compute_pfe` retains its own `@cites("CRR Art. 278")`. The pre-existing contract test was inverted from "raises NotImplementedError" to assert non-importability. Pinned by `tests/contracts/test_ccr_engine_scaffold.py::test_compute_pfe_ir_singleton_removed`. **Effort: S** | Ref: `src/rwa_calc/engine/ccr/pfe.py`
- **P6.31** [ ] **NEW (2026-05-26 audit — companion to P4.53; file:line re-scoped 2026-05-28)** — Two undocumented `CalculationConfig` fields: `audit_cache_dir: Path | None = None` and `audit_cache_max_runs: int | None = None` at `contracts/config.py:958-959` (earlier `:951-952` drifted as the dataclass grew; also surfaced via `crr()` and `basel_3_1()` factory kwargs at `:1021-1022, 1102-1103`). Used by `src/rwa_calc/api/service.py:81-94, 193-194, 203-204` to opt into per-run audit-cache materialisation. Only `docs/specifications/audit-cache.md` documents the feature; `docs/specifications/configuration.md` (the config spec) does not list either field. Pair with P4.53 (which already tracks `PostModelAdjustmentConfig`, `collect_engine`, `spill_dir`). **Effort: S** | Ref: `src/rwa_calc/contracts/config.py:946-952`, `docs/specifications/configuration.md`
- **P6.32** [ ] **NEW (2026-05-26 audit — TODO sweep)** — Two `# TODO: move ... 0.5 floor multiplier to data/tables/airb_floors.py` markers in `src/rwa_calc/engine/ccf.py:347` (CRE32.27 0.5 floor multiplier in `airb_revolving_ccf`) and `:417` (Art. 166D(5)(b) 0.5 floor multiplier in floor_b). Both inline `0.5` literals are regulatory scalars per `scripts/arch_check.py:53-68` `REGULATORY_SCALAR_ALLOWLIST` and should be hoisted into `src/rwa_calc/data/tables/airb_floors.py` (file may need to be created — `airb_ccf.py` is the closest existing sibling). No calculation impact; an arch-cleanup ticket. **Effort: S** | Ref: PRA PS1/26 Art. 166D(5)(b); BCBS CRE32.27
- **P6.33** [ ] **NEW (doc-writer SA-CCR batch surfacing — RC duplication)** — Duplicate unmargined-RC implementation inside `compute_pfe`. `src/rwa_calc/engine/ccr/pfe.py:110` inlines `pl.max_horizontal(v_minus_c, pl.lit(0.0)).alias("rc_unmargined")` instead of delegating to the canonical `compute_rc_unmargined` exported from `src/rwa_calc/engine/ccr/rc.py:33-48`. The two implementations are formula-equivalent for the unmargined branch (both encode CRR Art. 275(1) `max(V_net − C_net, 0)`), so this is currently a no-op duplication, but a future tweak to the canonical RC kernel (e.g. an Art. 275 amendment, an FX-of-collateral adjustment, or a logging hook) would have to be applied in two places. Refactor `compute_pfe` to call `compute_rc_unmargined` directly on the input frame before composing `pfe_addon` / `ead_ccr` (the function already returns the right column name with the right dtype). No calculation impact; arch-cleanup / code-hygiene ticket. **Effort: S** | Ref: PRA PS1/26 Art. 275(1); CRR Art. 275(1); `src/rwa_calc/engine/ccr/pfe.py:99-116`, `src/rwa_calc/engine/ccr/rc.py:33-48`
- **P6.35** [ ] **NEW (doc-writer D2.79 SA-CCR batch surfacing 2026-05-28 — citation correction)** — `apply_ccp_risk_weight` `@cites(...)` decorator and docstring cite wrong CRR article for the 4% client-cleared RW. `src/rwa_calc/engine/ccr/ccp.py:47` carries `@cites("CRR Art. 307")` (stacked alongside the correct `@cites("CRR Art. 306")` at `:46`); the docstring at `:60` reads `is_qccp=True, is_client_cleared=True -> 0.04 (Art. 307)`. **PRA PS1/26 Annex R Section 9 places both the 2% proprietary and the 4% client-cleared trade-exposure RWs in Article 306 paragraph 1** (per `docs/assets/ps126app1.pdf` page 457: "Article 306 — Own Funds Requirements for Trade Exposures"). Article 307 covers the *trade-exposure value* definition (not the RW), and Articles 308/309 cover default-fund contributions (tracked separately under P8.49). Runtime behaviour is correct (`QCCP_CLIENT_CLEARED_RW = Decimal("0.04")` at `data/tables/crr_risk_weights.py:603` is applied to the right branch in the `pl.when().then().when().then().otherwise()` ladder at `ccp.py:104-111`), only the regulatory citation is mislabelled — so this is cosmetic / metadata only with no RWA impact. **Fix**: (a) remove the `@cites("CRR Art. 307")` decorator at `ccp.py:47` (leaving the correct `@cites("CRR Art. 306")` at `:46`); (b) update the docstring at `:59-60` to cite `Art. 306(1)(c)` (or just `Art. 306(1)`) for both the 2% and 4% RWs; (c) regenerate the watchfire citation matrix via `uv run watchfire matrix` and re-run `uv run python scripts/arch_check.py` (which invokes `watchfire check` as the final gate step per `CLAUDE.md`). Cross-ref: D2.79 docs spec already uses the correct Art. 306(1)(c) citation. **Effort: XS** (one-line citation fix + docstring edit + matrix regen) | Ref: PRA Rulebook CCR (CRR) Part Art. 306(1); CRR Art. 306(1); `docs/assets/ps126app1.pdf` page 457; `src/rwa_calc/engine/ccr/ccp.py:46-60`; `docs/development/citation-tracking.md`
- **P6.34** [ ] **NEW (doc-writer SA-CCR batch surfacing — watchfire CRR index gap)** — Five SA-CCR engine functions waive their `@cites(...)` decorator with a NOTE comment because the bundled watchfire CRR rulebook index does not recognise CRR Art. 274 / Art. 275: `src/rwa_calc/engine/ccr/sa_ccr.py:48-52` (`compute_ead` — Art. 274), `src/rwa_calc/engine/ccr/rc.py:28-32` (`compute_rc_unmargined` — Art. 275(1)), `src/rwa_calc/engine/ccr/rc.py:51-52` (`compute_rc_margined` — Art. 275(2)). Same pattern previously surfaced for Art. 280a/b/c (P8.7 fix-commit precedent) and Art. 277/277a (collapsed to parent Art. 277 in `pfe.py:138`). Should be batched into a single watchfire CRR rulebook-index extension that covers Art. 274, 275, 277, 277a, 278, 279a/b/c, and 280a/b/c so the per-function `@cites` decorators can be added and the article→function matrix produced by `uv run watchfire matrix` becomes complete for the SA-CCR chain. Complements partial-coverage tracker P8.48. **Effort: S** | Ref: `docs/development/citation-tracking.md`; watchfire 0.3.1 CRR rulebook_version 2026-05-15; `src/rwa_calc/engine/ccr/sa_ccr.py:48-52`, `src/rwa_calc/engine/ccr/rc.py:28-52`, `src/rwa_calc/engine/ccr/failed_trades.py:72-74`, `src/rwa_calc/engine/ccr/pipeline_adapter.py:127`

- **P6.36** [ ] **NEW (2026-05-28 audit — SFT-FCCM follow-on consistency gap)** — `ccr_method` output column now carries a **third literal** `"fccm_sft"` (emitted at `src/rwa_calc/engine/ccr/sft_fccm.py:252` and documented at `src/rwa_calc/domain/enums.py:459-460`), introduced by the P8.38 SFT-FCCM branch, but the surrounding contracts still encode the two-/one-literal world: `CCRConfig.method` is typed `Literal["sa_ccr"]` at `src/rwa_calc/contracts/config.py:866` (this is the *EAD method* knob, distinct from the new `sft_method` field, but the comment / Literal no longer reflects that `fccm_sft` is a possible *output* discriminator), and the `CCRCalculator` protocol output-schema docstring at `src/rwa_calc/contracts/protocols.py:253-254` still reads "`ccr_method` (pl.String): method discriminator. First batch returns `"sa_ccr"` for every row". Update the protocol docstring to enumerate `{"sa_ccr", "fccm_sft"}` (and the future `"imm"`), and add a one-line note on `CCRConfig.method` clarifying it is the derivative-EAD method while `sft_method` selects the SFT-EAD method. No calculation impact — typing/docstring consistency only, so a downstream consumer reading the protocol contract isn't surprised by the third value. **Effort: S** | Ref: `src/rwa_calc/contracts/protocols.py:253-254`, `src/rwa_calc/contracts/config.py:866`, `src/rwa_calc/engine/ccr/sft_fccm.py:252`
- **P6.37** [ ] **NEW (2026-05-28 audit — Polars forward-compat)** — 70 `.is_in([list-literal])` call sites across `src/rwa_calc/engine/` and `src/rwa_calc/reporting/` trigger the Polars `DeprecationWarning: "is_in with a collection of the same datatype is ambiguous and deprecated. Please use implode to return to previous behavior."` (observed on every CCR acceptance run, e.g. emitted from `engine/materialise.py:146` collect path). Representative sites: `reporting/corep/generator.py:2300,2348,2628,3980,4133,4156,4164,4166,4180`, `engine/classifier.py:313,316,1841,1910`, `engine/hierarchy.py:2494,2779`, `engine/equity/calculator.py:551`, `engine/ccf.py:300`. The warning is currently benign but a future Polars major will change `is_in([...])` semantics or remove the list-collection form. Sweep all 70 sites to the recommended form (wrap the literal in `pl.Series` / use `.implode()` per the polars guidance, or pin the comparison dtype). Pure forward-compat / code-hygiene — no current calculation or test impact (warnings only). **Effort: M** | Ref: https://github.com/pola-rs/polars/issues/22149; `grep -rn "\\.is_in(\\[" src/rwa_calc/` (70 hits)

**Note on P6.16–P6.23 ID collision:** The Tier 6 bullet entries P6.16–P6.23 added in Phase 9 conflict with pre-existing detailed `### P6.16` through `### P6.21` subsections (now removed from this file along with all detailed sections). The Phase 9 entries are distinct new items but reuse the completed IDs. Documented here for a future renumbering pass.

### Tier 7 — Future / v2.0 (Not Yet Planned)

- **P7.1** [ ] Stress testing integration (M4.3)
- **P7.2** [ ] Portfolio-level concentration metrics (M4.4)
- **P7.3** [ ] REST API (M4.5)
- **P7.4** [partial] Additional exposure classes — phase 1 securitisation FLAG + EXCLUDE shipped (`SecuritisationAllocator` stage + aggregator residual multiplier + per-pool summary + per-exposure audit; CRR Art. 109 / 244-246; PRA PS1/26 Art. 147A(1)(j)). Spec at `docs/specifications/securitisation-pool-allocation.md`. **Still open**: SEC-SA / SEC-IRBA / SEC-ERBA RWA framework (CRR Art. 259-264), significant risk transfer assessment (Art. 244-246 conditions), tranche-level capital, shared-collateral CRM re-allocation; CIU beyond fallback, purchased receivables, dilution risk.
- **P7.5** [ ] **NEW** — Additional CRM methods not in scope: Art. 217 basket credit derivatives, Art. 219 on-balance-sheet netting, Art. 214 counter-guarantees by sovereigns, Art. 215(2) mutual guarantee schemes, Art. 222(5) derivative cash collateral 0%, Art. 230(3) UK IRB 50% RW property option, Art. 197(4) unrated bond eligibility route
- **P7.5** [ ] **NEW** — Art. 150(1A) materiality/immateriality thresholds for IRB firms using SA for immaterial classes. Currently only in COREP templates, not in the engine. **Effort: M**
- **P7.6** [ ] **NEW** — Art. 147B roll-out class tracking in the classification engine (currently only in COREP reporting). **Effort: M**
- **P7.8** [ ] **NEW** — CRR Art. 121(4) trade finance preferential RW for unrated institutions (CRR-only, sunsets 31 Dec 2026). Flat 50% (≤1y) / 20% (≤3m), independent of sovereign CQS ladder. CRR SA routes all unrated institutions through Art. 121(1); CQS 2-6 over-weighted. Add `is_trade_finance` Bool, `CRR_ART_121_4_*_RW` constants, gate in CRR institution branch. **Effort: S** | Ref: CRR Art. 121(4); 162(3); 4(1)(80)

### Tier 8 — Counterparty Credit Risk (CCR) Integration (NEW 2026-05-21; reordered 2026-05-22 for first-batch execution)

Major new epic adding Counterparty Credit Risk to the project. CCR is a separate-but-adjacent risk type to default credit risk: it produces the **EAD** for derivative and SFT (securities financing transaction) exposures, which then flows through the existing SA / IRB risk-weight machinery to produce the counterparty default-risk RWA. CVA (Credit Valuation Adjustment) capital is a related but separate framework and is scoped as a sub-epic (P8.60–P8.69).

**Pipeline shape:** the CCR stage sits between `Loader` and `Classifier` (or between `HierarchyResolver` and `Classifier`, TBD per P8.20). It consumes trade-level and netting-set-level inputs, produces exposure-level rows keyed by counterparty, and merges those rows into the existing `ExposureBundle` so downstream SA / IRB / Aggregator stages need minimal change.

**Execution ordering (2026-05-22 operator-approved):** the **first executable batch** (top of this tier) ships Phase 1 foundations + a single end-to-end vertical slice — **CCR-A1** (one unmargined IR swap → SA-CCR EAD → SA-RW → aggregator → pinned acceptance test). Source plan: `.claude/plans/can-we-start-to-linear-steele.md`. Within Tier 8 the first-batch items are listed at the top in dependency order; items marked **deferred to next batch after CCR-A1** are listed below them; items marked **deferred to v2.0** sit in Phase 10 alongside IMM.

**Scope decisions (operator-confirmed 2026-05-22):**
1. **Simplified SA-CCR (Art. 281) and OEM (Art. 282) — deferred to v2.0 / next-major-epic.** Not in initial epic. Sub-threshold portfolios fall through to full SA-CCR (always conservative). P8.21 and P8.22 reclassified accordingly (moved to Phase 10).
2. **CVA — BA-CVA only initially.** P8.60 (BA-CVA), P8.62 (hedges eligibility), P8.63 (aggregated CVA on bundle) stay in scope as the CVA sub-epic. **P8.61 (SA-CVA) deferred to v2.0** (moved to Phase 10).
3. **IMM — confirmed deferred to v2.0** (no change; P8.90 / P8.91 already there).
4. **SFTs — route through CCR with `risk_type=CCR_SFT`, reuse existing CRM haircut tables in `engine/crm/`.** No duplication of the supervisory haircut table. ~~The dual `risk_type` routing is already encompassed by existing tickets (no new P-code).~~ **Correction (2026-05-28):** the closing sentence proved wrong in practice. The current `engine/ccr/sa_ccr.py::compute_ead` runs the derivative `α·(RC+PFE)` formula on `transaction_type="sft"` rows regardless — only `engine/ccr/maturity_factor.py:121-127` branches on the SFT flag (5-BD margined MF floor). For a £60.7M debt-security SFT booked through the `trades` table this gives ~£4M EAD instead of the ~£65M `E·(1+HE)` the FCCM formula would produce. SFT-specific EAD wiring under SA-CCR per CRR Art. 271(2) is now tracked under the new **P8.38** below. The `CCR_SFT` placeholder comment at `src/rwa_calc/domain/enums.py:457` ("Placeholder pending SFT-specific EAD wiring") is the canonical in-code marker.

**Cross-cutting blockers:**
- Trade-level fixtures are a step-change from the current exposure-level parquet shape. `fixture-builder` agent needs new builders before any acceptance scenario can be pinned (P8.40).
- The `output_floor.py` numerator must include SA-CCR-derived EAD × SA-RW for IMM firms (P8.55).

#### First batch — Foundations + vertical slice CCR-A1 (operator-approved 2026-05-22)

The 16 items below are the first executable batch in dependency order. Source plan: `.claude/plans/can-we-start-to-linear-steele.md`. The batch is "done" when CCR-A1 (one unmargined IR swap → SA-CCR EAD → SA-RW → aggregator → pinned acceptance test) is green end-to-end. P8.20 is single-stream (touches `engine/pipeline.py`); the rest can parallelise up to N=3 under `/next-items`.

- ✅ **All 16 first-batch items complete** — P8.1–P8.7, P8.10, P8.12–P8.14, P8.17, P8.20, P8.40, P8.41 (CCR-A1) closed v0.2.11. See **Completed Items** at the foot of this file.
- **P8.48 (partial)** [ ] **NEW — first-batch (16/16) — partial scope: only the articles exercised in this batch** — `@cites` decorators for CRR Art. 271, 272, 274, 275(1), 279a, 279b, 279c(1), 280 on every new function in the formula path that this batch creates. Stack PS1/26 alongside where the article diverges. `uv run watchfire matrix` must produce a clean article→function index. **`watchfire`'s CRR index may need extending to cover Art. 271-280** — that's a sub-task of this ticket, not a reason to skip the citation. Full coverage of Art. 271-311 (including 277/277a/278/281-311) is **deferred** to the corresponding follow-up batches as those formulas land. **Effort: S** | Ref: `docs/development/citation-tracking.md`

#### Deferred to next batch after CCR-A1 (Phase 2 follow-up — SA-CCR formula coverage)

These items are in scope for this epic but NOT in the first executable batch. They land in the immediate follow-up batch(es) once CCR-A1 is green.

- **P8.53** [ ] **NEW (doc-writer D2.78 SA-CCR batch surfacing 2026-05-28 — Tier 1 calculation correctness; renumbered from P8.38 on 2026-05-28 to resolve ID collision with the shipped SFT-FCCM P8.38)** — `apply_wwr_gate` implemented (P8.27, v0.2.11) but **not wired through the orchestrator**. `src/rwa_calc/engine/ccr/wwr.py::apply_wwr_gate` is fully implemented and unit-tested under `tests/unit/ccr/test_wwr.py` (13 tests covering specific-WWR trade break-out into `<ns_id>__wwr__<trade_id>` synthetic NSes + `wwr_lgd_override=1.0` per Art. 291(5)(c) + general-WWR diagnostic CCR011), but `grep -rn "apply_wwr_gate" src/` returns only the definition site at `wwr.py:87` and its docstring references at `wwr.py:5, 52, 61, 106` plus the public re-export in `engine/ccr/__init__.py`. The orchestrator at `src/rwa_calc/engine/pipeline.py:566` calls `apply_legal_enforceability_gate(data.ccr)` followed directly by `ccr_rows_to_exposures(raw_ccr_gated, ...)` at `:567` — `apply_wwr_gate` is **never invoked** (re-verified 2026-05-28). Consequence: no end-to-end SA-CCR run currently routes through the WWR LGD = 100% override per PRA Rulebook CCR (CRR) Part Art. 291(5)(c), so any trade flagged with `is_specific_wwr=True` is treated as a non-WWR trade for LGD purposes (LGD reverts to the standard F-IRB / A-IRB value instead of 100%), understating capital on the affected netting sets. Same pattern as P8.19 (margined-RC wiring gap) and P8.39 below (QCCP risk-weight wiring gap) — implementation slipped past the orchestrator integration step. Suggested wiring location: between `apply_legal_enforceability_gate` at `pipeline.py:566` and `ccr_rows_to_exposures` at `:567` — `raw_ccr_gated = apply_wwr_gate(apply_legal_enforceability_gate(data.ccr))`. Block on: nothing (apply_wwr_gate already returns a `RawCCRBundle`, drop-in compatible with the existing pipeline shape). Tests already exist; only the orchestrator call site needs adding plus an acceptance test exercising the override end-to-end (split a netting set with one specific-WWR trade + one non-WWR trade, assert the WWR trade lands in a `__wwr__` synthetic NS with `wwr_lgd_override=1.0` while the non-WWR trade stays in the parent NS — see P5.17). **Tier 1 candidate** (regulatory correctness — capital understatement on every netting set with a specific-WWR trade). Cross-ref: D2.78 docs spec (`docs/specifications/crr/ccr/wwr.md`) lands the regulatory citation and the engine status note. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 291(5)(c); CRR Art. 291(5)(c); `src/rwa_calc/engine/ccr/wwr.py:87`, `src/rwa_calc/engine/pipeline.py:566-573`, `tests/unit/ccr/test_wwr.py`
- **P8.19** [x] **NEW (doc-writer SA-CCR batch surfacing — Tier 1 calculation correctness)** — Margined RC implemented (P8.11) but **not wired through the orchestrator**. `compute_rc_margined` at `src/rwa_calc/engine/ccr/rc.py:53-79` is fully implemented and unit-tested under `tests/unit/ccr/test_rc_margined.py`, but `src/rwa_calc/engine/ccr/pipeline_adapter.py::ccr_rows_to_exposures` (`:240` `ns_with_ead = compute_pfe(ns_frame, config_ccr)`) only calls `compute_pfe`, which internally inlines the *unmargined* RC at `engine/ccr/pfe.py:110`. The synthetic exposure row emitted at `pipeline_adapter.py:246-271` therefore carries only `rc_unmargined` and never an `rc_margined` column, so the COREP-reconciliation surface and any downstream consumer get the unmargined RC for **every** netting set — including margined ones where the threshold-floor branch `max(V−C, TH+MTA−NICA, 0)` should bind. New docs page `docs/specifications/crr/ccr/rc-calculation.md:370-371` already flags the gap explicitly: "compute_rc_margined ... is not yet routed through the orchestrator pending the margined-netting-set acceptance batch". Per Art. 275(2) the threshold floor can be materially positive for negative MTM with a low / zero collateral threshold, producing an RC understatement on every margined netting set that hits the floor. **Tier 1 candidate** (regulatory correctness — different formula per Art. 275(2), capital understatement on margined netting sets). Implementation: extend `pipeline_adapter.py` to call `compute_rc_margined` on the NS frame (joining MTA / TH / NICA from `raw_ccr.margin_agreements`), then `coalesce(rc_margined, rc_unmargined)` into a unified `rc` column that `compute_pfe` and the synthetic-row selection both consume; the lazy plan stays single-pass. Block on CCR margined-NS acceptance scenarios (pairs with P8.14 margined MF sub-piece — also a CCR-A* acceptance follow-up). **Effort: M** | Ref: PRA PS1/26 Art. 275(2); CRR Art. 275(2); Art. 285 (MPOR floors); `src/rwa_calc/engine/ccr/pipeline_adapter.py:240`, `src/rwa_calc/engine/ccr/rc.py:53-79`, `docs/specifications/crr/ccr/rc-calculation.md:366-395`
- **P8.15** [~] **PARTIAL — closing claim overstated; IR + FX shipped only (batch 20260523-1500); credit / equity / commodity remain open under P8.34** — SA-CCR Hedging sets and asset-class add-on aggregation. `engine/ccr/hedging_sets.py` + `engine/ccr/pfe.py::compute_addon_per_asset_class`. **Correction (2026-05-26 audit, similar to P8.20's "Option B placement" and P8.27's "plan bullet was wrong on three counts" self-corrections):** the original closing summary stated the hedging-set partition covered "credit by reference entity; equity by issuer; commodity by sub-type" — that was incorrect. `assign_hedging_set` at `engine/ccr/hedging_sets.py:140-147` only emits a non-null `hedging_set_id` for `asset_class in {"interest_rate", "fx"}`; all other asset classes fall through to `pl.lit(None, dtype=pl.Utf8)`. `compute_addon_per_asset_class` at `engine/ccr/pfe.py:127-170` correspondingly only dispatches `_compute_addon_ir` and `_compute_addon_fx`; credit / equity / commodity rows return null `asset_class_addon` by design. The shipped IR three-bucket aggregation per Art. 277a(1)(a) and FX per-currency-pair aggregation per Art. 277(3)(a) + CRE52.55 are both correct and pinned. **Remaining work** for credit (single-name + index hedging sets per Art. 277(2)(c) + Art. 277a / CRE52.60-64), equity (single-name + index hedging sets per Art. 277(2)(d) + CRE52.65-66), and commodity (5-bucket partition per Art. 277(3)(b) — electricity / oil_gas / metals / agriculture / other — per CRE52.67-69) is now tracked under the new **P8.34** below (hedging-set extension) and the per-asset-class adjusted-notional + PFE add-on items **P8.35** (credit), **P8.36** (equity), **P8.37** (commodity). Aggregation formulas per Art. 277a using supervisory correlations from P8.7. **Effort: L (originally); residual under P8.34/35/36/37** | Ref: CRR Art. 277, 277a, 280; BCBS CRE52.60-72

#### Phase 3 — Alternative methods, CCP, and edge cases (in scope of epic; later batches)

- **P8.23** [ ] **NEW** — Long-settlement transactions Art. 271(2). Treated under SA-CCR by default but with bespoke MPOR floor. Separate flag `is_long_settlement` on `TradeBundle`. **Effort: S** | Ref: CRR Art. 271(2)
- **P8.39** [ ] **NEW (doc-writer D2.79 SA-CCR batch surfacing 2026-05-28 — Tier 1 calculation correctness)** — `apply_ccp_risk_weight` implemented (P8.25, v0.2.11) but **not wired through the orchestrator**. `src/rwa_calc/engine/ccr/ccp.py::apply_ccp_risk_weight` is fully implemented and unit-tested under `tests/unit/ccr/test_ccp.py` (8 tests covering QCCP proprietary 2% / client-cleared 4% / non-QCCP pass-through across CCR-B1a/b/c, plus EAD-invariance assertions), but `grep -rn "from rwa_calc.engine.ccr.ccp"` returns only test imports (no consumer in `src/`). The orchestrator at `src/rwa_calc/engine/pipeline.py:561-573` calls `apply_legal_enforceability_gate` then `ccr_rows_to_exposures` and exits the CCR stage — `apply_ccp_risk_weight` is **never invoked**. Consequence: every QCCP trade is currently routed through the standard SA institution risk-weight ladder (CRR Art. 120 Table 3 by CQS) instead of the regulatory 2% / 4% pin per PRA Rulebook CCR (CRR) Part Art. 306(1). For a typical CQS-2 institution counterparty the standard SA RW is 50%, so a QCCP-proprietary trade gets 50% instead of 2% and a client-cleared trade gets 50% instead of 4%: a ~25x over-statement of QCCP RWA and ~12.5x for client-cleared. **Conservative mis-pricing (over-states capital) but still regulatorily wrong** — firms over-allocate capital to QCCP exposures. Same pattern as P8.19 (margined-RC wiring) and P8.53 (WWR-gate wiring) — implementation slipped past the orchestrator integration step. Suggested wiring location: after `ccr_rows_to_exposures` returns the synthetic exposure rows in `pipeline.py:582-584`, call `apply_ccp_risk_weight(ccr_exposure_rows, resolved.counterparty_lookup.counterparties, data.ccr.trades)` to overlay the regulatory 2%/4% on QCCP rows (non-QCCP rows get NULL `risk_weight` which the classifier then resolves via the standard SA ladder — the function is already designed for this pass-through pattern). Production wiring needs keyed joins on `counterparty_reference` / `trade_id` (the existing test fixture uses cross-join because it's keyless). Block on: nothing in principle, but pairs with P8.30 (general SA routing for `risk_type=CCR_DERIVATIVE`) — the QCCP pin must dispatch BEFORE the standard SA classifier so the 2%/4% takes precedence. Tests already exist; only the orchestrator call site needs adding plus an acceptance test exercising the override end-to-end (direct-cleared and client-cleared QCCP scenarios under both CRR and B3.1). **Tier 1 candidate** (regulatory correctness — material RWA over-statement on every QCCP exposure). Cross-ref: D2.79 docs spec (`docs/specifications/crr/ccr/ccp-exposures.md`) lands the regulatory citation and the engine status note. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 306(1); CRR Art. 306(1); `src/rwa_calc/engine/ccr/ccp.py:48`, `src/rwa_calc/engine/pipeline.py:561-584`, `tests/unit/ccr/test_ccp.py`
- **P8.49** [ ] **NEW (doc-writer D2.79 SA-CCR batch surfacing 2026-05-28 — Phase 3 CCP; SUPERSEDES P8.26 with concrete scope)** — Art. 308 / 309 default-fund-contribution stack not implemented in any form. **Engine gap survey**: no `K_CCP` / `K_CM` allocation logic anywhere in `src/rwa_calc/engine/ccr/`. The single module `ccp.py` covers Art. 306 trade-exposure RW only (the new `apply_ccp_risk_weight` shipped under P8.25 in v0.2.11); there is no companion module for the default-fund-contribution treatment. `grep -rn "default_fund\|K_CCP\|K_CM\|dfc_capital" src/` returns zero hits. No `default_fund_contributions: pl.LazyFrame | None` field on `RawCCRBundle` (`contracts/bundles.py`); no `DF_CONTRIBUTION_SCHEMA` in `data/schemas.py`; no `compute_dfc_capital` referenced by P8.26's bullet text. Firms with pre-funded default-fund contributions cannot compute that RWA in-system today — the calculator silently omits the contribution from the RWA total. **Required deliverable**: new module `src/rwa_calc/engine/ccr/default_fund.py` implementing (a) the **K_CCP hypothetical-capital alternative formula** per PRA Rulebook CCR (CRR) Part Art. 308(2) (or institution-published K_CCP input — CCP loss-mutualisation simulation is out-of-scope per `IMPLEMENTATION_PLAN.md:394` explicit non-goal), (b) the **K_CM clearing-member allocation** `K_CCP × DF_i / DF_CM`, (c) the **12.5× multiplier** per Art. 308(3) for pre-funded QCCP and Art. 309(2) for non-QCCP (`RWEA = K × 12.5`). New schema additions to `RawCCRBundle`: `default_fund_contributions: pl.LazyFrame | None` (columns: `contribution_id`, `ccp_reference`, `is_qccp_ccp`, `df_i_contribution_amount`, `df_cm_total_contributions`, `k_ccp_published`, optional `is_unfunded_commitment` per Art. 309 unfunded-contributions branch). New `data/tables/default_fund.py` for the `DF_RWA_MULTIPLIER = Decimal("12.5")` constant (matches the existing `engine/ccr/failed_trades.py` 12.5 own-funds-to-RWA factor precedent — keep in data layer per arch_check checks 5/6). Aggregator extension: route the new `rwa_default_fund` column into `AggregatedResultBundle.rwa_ccr_default_fund` (already enumerated in P8.52). Pinned by CCR-B2..B4 acceptance scenarios (direct-cleared QCCP DF contribution + non-QCCP DF contribution + unfunded non-QCCP contribution under Art. 309). Cross-ref: D2.79 docs spec already flags this as pending and forward-cites the new module path. **Note on Art. 307**: the user-supplied finding text referenced "Art. 307 / 308(3) / 309(2)" — PS1/26 Annex R Section 9 confirms the operative articles are **308** (pre-funded QCCP) and **309** (non-QCCP / unfunded), with Art. 307 covering the trade-exposure value definition (not default-fund allocation). The user's K_CCP / K_CM formula maps to Art. 308(2)-(3) and the 12.5× multiplier appears in both Art. 308(3) and Art. 309(2) per `docs/assets/ps126app1.pdf` page 457. **Effort: M** (new module + schema columns + data table + 3 acceptance scenarios + aggregator wiring) | Ref: PRA Rulebook CCR (CRR) Part Art. 308(2)/(3), Art. 309(1)/(2); CRR Art. 308, 309; `docs/assets/ps126app1.pdf` page 457; `src/rwa_calc/engine/ccr/ccp.py`, `src/rwa_calc/contracts/bundles.py`, `src/rwa_calc/data/schemas.py`
- **P8.26** [ ] **SUPERSEDED BY P8.49 (2026-05-28)** — Default fund contributions to QCCP. `engine/ccr/ccp.py::compute_dfc_capital`. Per Art. 308 alternative formula `K_CCP × DF_i / DF_CM`. Non-QCCP default fund contributions: 1,250% RW (Art. 309). P8.49 below carries the load-bearing description (new `default_fund.py` module, schema columns, data-table constants, acceptance scenarios CCR-B2..B4). Note: the original P8.26 reference to "1,250% RW (Art. 309)" was a simplification — Art. 309(2) actually applies the 12.5× multiplier to the K capital requirement (same mechanism as Art. 308(3) for QCCPs), not a flat 1,250% RW on the contribution amount. **Effort: M** | Ref: CRR Art. 308, 309 — see P8.49
- **P8.28** [ ] **NEW (doc-writer SA-CCR batch surfacing — Tier 1 calculation correctness)** — Art. 274(2) α = 1 carve-out **not engine-wired**. `src/rwa_calc/engine/ccr/sa_ccr.py::compute_ead` at `:77-80` reads `alpha_value = float(config.alpha) if config is not None else 1.4` and applies the same scalar uniformly to every netting set; `src/rwa_calc/engine/ccr/pfe.py::compute_pfe` at `:94, 114` does likewise inside the inlined EAD step. PRA PS1/26 Art. 274(2) (and CRR Art. 274(2)) carves out **α = 1** for trades whose counterparty is (i) a **non-financial counterparty** as defined in EMIR Art. 2(9), (ii) a **pension scheme arrangement** as defined in EMIR Art. 2(10), or (iii) a **pension-default compensation entity**. The per-counterparty dispatch is entirely absent: `RawCCRBundle` (`contracts/bundles.py`) has no `counterparty_type` field, `CCRConfig` (`contracts/config.py`) has no per-counterparty alpha override, and the pipeline adapter passes a single scalar `config.alpha` regardless. Capital impact: ~28% understatement of EAD on any in-scope counterparty whose true α should be 1.0 rather than 1.4 (`(1.4 − 1.0) / 1.4 ≈ 28.6%`). The new docs page `docs/specifications/crr/ccr/ead-composition.md:74-93, 310-323, 332-341` already documents the gap with a side-by-side α=1 vs α=1.4 worked example for CCR-A1 (`499,829` vs `699,760`). Implementation: extend `NETTING_SET_SCHEMA` or `COUNTERPARTY_SCHEMA` with a `counterparty_type: pl.String` (or equivalent enum) sourced from EMIR Art. 2(9)/(10) classifications; join into the NS frame in `pipeline_adapter.py` between the v_net/c_net aggregation and `compute_pfe`; replace the scalar `alpha_value` with a per-row `pl.when(counterparty_type.is_in(NON_FIN_OR_PENSION)).then(1.0).otherwise(1.4)` expression; emit a per-NS `alpha_applied` column for COREP reconciliation. **Tier 1 candidate** (regulatory correctness — material EAD understatement on in-scope counterparty types). **Effort: M** (schema extension + dispatch + acceptance scenarios for each carve-out type) | Ref: PRA PS1/26 Art. 274(2); CRR Art. 274(2); EMIR Art. 2(9)-(10); `docs/assets/ps126app1.pdf` page 456; `src/rwa_calc/engine/ccr/sa_ccr.py:77-80`, `src/rwa_calc/engine/ccr/pfe.py:94-115`, `src/rwa_calc/engine/ccr/pipeline_adapter.py:240`
- **P8.29** [ ] **NEW (doc-writer SA-CCR batch surfacing — transitional regulatory feature)** — Art. 274(2A)–(2B) **transitional alpha add-on not implemented**. PRA PS1/26 Art. 274(2A) defines a phased add-on `transitional_add_on = phase_factor × [EAD(α=1.4) − EAD(α=1)]` for **legacy CVA-exempt trades** (trades entered into prior to 1 January 2027), phased in at **60% (2027) → 40% (2028) → 20% (2029) → 0% (2030+)**; Art. 274(2B) excludes this add-on from the leverage-ratio EAD numerator. Nothing in `src/rwa_calc/engine/ccr/` implements this transitional: no `is_legacy_cva_exempt: pl.Boolean` flag on `TRADE_SCHEMA`, no `trade_inception_date`-vs-2027-cutoff gate, no `transitional_add_on` column on the synthetic exposure row, no leverage-ratio-EAD vs default-EAD bifurcation. Documented forward-visibility in the new docs page `docs/specifications/crr/ccr/ead-composition.md:83-94, 338-345, 352-355`: "the engine implements only the steady-state EAD per Art. 274(2); the transitional add-on is a documented gap and will be re-routed once Art. 274(2A) is wired". Should be its own engine batch and a follow-on docs page `docs/specifications/crr/ccr/alpha-add-on-transitional.md` (recommended cross-file move to `DOCS_IMPLEMENTATION_PLAN.md` once this lands). Block on P8.28 (Art. 274(2) carve-out gate — the add-on is defined in terms of the carve-out's `EAD(α=1)` branch). **Tier 8 candidate** (CCR transitional regulatory feature; date-gated 2027-01-01 to 2030-01-01). **Effort: M** (per-trade legacy-vs-new flag + per-year phase-factor table + leverage-ratio bifurcation + acceptance scenarios across the four years) | Ref: PRA PS1/26 Art. 274(2A)–(2B); `docs/assets/ps126app1.pdf` page 456; `src/rwa_calc/engine/ccr/sa_ccr.py`, `src/rwa_calc/data/schemas.py` `TRADE_SCHEMA`

#### Phase 4 — Counterparty default-risk RWA routing (SA + IRB)

- **P8.30** [ ] **NEW** — Route SA-CCR EAD through SA risk weights. `engine/classifier.py` learns `risk_type=CCR_DERIVATIVE` and looks up counterparty class (Institution, Corporate, Sovereign, Retail-derivative). EAD from P8.17 replaces drawn-amount / CCF flow. No changes needed in `engine/sa/`. **Effort: S**
- **P8.31** [ ] **NEW** — Route SA-CCR EAD through IRB (F-IRB / A-IRB). `engine/irb/` integration. IRB derivative exposures use SA-CCR EAD as the EAD input to existing PD/LGD/M machinery. Maturity `M` for derivatives uses Art. 162(2)(g)-(i) special rules (weighted MTM-positive cash flows). **Effort: M** | Ref: CRR Art. 162(2)(g)-(i)
- **P8.32** [ ] **NEW** — CCR-specific PD floor application. Per Art. 153(1)(iii) PD floor of 0.03% is replaced by 0.05% for unrated large financial sector entities under B3.1 (PRA PS1/26 Annex 1 Art. 153). Wire into existing PD-floor logic. **Effort: S** | Ref: PRA PS1/26 Art. 153(1)

#### Phase 5 — CVA capital sub-epic (separable)

- **P8.60** [ ] **NEW (sub-epic)** — BA-CVA (Basic Approach). `engine/cva/ba_cva.py`. Per PRA PS1/26 Art. 383a-c (post-B3.1). Compute `K_reduced` and `K_full` with hedge recognition, aggregate per Art. 383b formula. Output `cva_rwa` field on `AggregatedResultBundle`. **Effort: L** | Ref: PRA PS1/26 Art. 383, 383a-c
- **P8.62** [ ] **NEW (sub-epic)** — CVA hedges eligibility. Per Art. 386 (B3.1 amendments). Eligible single-name and index CDS, with attribution to specific counterparties / sub-portfolios. **Effort: M** | Ref: PRA PS1/26 Art. 386
- **P8.63** [ ] **NEW (sub-epic)** — Aggregated CVA on `AggregatedResultBundle`. New fields: `cva_rwa`, `cva_method`, `cva_hedges_recognised`. Update aggregator to compose default-risk RWA + CVA RWA. **Effort: S**

#### Phase 6 — Reporting (COREP + Pillar III)

- **P8.50** [ ] **NEW** — COREP CCR template suite. `reporting/corep/templates.py` + `generator.py`. New templates: **C 34.01** (CCR analysis by approach), **C 34.02** (SA-CCR), **C 34.03** (IMM, placeholder for v2.0), **C 34.04** (CVA), **C 34.05** (composition of collateral), **C 34.06** (top 10 counterparties), **C 34.07** (IRB CCR — supersedes P2.9), **C 34.08** (exposures to CCPs), **C 34.09** (RWEA flow CCR), **C 34.10** (RWEA flow CVA), **C 34.11** (CCR IRB). **Effort: L** | Ref: COREP Annex II §C 34.01-11; supersedes P2.9
- **P8.51** [ ] **NEW** — Pillar III CCR disclosure templates. `reporting/pillar3/templates.py` + `generator.py`. New tables: **CCR1** (CCR analysis by approach), **CCR2** (CVA capital), **CCR3** (SA EAD by RW), **CCR4** (IRB EAD by PD scale), **CCR5** (collateral composition), **CCR6** (credit derivatives), **CCR7** (RWEA flow under IMM — placeholder), **CCR8** (CCP exposures). **Effort: L** | Ref: PRA PS1/26 Annex XXII §CCR1-8
- **P8.52** [ ] **NEW** — `AggregatedResultBundle` extension for CCR reporting columns. Add: `ead_ccr_total`, `rwa_ccr_default`, `rwa_ccr_qccp_trade`, `rwa_ccr_default_fund`, `cva_rwa`, `failed_trades_rwa`. **Effort: S**

#### Phase 7 — Output floor

- **P8.55** [ ] **NEW** — Output floor S-TREA / U-TREA inclusion. `engine/aggregator/_floor.py`. SA-CCR-equivalent EAD × SA-RW always contributes to S-TREA (the floor numerator). IMM firms (when P8.90 lands) must compute SA-CCR EAD as the floor numerator regardless of which method they use for U-TREA. Until IMM is implemented, S-TREA = U-TREA for CCR (no floor binding for CCR specifically). **Effort: S** | Ref: PRA PS1/26 Art. 92(2A); BCBS CRE52 floor commentary

#### Phase 8 — Fixtures, tests, and documentation

- **P8.42** [ ] **NEW** — Acceptance scenarios **CCR-B1..B5** (CCP exposures). QCCP 2% RW; non-QCCP 1,250%; default-fund alternative formula. **Effort: M**
- **P8.43** [ ] **NEW** — Acceptance scenarios **CCR-C1..C3** (failed trades). DvP escalating multiplier across t+5..t+45. **Effort: S**
- **P8.44** [ ] **NEW** — Acceptance scenarios **CCR-D1..D3** (Simplified SA-CCR + OEM). Threshold-gated portfolios. **Effort: S**
- **P8.45** [ ] **NEW** — Acceptance scenarios **CCR-E1..E5** (default-risk RWA integration). SA-CCR EAD → Institution / Corporate / Sovereign / Retail-derivative routing under both CRR and B3.1. Cross-check that EAD is consistent and RW lookup uses existing tables. **Effort: M**
- **P8.46** [ ] **NEW (sub-epic)** — Acceptance scenarios **CVA-A1..A10** (BA-CVA + SA-CVA). Block on P8.60 / P8.61. **Effort: L**
- **P8.47** [ ] **NEW** — Performance benchmarks for CCR pipeline. `tests/benchmarks/test_ccr_perf.py`. 100k trades / 10k netting sets target wall-clock. Profile group-by-netting-set; consider streaming if memory pressure. **Effort: S**
- **P8.48** [ ] **NEW — residual after first-batch partial** — `@cites` decorators for the **remaining** CRR Art. 271-311 articles + PRA PS1/26 CCR articles that the first batch's partial scope (Art. 271, 272, 274, 275(1), 279a/b/c(1), 280) did not cover: Art. 275(2), 277, 277a, 278, 279a (option-delta sub-branch), 279c(2), 281, 282, 283-294 (IMM — deferred), 295-297, 306-310, 378-380, 386, 291. `watchfire matrix` must produce a clean article→function index for the new module across all batches. **Effort: S** | Ref: `docs/development/citation-tracking.md`

#### Phase 9 — Documentation

- **P8.70** [ ] **NEW** — `docs/specifications/ccr.md` — canonical specification page. Covers: scope, SA-CCR formula chain, simplified / OEM thresholds, CCP treatment, integration into existing pipeline, configuration knobs, CVA scope. **Effort: M** | Owner: `doc-writer` (post-implementation) — tracked in `DOCS_IMPLEMENTATION_PLAN.md`
- **P8.71** [ ] **NEW** — `docs/user-guide/regulatory/ccr.md` — user-facing guide with worked examples. **Effort: M**
- **P8.72** [ ] **NEW** — Update `docs/specifications/architecture.md` data-model section with the four new bundles (`TradeBundle`, `NettingSetBundle`, `MarginAgreementBundle`, `CCRCollateralBundle`). **Effort: S**
- **P8.73** [ ] **NEW** — Update `docs/plans/implementation-plan.md` (the published Zensical narrative) with the CCR roadmap once Phase 1 (P8.1-P8.7) is closed. **Effort: S**
- **P8.74** [ ] **NEW** — `.claude/skills/basel31/references/ccr-changes.md` + `.claude/skills/crr/references/ccr.md` — skill reference files so the `crr` and `basel31` skills can answer SA-CCR queries. **Effort: M**

#### Phase 10 — Deferred (v2.0 / next-major-epic)

- **P8.21** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 1)** — Simplified SA-CCR. `engine/ccr/simplified.py`. Per Art. 281: same PFE structure but **PFE multiplier fixed at 1** and **MF simplification**. Available only for firms below derivative-portfolio threshold (Art. 273a(1)) — config-gated. **Not in initial epic** — sub-threshold portfolios fall through to full SA-CCR (always conservative; no capital understatement risk). **Effort: M** | Ref: CRR Art. 281, 273a(1)
- **P8.22** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 1)** — Original Exposure Method (OEM). `engine/ccr/oem.py`. Per Art. 282: `EAD = (α × notional) × supervisory factor by residual maturity`. Available only for small derivative portfolios (Art. 273a(2)). **Not in initial epic** — sub-threshold portfolios fall through to full SA-CCR. **Effort: S** | Ref: CRR Art. 282, 273a(2)
- **P8.61** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 2 — CVA sub-epic ships BA-CVA only initially)** — SA-CVA (Standardised Approach). `engine/cva/sa_cva.py`. PRA permission gated. Sensitivity-based: delta + vega risk across six risk classes (IR, FX, counterparty credit spread, reference credit spread, equity, commodity). 3-4 months on its own. **Effort: L (3-4 months)** | Ref: PRA PS1/26 Art. 383d-w
- **P8.90** [ ] **DEFERRED v2.0** — Internal Model Method (IMM). CRR Art. 283-294. Large scope: EPE/EEPE simulation engine, alpha calibration, model-validation governance, stressed-EPE add-on, PRA permission process. Recommended only after SA-CCR + CVA are stable in production. **Effort: XL (6-9 months)** | Ref: CRR Art. 283-294; PRA SS12/13 IMM
- **P8.91** [ ] **DEFERRED v2.0** — IMM-specific output-floor S-TREA computation per P8.55. Block on P8.90. **Effort: M**



The bullet list above is the canonical work-queue. The items below have substantive sub-issue structure that doesn't compress cleanly into a single bullet, so they are kept in long-form.

### P1.9 Output Floor — OF-ADJ, portfolio-level application, U-TREA/S-TREA

- **Status:** [~] Partial — sub-items (a) OF-ADJ, (b) portfolio-level, (d) skip-transitional all FIXED; only (c) remains.
- **Remaining (c):** U-TREA/S-TREA COREP export. `OutputFloorSummary` is on `AggregatedResultBundle`, but full `OF 02.01` COREP template wiring (4-column comparison) not yet done — tracked under P2.1.
- **File:Line:** `engine/aggregator/_floor.py`, `_schemas.py`, `aggregator.py`, `contracts/bundles.py`
- **Spec ref:** `docs/specifications/output-reporting.md`, PRA PS1/26 Art. 92(2A)/(3A)/(5)
- **Tests:** 80 unit tests across `test_portfolio_level_floor.py`, `test_of_adj.py`, `test_output_floor_skip_transitional.py`.

### P2.1 COREP template rework — structure alignment

- **Status:** [~] Needs rework
- **Impact:** Current COREP generator (`reporting/corep/generator.py`) uses simplified column sets and one-row-per-class structure. Only C 07.00, C 08.01, C 08.02 (and OF variants) are implemented. Full-width CRR/B31 column definitions exist in `templates.py` (lines 1-651) but generator uses backward-compatibility aliases. Specific sub-gaps:
  - C 08.01 col `0120` ("Of which: off balance sheet") still null — needs off-BS EAD pipeline column
  - B31 OF 08.02 missing columns `0001` and `0101-0105` (per-grade CCF breakdown)
  - B31 C 08.01 off-BS CCF sub-rows `0031-0035` always null
  - C 07.00 B31 CIU sub-rows `0284/0285` defined but never populated by generator
  - B31 slotting FCCM cols `0101-0104` in C 08.01 still null — pipeline FCCM for slotting not yet wired
  - **From PDF comparison:** OF 07.00 missing cols 0230/0235/0240 (ECAI breakdown). OF 09.02 missing cols. OF 08.01 missing cols 0254/0265/0282; col 0280 renamed. OF 02.00 needs rows 0034/0035/0036 (floor indicator/multiplier/OF-ADJ). OF 08.06 col 0070 removed in B3.1; col 0031 is a deduction. OF 08.07 cols 0160-0180 consolidated-basis-only. OF 09.01 missing col 0061.
- **File:Line:** `reporting/corep/generator.py`, `reporting/corep/templates.py`
- **Fix:** Migrate generator to use full template definitions. Rework row/column logic. Add missing pipeline columns for equity transitional and currency mismatch reporting. Remove dead alias objects.
- **Tests needed:** Rewrite COREP tests (~250 tests in `tests/unit/test_corep.py`).

### P8 Tier 8 — CCR Integration (long-form roadmap)

- **Status:** [ ] Not started — new epic added 2026-05-21
- **Total effort:** L–XL across 30+ tickets, sequenced over 6–9 months for SA-CCR + CVA; IMM deferred to v2.0 (+6 months).
- **Headline rationale:** the calculator currently has no representation of derivative or SFT exposures. Trade-level data, netting sets, and margin agreements have no schema; the pipeline has no stage that produces an EAD from those inputs. Without CCR, the calculator cannot produce a complete RWA number for any firm with non-trivial derivative activity. SA-CCR is the regulatory floor method post-CRR2 and is mandatory under PRA PS1/26 for all firms above the small-derivative-portfolio threshold (Art. 273a). CVA capital is a separable but adjacent epic that depends on SA-CCR EAD.

#### Reference materials
- **CRR Title II Chapter 6 (Articles 271-311)** — counterparty credit risk methods. PDF in `docs/assets/` (search "CCR").
- **CRR Title VI (Articles 381-386)** — own funds requirements for CVA risk.
- **PRA PS1/26 Appendix 1** — UK Basel 3.1 amendments to CCR and CVA. `docs/assets/ps126app1.pdf`.
- **BCBS CRE52** — SA-CCR methodology. https://www.bis.org/basel_framework/standard/CRE.htm (sub-pages CRE52, CRE53 IMM, CRE54 simplified).
- **BCBS MAR50** — minimum capital requirements for CVA risk.
- **PRA SS12/13** — Counterparty credit risk model permissions (IMM governance; v2.0 reference only).

#### Architectural impact summary

| Layer | Net new files / surfaces |
|---|---|
| `contracts/bundles.py` | `TradeBundle`, `NettingSetBundle`, `MarginAgreementBundle`, `CCRCollateralBundle`, `RawCCRBundle`; field on `RawDataBundle`; CCR fields on `AggregatedResultBundle` |
| `contracts/protocols.py` | `CCRCalculator` Protocol |
| `contracts/config.py` | `CCRConfig` dataclass; factory wiring on `crr()` / `basel_3_1()` |
| `engine/ccr/` | `sa_ccr.py`, `rc.py`, `pfe.py`, `hedging_sets.py`, `supervisory_delta.py`, `adjusted_notional.py`, `maturity_factor.py`, `simplified.py`, `oem.py`, `ccp.py`, `failed_trades.py`, `wwr.py`, `namespace.py` |
| `engine/cva/` | `ba_cva.py`, `sa_cva.py`, `hedges.py` (sub-epic) |
| `data/tables/` | `sa_ccr_factors.py` (supervisory factors, correlations, maturity scalers, PFE multiplier constants) |
| `data/schemas.py` | `TRADE_SCHEMA`, `NETTING_SET_SCHEMA`, `MARGIN_AGREEMENT_SCHEMA`, `CCR_COLLATERAL_SCHEMA`; CCR-specific enums (asset class, transaction type, margin type) |
| `engine/pipeline.py` | New stage between Loader/Hierarchy and Classifier, wrapped with `stage_timer` |
| `engine/loader.py` | Read 4 new parquet sources |
| `engine/classifier.py` | Route `risk_type=CCR_DERIVATIVE` rows to existing counterparty class lookup |
| `engine/aggregator/_floor.py` | SA-CCR EAD always contributes to S-TREA; bookkeeping for IMM firms (deferred) |
| `reporting/corep/` | 11 new templates C 34.01-11 |
| `reporting/pillar3/` | 8 new templates CCR1-8 |
| `tests/fixtures/ccr/` | Trade / netting-set / margin builders |
| `tests/acceptance/ccr/` | ~30 CCR-* / CVA-* scenarios |
| `tests/benchmarks/` | `test_ccr_perf.py` |
| `docs/specifications/ccr.md` | Canonical spec page |
| `.claude/skills/{crr,basel31}/references/` | `ccr.md`, `ccr-changes.md` |

#### Sequencing & dependency notes

1. **Phase 1 (P8.1–P8.7)** must land first — without bundles + loader + config + supervisory factors, nothing downstream can be tested.
2. **Phase 2 (P8.10–P8.18)** is the SA-CCR core. It must close before Phase 4 (default-risk RWA integration) — there is no EAD to route otherwise.
3. **Phase 3 (P8.20–P8.27)** can run in parallel with Phase 4 once Phase 2 closes. P8.20 (pipeline integration) is the linchpin.
4. **Phase 4 (P8.30–P8.32)** unblocks the first end-to-end acceptance scenarios.
5. **Phase 5 (P8.60–P8.63 CVA)** is a separable sub-epic. Can start once Phase 4 closes; does not block default-risk CCR shipping.
6. **Phase 6 (P8.50–P8.52 reporting)** depends on Phase 4 (CCR columns on `AggregatedResultBundle`) and Phase 5 (CVA columns) for full content.
7. **Phase 7 (P8.55 output floor)** is a small add once Phase 4 is wired.
8. **Phases 8–9 (fixtures, tests, docs)** run alongside each phase, not after — agents (`fixture-builder` → `test-writer` → `engine-implementer`) follow the per-item pipeline established in `/next-items`.

#### Scope decisions (operator-confirmed 2026-05-22)

1. **Simplified SA-CCR (Art. 281) + OEM (Art. 282) — deferred to v2.0 / next-major-epic.** Not in initial epic. Sub-threshold portfolios fall through to full SA-CCR (always conservative; no capital understatement risk). P8.21 and P8.22 moved to Phase 10.
2. **CVA epic — BA-CVA only initially.** P8.60 (BA-CVA), P8.62 (hedges eligibility), P8.63 (aggregated CVA on bundle) stay in scope. **P8.61 (SA-CVA) deferred to v2.0** (moved to Phase 10).
3. **IMM — confirmed deferred to v2.0.** Model-governance footprint disproportionate to scope. P8.90 / P8.91 in Phase 10.
4. **SFTs — route through CCR with `risk_type=CCR_SFT`, reusing existing CRM haircut tables in `engine/crm/`.** No duplication of the supervisory haircut table. The dual `risk_type` routing is already encompassed by existing tickets (P8.5 schemas extend `risk_type`; P8.20 pipeline integration; P8.30 SA routing) — no new P-code needed.

#### Risk register

- **R-CCR-1** — Trade-level data is a step-change. Existing fixtures are exposure-level. Mitigation: build minimal CCR-A1 (one trade, one netting set, unmargined IR swap) in Phase 1 alongside loader work, not at the end.
- **R-CCR-2** — Netting-set group-by is a new performance class. Polars handles this well, but every per-trade column → per-netting-set aggregation is a shuffle. Mitigation: benchmark from CCR-A2 onward, not at the end.
- **R-CCR-3** — Supervisory delta for options requires Φ(d1). Already have `polars-normal-stats` in deps (used by IRB). Verify dependency covers our needs.
- **R-CCR-4** — α=1.4 is regulator-default; some firms have PRA permission for firm-specific α. Config-driven from day 1 (P8.6) avoids retrofitting.
- **R-CCR-5** — COREP C 34.x templates are large (~11 templates, dozens of columns each). Mitigation: ship the SA-CCR templates (C 34.01, C 34.02, C 34.06, C 34.07, C 34.08) first; defer IMM/CVA-only templates (C 34.03, C 34.04, C 34.10, C 34.11) until those engines exist.

#### Out-of-scope (explicit non-goals for this epic)

- **Margin model permission** — we compute SA-CCR assuming margin agreement inputs are accurate; we do not validate or model the margin process itself.
- **CCP loss-mutualisation simulation** — `K_CCP` is treated as a CCP-published input, not computed from the calculator.
- **Securities financing transactions exhaustive coverage** — only those SFTs already covered by CCR Art. 271(2)(b) are in scope. Repo/reverse-repo/securities lending under comprehensive method stay in CRM (see scope decision 4 above).
- **Long-settlement transaction modelling beyond the MPOR floor** — Art. 271(2) explicit, but bespoke modelling is not.
- **Market risk on derivatives** — only counterparty default risk (and CVA in sub-epic). Trading book / FRTB market risk is a separate project.

---

## Audit History

For per-pass detail see git history (`git log -p IMPLEMENTATION_PLAN.md`) and `docs/appendix/changelog.md`.

| Pass | Date | New items | Notes |
|------|------|-----------|-------|
| Operator-directed curator | 2026-05-28 | 4 new (P8.38, P8.39, P8.49, P6.35) | Append-only pass routing 4 code-side findings surfaced by `doc-writer` agents during the D2.77 / D2.78 / D2.79 SA-CCR docs batch `be337fa` on 2026-05-28. **New P-codes**: **P8.38** [renumbered → **P8.53** on 2026-05-28 to resolve a collision with the shipped SFT-FCCM P8.38] (Phase 2 follow-up; Tier 1 correctness — `apply_wwr_gate` implemented under P8.27 v0.2.11 but never invoked by `engine/pipeline.py:561-573`, so the Art. 291(5)(c) LGD=100% override is dead code for end-to-end runs); **P8.39** (Phase 3 CCP; Tier 1 correctness — `apply_ccp_risk_weight` implemented under P8.25 v0.2.11 but never invoked, so QCCP trades get 50% institution SA RW instead of regulatory 2%/4% per Art. 306(1) — conservative mis-pricing but regulatorily wrong); **P8.49** (Phase 3 CCP; **supersedes P8.26** with concrete deliverable scope — new `engine/ccr/default_fund.py` module, schema columns on `RawCCRBundle`, `data/tables/default_fund.py` for the 12.5× multiplier constant, acceptance scenarios CCR-B2..B4, aggregator wiring per existing P8.52); **P6.35** (Tier 6 citation cleanup — `apply_ccp_risk_weight` at `engine/ccr/ccp.py:47` carries `@cites("CRR Art. 307")` for the 4% client-cleared RW, but PS1/26 Annex R Section 9 places both 2% and 4% in Art. 306(1); runtime correct, metadata-only fix). **Re-scoped**: **P8.26** marked SUPERSEDED BY P8.49 with cross-ref note (kept as a tracking entry so the prior bullet's "1,250% RW" simplification doesn't leak forward). Verified by Skill `basel31` lookup + `docs/assets/ps126app1.pdf` page 455-457 (PS1/26 Annex R Section 9 confirms Art. 306 = trade-exposure RW, Art. 308 = pre-funded QCCP DF, Art. 309 = non-QCCP DF) + `grep -rn` confirming neither `apply_wwr_gate` nor `apply_ccp_risk_weight` is invoked outside its own definition site + tests. Header re-baselined (0.2.15 → 0.2.16 per `pyproject.toml`). No other plan sections touched; no items closed; no cross-file moves recommended (all four are unambiguously engine code-side). |
| Curator | 2026-05-28 (audit) | 3 new (P5.17, P6.36, P6.37) | Audit pass against `src/`, `tests/`, `docs/specifications/`, regulatory PDFs + `crr`/`basel31` Skills. **Closed (silent ship):** CCR-A3 / CCR-A5 / CCR-A7 / CCR-A8 — all four under the P8.41 umbrella showed `[ ]` but have full acceptance tests + golden JSONs and pass green (26 tests); marked the whole P8.41 CCR-A1..A12 series complete. **Resolved ID collision:** two `P8.38` bullets — WWR-gate-wiring item renumbered → **P8.53**; SFT-FCCM item keeps `P8.38 [x]`. **Re-scoped file:line drift:** P2.21 (`:509,511,644,646,682` → `:510,512 / :645,647 / :683,684`), P1.108 (`formulas.py:360`→`:383-384`, `namespace.py:488`→`:720,887`), P6.31 (`config.py:951-952`→`:958-959`). **New items:** P5.17 (WWR/CCP end-to-end acceptance companion to P8.53/P8.39), P6.36 (`ccr_method` third-literal `"fccm_sft"` vs `Literal["sa_ccr"]` / protocol-docstring consistency), P6.37 (70 `.is_in([list])` Polars deprecation sites). **Verified open without change:** P1.130 (pre-floor summaries — output floor confirmed portfolio-level via basel31 skill), P1.183 (CRR Art. 164(4) 10%/15% portfolio LGD floor confirmed against `crr.pdf` page 160; `LGDFloors.crr()` returns zeros), P6.30 (dead `compute_pfe_ir_singleton` stub), P6.32 (two CCF TODOs), P8.53/P8.39 (re-confirmed neither `apply_wwr_gate` nor `apply_ccp_risk_weight` invoked in `pipeline.py`). Header version-drift noted (P8.38 claims v0.2.17 but `pyproject.toml` still 0.2.16). Tier 3 COREP line refs flagged as broadly stale (generator now 4,680 lines) but not re-scoped this pass. |
| Curator | 2026-05-26 (audit) | 3 new (P6.30, P6.31, P6.32) | Audit pass against `src/`, `tests/`, `docs/specifications/`, regulatory PDFs. **Closed silent-fix:** P4.30 (regulatory-compliance matrix now has B31-K/L/M and CRR-J; CRR-D2 covered by D1–D6); P4.49 (cited file `irb/ccf.py` doesn't exist; live `engine/ccf.py` already uses Art. 166(8)). **Re-scoped file:line drift:** P1.188 (`schemas.py:1446` → `:1904`; also added pyproject.toml ref); P2.21 (`pipeline.py:348-356` → `:509, 511, 644, 646, 682`); P2.40 (`hierarchy.py:~2297-2331` → `:2211-2255`); P4.48 (`irb/firb_lgd.py` → `data/tables/firb_lgd.py`; line list updated to `:289, 296, 778, 797, 846, 874`); P4.51 (line list updated to `:52` + `:106`; narrowed to 2 typos, dropped the false third claim about line 91). **New items:** P6.30 (dead-stub `compute_pfe_ir_singleton` superseded by P8.16 `compute_pfe`); P6.31 (undocumented `audit_cache_dir` / `audit_cache_max_runs` — companion to P4.53); P6.32 (two `TODO` markers for CRE32.27 + Art. 166D(5)(b) 0.5 floor-multiplier hoist). Header re-baselined (package version 0.2.10 → 0.2.15; TODO count 0 → 2). Spot-checked all Tier 1 items: P1.94, P1.108, P1.130, P1.183, P2.21, P6.16, P6.24, P6.25 all verified open without change. Tier 8 CCR items not re-audited this pass — last touched 2026-05-26 in prior pass, most closed or actively shipping. |

---

## Completed Items (Reference)

Closure detail (citations, file:line, hand-calcs, pinning tests) is preserved in git history (`git log -p IMPLEMENTATION_PLAN.md`) and `docs/appendix/changelog.md`. IDs are listed by closing version below.

**Closed by version**

- **v0.2.6:** P1.97, P1.98, P1.106, P1.112, P1.114, P1.158, P1.169
- **v0.2.7:** P1.107, P1.117, P1.124, P1.125, P1.126, P1.144, P1.156, P1.182, P1.187
- **v0.2.10:** P1.100, P1.101, P1.177
- **v0.2.11:** P1.104, P1.157, P1.181
- **v0.2.12:** P1.105 (was P7.7), P1.164
- **v0.2.13:** P1.103, P1.128, P1.186
- **v0.2.14:** P1.96, P1.118 (partial — FX-settlement / securities-settlement sub-paragraphs deferred), P1.120
- **v0.2.15:** P1.151, P1.162, P1.184
- **v0.2.16:** P1.93, P1.159, P2.14
- **v0.2.17:** P1.109, P1.110, P1.160
- **v0.2.18:** P1.145, P1.165, P1.180
- **v0.2.19:** P1.146, P1.147, P2.39
- **v0.2.20:** P1.140, P1.161
- **v0.2.21:** P2.34, P6.18, P6.20
- **v0.2.22:** P1.123, P1.154 (regression-guard)
- **v0.2.23:** P1.95, P2.42, P6.19
- **v0.2.24:** P1.127, P2.22 (regression-guard)
- **v0.2.25:** P1.179 (regression-guard), P2.12, P2.35
- **v0.2.26:** P2.18 (regression-guard), P2.24
- **v0.2.27:** P1.166
- **v0.2.28:** P1.163, P1.168, P1.185
- **v0.2.29:** P2.17, P2.43, P6.26
- **v0.2.30:** P2.20, P2.36

**Closed — Tier 1 / Tier 6 (inline `[x]` pruned to this list 2026-05-28)**

- **v0.2.15:** P1.122 (all 3 sub-claims; (a) v0.2.30, (b) v0.2.15, (c) v0.2.22), P6.16, P6.22
- **Unreleased (`feat/ccr-updates`):** P1.189, P1.190 (Basel 3.1 F-IRB FCCM collateral, Art. 230)

**Closed — Tier 8 Counterparty Credit Risk (SA-CCR first batch + Phase 2/3 follow-ups)**

Shipped across dev batches on `feat/ccr-updates`; full closure detail (file:line, hand-calcs, pinning tests) in git history + `docs/appendix/changelog.md`.

- **v0.2.11 — foundations + CCR-A1 vertical slice:** P8.1, P8.2, P8.3, P8.4, P8.5, P8.6, P8.7, P8.10, P8.11, P8.12, P8.13, P8.14, P8.16, P8.17, P8.18, P8.20, P8.24, P8.25, P8.27, P8.40, P8.41 (CCR-A1)
- **v0.2.15 — credit / equity / commodity asset classes + acceptance scenarios:** P8.33, P8.34, P8.35, P8.36, P8.37; P8.41 sub-scenarios CCR-A4 / A6 / A9 / A10
- **v0.2.17 — SFT FCCM EAD branch** (note: `pyproject.toml` still 0.2.16 — version-string drift to reconcile): P8.38
- **Unreleased (`feat/ccr-updates`, 2026-05-28 audit silent-ship):** P8.41 sub-scenarios CCR-A3 / A5 / A7 / A8

**Closed without a code change**

- Silent fixes (closed during 2026-05-03 audit): P1.99, P1.121, P1.132
- Closed-claim-invalid: P1.131; P1.94 sub-claim (c)
- Silent fixes (closed during 2026-05-16 audit): P1.137, P1.138 (equity transitional Rule 4.2/4.3 ladder + Rule 4.6 IRB higher-of were already implemented in `engine/equity/calculator.py::_apply_transitional_floor` via `pl.max_horizontal(risk_weight, transitional_rw)`)
- Closed-claim-invalid (2026-05-16 audit): P1.171 (CIU=1,250% already), P4.26 (duplicate of P1.163), P4.27 (skill ref already correct)
- Tracking-entry cleanups (2026-05-16 audit): P1.167 (was P2.14), P1.174 (was P1.120), P2.13 (was P1.123)
- Silent fixes (closed during 2026-05-26 audit): **P4.30** (regulatory-compliance matrix groups B31-K / B31-L / B31-M / CRR-J all present in `docs/specifications/regulatory-compliance.md:80,99-101`; CRR-D enumeration at `:73` covers D1–D6), **P4.49** (the cited file `irb/ccf.py` does not exist — the IRB CCF logic lives in the single `engine/ccf.py` module and already uses `Art. 166(8)` consistently; no `Art. 166(9)` occurrence remains in `engine/`)
