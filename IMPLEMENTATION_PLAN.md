# Implementation Plan

**Last updated:** 2026-07-07 — plan-curator fold of the **CRR / PS1-26 Art. 111–241 compliance audit** (`docs/plans/compliance-audit-crr-111-241-rectification.md`): **+81 items P1.216–P1.296**. The 7 high-severity items are hoisted to the head of Tier 1; the 41 medium + 28 low capital-correctness items are grouped by workstream (WS1–WS11) in a dated Tier 1 block; 5 pure-citation / spec-staleness lows (P1.266, P1.269, P1.280, P1.283, P1.295) went to Tier 5. **48 audit findings were already tracked** (no new P-code — §6, mapped onto P1.208/P1.183/P1.201/P1.199/P1.203/P1.209/P1.210/P1.178/P1.10/P1.198/P1.204–P1.207/P2.37/P2.47/P2.50–P2.52/P4.51/P5.16/P6.27/P6.40/P6.44/P7.4/P7.5 + the margined-sft-fccm-extension plan) and **3 were refuted** (§7 — do not re-file). **P1.221 (WS5) must run SINGLE-STREAM** (writes `engine/aggregator/_el_summary.py`). Schema-enablement-first items (nullable, conservative default, P2.40a→P2.40b pattern): P1.227, P1.229, P1.239, P1.240, P1.259, P1.261. Prior update 2026-06-01 — workflow regulatory-conformance-audit 2026-06-01: +36 items in new Tier 9 (24 capital-impacting P1.191–P1.214, 3 reporting P2.50–P2.52, 9 code-quality P6.38–P6.46); each independently reviewer-confirmed. Prior 2026-06-01 housekeeping prune: 33 newly-completed [x] items removed from the work-queue and recorded by closing version in the *Completed Items* reference list (foot of file); closure detail (citations, file:line, hand-calcs, pinning tests) preserved in git history (`git log -p IMPLEMENTATION_PLAN.md`) + `docs/appendix/changelog.md`. P8.19 reopened ([x] body described unfinished orchestrator wiring). Prior 2026-05-28 curator audit pass + housekeeping: completed `[x]` items pruned to the *Completed Items* reference list (foot of file); inline per-pass audit summaries collapsed into the Audit History table (full detail in git history + `docs/appendix/changelog.md`).
**Package version:** 0.2.21 in `pyproject.toml` 

_Per-pass audit narrative now lives in git history (`git log -p IMPLEMENTATION_PLAN.md`), `docs/appendix/changelog.md`, and the Audit History table near the foot of this file; the verbose inline summaries were removed 2026-05-28 to keep this file readable._

---

## Status Legend

- [ ] Not started
- [~] Partial / needs rework
- [x] Complete (pruned to the reference list at the end of this file)

---

## Architecture Migration (cross-reference)

> **2026-06-11:** A full architecture review produced a target architecture (regime-as-data rulepacks, producer-sealed eager stage edges, uniform stage model) and a phased (0–8) strangler migration plan: **`docs/plans/target-architecture-migration.md`**. Phases 0 and 1 landed 2026-06-11 (merged via PR #386); Phase 2 (dead-path deletion, branch-path error channel, protocol diet, sentinel-to-None) landed 2026-06-12 on `feat/phase2`. Next: Phase 3 (producer-sealed edge contracts). Plan-curator: schedule migration work per phase from that document rather than minting parallel P-codes; phase-relevant existing items are called out in its "folds in" notes (e.g. P2.21 → Phase 4; P1.183/P2.16b → Phase 4 aggregate-stage conversion; P8.39/P8.53/P5.17 CCR wirings → Phase 4 "ordinary registered stage" work; P2.5/P2.7/P2.25/P2.27/P2.28/P4.20 → Phase 7 per-template modules). The do-not-do register in that plan is binding for all batches (notably: no bulk golden regeneration; no agent edits to `tests/oracle/`; Float/String nulls never filled to 0.0).

## Remaining Work — Prioritized Bullet List

Items are sorted by priority. Each item notes its ID, status, and effort estimate (S/M/L).

### Tier 1 — Calculation Correctness (must fix for regulatory accuracy)

> **2026-07-07 compliance-audit intake (Art. 111–241, both regimes).** The 7 high-severity items below are the priority head of Tier 1. Each is a localised change with a hand-calc and (mostly) an existing mis-pinned test to invert; full evidence/fix prose lives in `docs/plans/compliance-audit-crr-111-241-rectification.md` §1/§5. Their medium/low workstream siblings sit in the dated "2026-07-07 audit" block lower in Tier 1. Direction of error is noted per item.

- **P1.216** [x] FIXED (Unreleased; closes cluster P1.224/P1.226 — added cited pack table `crr_short_term_ecai_risk_weights` + `has_short_term_ecai`-gated CRR institution & corporate Art. 131 Table 7 branches; pinned by `tests/acceptance/crr/test_p1_216_art_131_table_7_short_term_ecai.py`) **(WS1 · HIGH)** CRR short-term-rated bank/corporate paper never routed through Art. 131 Table 7 — `apply_short_term_rating_override` sets `has_short_term_ecai` under both regimes but only the B31 Table 4A/6A branches read it; the CRR ladder has no Table 7 branch, so ST-CQS2/3 paper is under-weighted (e.g. 20% vs 50%/100%). Add cited pack table `crr_short_term_ecai_risk_weights` (20/50/100/150/150/150) + `has_short_term_ecai`-gated CRR institution & corporate branches. **Understatement. Effort: M** | Ref: CRR Art. 131 (Table 7) · Ev: `engine/stages/hierarchy/enrich.py:158-240`, `engine/sa/risk_weights.py:805-832,1202-1368` · §5 WS1
- **P1.217** [x] FIXED v0.3.14 (CRR OC 50/20 CCF split now keys on ORIGINAL maturity, not residual — `_apply_oc_original_maturity_ccf` prefers `original_maturity_years`, else `maturity_date - value_date`, else conservative 50%; added nullable `original_maturity_years` to FACILITY/CONTINGENTS schemas; pinned by `tests/unit/test_ccf.py::TestOtherCommitCCF::test_sa_pipeline_oc_50_percent_crr_seasoned_original_maturity`) **(WS10 · HIGH)** CRR "other commitments" 50%/20% CCF split keyed on remaining maturity, not original — Annex I sets 50% (MR) when the undrawn facility's *original* maturity > 1yr and 20% only when *original* maturity ≤ 1yr (verified crr.pdf pp.433-434; skill confirms the 50/20 split), but `_compute_ccf` tests `maturity_date − reporting_date`, so every seasoned >1yr revolver tagged `risk_type=OC` drops to 20% CCF in its final year. Derive original maturity (prefer `original_maturity_years`, else `maturity_date − start_date`), default conservative 50%. **Understatement. Effort: M** | Ref: CRR Art. 111(1) / Annex I items 2(b),3(b) · Ev: `engine/ccf.py:486-501`, `rulebook/packs/common.py:603-607` · §5 WS10
- **P1.218** [x] FIXED v0.3.14 (guarantee coverage fraction Eg=min(GA,E) now measured on the CCF=100% basis `ead_for_crm` via `_crm_basis`, not post-CCF `ead_after_collateral`; total EAD invariant, only RWA re-split (worked case 180k->340k); pinned by `tests/acceptance/crr/test_p1_218_guarantee_coverage_ccf_basis.py`) **(WS2 · HIGH)** Guarantee coverage fraction computed on post-CCF EAD, not the CCF=100% basis — Eg = min(GA, E) must measure E at 100% of an off-B/S item's value (CCF re-applied to covered/uncovered parts afterwards), but the split caps and pro-rates coverage against `ead_after_collateral` (post-CCF), over-recognising cover on undrawn commitments (≈47% RWA under-statement in the worked case). Cap/derive the ratio against `ead_for_crm`. **Understatement. Effort: M** | Ref: CRR Art. 235(1)/236(3); PS1/26 Art. 235(1)(a)/236(1)(a) · Ev: `engine/crm/guarantees.py:647-668,916,939-944`, `engine/crm/collateral.py:1079-1089` · §5 WS2
- **P1.219** [x] FIXED v0.3.15 (residual protection maturity now drives guarantee maturity-mismatch `t` per Art. 238(1) — `_apply_maturity_mismatch_to_guarantees` prefers the residual from `maturity_date`, `original_maturity_years` fallback only; Art. 237(2)(a) ≥1y gate untouched; pinned by `tests/acceptance/crr/test_p1_219_guarantee_maturity_mismatch_residual.py`; companion P1.109/P1.200 re-pinned) **(WS2 · HIGH)** Guarantee maturity-mismatch `t` uses original term, not residual — in GA = G*·(t−0.25)/(T−0.25), `t` is the years *remaining* to protection maturity (Art. 238), but `_apply_maturity_mismatch_to_guarantees` prefers `original_maturity_years` over the residual from `maturity_date`, so any dataset that populates the original term for the Art. 237(2)(a) 1yr gate silently over-recognises seasoned guarantees. Invert the preference (residual wins; original only as fallback). **Understatement. Effort: M** | Ref: CRR Art. 239(3) w/ 238(1); PS1/26 Art. 239(3) · Ev: `engine/crm/guarantees.py:1330-1336`, `data/schemas.py:588-592` · §5 WS2
- **P1.220** [x] FIXED v0.3.14 (`rgla_institution`/`pse_institution` added to `B31_SOVEREIGN_LIKE_ENTITY_TYPES` so `b31_sa_only` forces SA for the quasi-sovereign class under the `approach_restrictions_b31_applicable` Feature; premise CONFIRMED against primary ps126app1.pdf p.89 Art. 147(3)(c)-(e) w/ 147A(1)(a) — 0%-RW qualifier binds only (g) international orgs, so RGLAs/LAs/PSEs are quasi-sovereign unconditionally; NB repo skill/specs mis-state this, follow-on docs item; pinned by `tests/acceptance/basel31/test_p1_220_pse_institution_sa_only.py`) **(WS5 · HIGH)** Institution-typed RGLAs/PSEs stay IRB-modellable under Basel 3.1 — PS1/26 Art. 147(3)(c)-(e) assigns regional govts / local authorities / PSEs to the quasi-sovereign class unconditionally and Art. 147A(1)(a) mandates SA for that class, but `B31_SOVEREIGN_LIKE_ENTITY_TYPES` excludes `rgla_institution`/`pse_institution` so an internally-rated one with an institution model permission gets F-IRB (modelled RW below the SA floor). Add them to a B31 SA-only set under the `approach_restrictions_b31_applicable` Feature. **Understatement. Effort: M** | Ref: PS1/26 Art. 147(3)(c)-(e) w/ 147A(1)(a) · Ev: `data/schemas.py:1538-1548`, `engine/stages/classify/approach.py:204-215` · §5 WS5
- **P1.221** [x] FIXED (Unreleased — `compute_el_portfolio_summary` now derives nd/d shortfall/excess from the AGGREGATE default-status pool sums `max(0, ΣEL−Σpool_b)` instead of summing the per-row floored columns; per-row audit columns, two-branch selector and T2 cap unchanged; no-EL-data frames keep the per-row fallback; pinned by `tests/unit/test_el_two_branch.py::TestP1221PoolAggregateNetting` + 4 inverted mixed-sign pins + re-derived CRR-G acceptance (1,250 shortfall / 861,274.40 excess); P1.127 guard green; reviewer pass) **(WS5 · HIGH) — ran SINGLE-STREAM** (`engine/aggregator/_el_summary.py` is a hard-excluded shared file; pick alone via `/implement-scenario P1.221`, mirrors the P1.183/P2.16b guardrail) — IRB EL-vs-provision netting done per-row-then-summed instead of pool-aggregate — Art. 159 requires comparing *total* EL against *total* eligible provisions per default-status pool, but `adjustments.py` computes per-row `max(0, EL_i − poolB_i)` shortfall/excess and `_el_summary.py` then sums the already-floored row values, so intra-pool netting never happens — over-stating both the CET1 shortfall deduction and the T2 excess credit. Aggregate raw sums per pool, then floor once. **Both directions. Effort: M** | Ref: CRR Art. 159 / PS1/26 Art. 159 (CRE35.4) · Ev: `engine/irb/adjustments.py:360-364`, `engine/aggregator/_el_summary.py:99-101,197-230` · §5 WS5
- **P1.222** [x] FIXED v0.3.15 (Art. 115(5) flat-20% RGLA weight restricted to the UK/GBP limb via a dedicated `is_uk_domestic` flag on both regime override chains; CGCB 0% branch keeps the composite flag; unrated EU RGLAs now route to Table 1A sovereign-derived (CQS3=100%); pinned by `tests/acceptance/crr/test_p1_222_rgla_uk_gbp_flat_20.py` + inverted `test_eu_domestic_currency_rgla`) **(WS6 · HIGH)** Flat-20% domestic-currency RGLA weight over-extended from UK to all EU member states — UK CRR / PS1/26 Art. 115(5) scopes the flat 20% to *UK* RGLAs denominated and funded in sterling, but `is_domestic_currency = is_uk_domestic | is_eu_domestic` applies 20% to every EU-domestic-currency RGLA ahead of the Table 1B/1A branches, so an unrated EU municipality (sovereign CQS3) gets 20% instead of up to 150%. Restrict the 20% branch to the UK/GBP limb. **Understatement. Effort: M** | Ref: Art. 115(5) (CRR and PS1/26) · Ev: `engine/sa/risk_weights.py:894-897,1085-1087,1291-1293`, `rulebook/packs/common.py:780-815` · §5 WS6

- **P1.10** [x] RE-SCOPED + FIXED (Unreleased, 2026-07-16) — bullet previously described the Rule 4.11 *transitional*, which is P1.143's scope; the audit (finding `art213-eligibility-conditions-unvalidated`, §6) and P1.143's own text both treat P1.10 as the BASE **Art. 213(1)(c)(i) eligibility validation**, now implemented: nullable `is_unilaterally_cancellable`/`is_unilaterally_changeable` on GUARANTEE_SCHEMA (null-PERMISSIVE per the Art. 237(2)(a) precedent — recorded deviation from the audit's "conservative default" boilerplate); pack Feature `ucp_unilateral_change_ineligible` (B31 on / CRR off — the "or change" arm is new in B31; the CRR Art. 213(1)(c)(ii) cost-increase limb is a recorded deferral); `_gate_unilateral_protection` in `_prepare_guarantees` drops ineligible protection pre-expansion (covers credit-derivative rows too) + CRM012 WARNING per dropped guarantee. Pinned by `tests/acceptance/{crr,basel31}/test_p1_10_art_213_ucp_eligibility.py` (10 tests: 200k eligible / 1M ineligible; CRM012 counts 1 CRR / 2 B31 as leak controls). Reviewer pass. | Ref: CRR Art. 213(1)(c)(i) / PS1/26 Art. 213(1)(c)(i)
- **P1.173** [x] CLOSED 2026-07-16 — NO LONGER REQUIRED (verified stale): the remaining cosmetic sub-gap was already fixed by P1.175 (commit `cb7b4c9a`, batch 20260530-1632, ancestor of HEAD — "123(3)(a-b)→123(4)"). Grep confirms zero `Art. 123(3)(a-b)` citations remain in `src/`; the payroll/pension 35% is correctly cited Art. 123(4) / CRR Art. 123 second subparagraph everywhere (`schemas.py` `is_payroll_loan`, `b31_risk_weight_tables.py`, `risk_weights.py::_crr_append_retail_branches`); the two surviving `Art. 123(3)(c)` cites describe the distinct PS1/26 non-regulatory-retail 100% provision and are correct. Behaviour itself was P2.17 (closed v0.2.29). P1.173 was a duplicate of the already-closed P1.175.
- **P1.178** [x] CLOSED 2026-07-16 as duplicate (no code work required) — the sole remaining sub-claim was closed with P1.122(b) (d9955a32, batch 20260526-1735): unrated B31 institution guarantors route through PS1/26 Art. 121 Table 5 SCRA grades on the IRB SA-fallback path (`engine/irb/guarantee.py` injects `guarantor_scra_grade`; `engine/sa/guarantor_rw.py::build_institution_guarantor_rw_expr`); pinned by `tests/acceptance/basel31/test_p1_122b_b31_unrated_scra_b_institution_guarantor_irb_sa_fallback.py`. P1.95/P1.110 previously closed. No remaining gap.
- **P1.183** [x] FIXED (Unreleased, 2026-07-16 — ran SINGLE-STREAM; new `engine/aggregator/_lgd_floor_check.py` computes the post-aggregation EW-avg own-LGD per retail-RE sub-class and emits IRB007 WARNING only (never an RWA/LGD adjustment; RWA-neutrality pinned bit-stable at 301,185.0126657611); thresholds 0.10/0.15 as cited ScalarParams in `packs/crr.py`; Feature `crr_retail_re_portfolio_lgd_floor` CRR-on/B31-off; the Art. 164(4) central-gov-guarantee exclusion catches the split `__G_` legs (verified load-bearing: 8.00% if wrongly included); null-LGD rows dilute the average downward by documented design (conservative over-flagging); pinned by `tests/acceptance/{crr,basel31}/test_p1_183_art_164_portfolio_lgd_floor.py`, pack pins in `tests/unit/rulebook/test_retail_re_portfolio_lgd_floor_pack_entries.py`, and commercial/mixed/null unit coverage in `tests/unit/test_lgd_floor_check.py`; reviewer revise→resolved via the 164(4) citation sweep) — **CRR Art. 164(4)/(5) portfolio-level LGD floor** (CRR-only; sunsets 31 Dec 2026 with the regime). CRR (as amended by CRR2) requires the *exposure-weighted-average* LGD across all A-IRB retail exposures secured by immovable property to be **≥10% (residential) / ≥15% (commercial)** — a portfolio-level backstop, **not** a per-exposure input floor (spec: `docs/specifications/crr/airb-calculation.md:44`). **Not closed by the per-exposure floors that now exist:** `engine/irb/formulas.py:191-231` applies per-exposure Art. 164(4) floors only under Basel 3.1 — gated on the `airb_lgd_floor` Feature, which is `False` under CRR (`packs/crr.py:65-68`) — so under CRR no LGD floor binds at all and the EW-average constraint is unenforced. The CRR `lgd_floors` pack bundle being all-zero is *correct* for the per-exposure floor and is **not** the bug. **Capital impact: understatement.** Implement as a post-aggregation EW-average check per retail RE sub-class emitting a WARNING (code `IRB007`), not a per-exposure re-floor. **Effort: M** | Ref: UK CRR Art. 164(4)/(5). **Must run SINGLE-STREAM** — the post-aggregation hook's call site lands in `engine/aggregator/aggregator.py` (a hard-excluded shared file); mid-IRB-stage emission would force a LazyFrame-contract-violating eager collect. Pick alone via `/implement-scenario P1.183`, not a parallel `/next-items` batch. **Architecture pointers refreshed 2026-07-07:** the two threshold scalars (`0.10` / `0.15`) belong as cited `ScalarParam` entries in `src/rwa_calc/rulebook/packs/crr.py` — the `data/tables/` package was deleted (Phase 5 S13) and is hard-banned by arch_check check 12, so the former `data/tables/firb_lgd.py` target and the `LGDFloors.crr()`-returns-zeros framing are both stale (`LGDFloors` survives only as the keying dataclass in `contracts/config.py`). Re-confirmed by the 2026-07-07 compliance audit (art164-crr-portfolio-avg-lgd-floors, §6).

- **P1.215** [x] FIXED (Unreleased, 2026-07-16 — design decision recorded: own-modelled-LGD INPUT carrier, not an engine-assigned "A-IRB LGD treatment" (any engine-assigned LGD is F-IRB relabelled; A-IRB LGD is by definition a firm input, Art. 143/153). New nullable `ccr_modelled_lgd` on SFT_TRADE_SCHEMA + TRADE_SCHEMA → EdgeColumn on CCR_EXIT_EDGE only (spread to the 3 CCR exits; contract-guarded absent from HIERARCHY_EXIT) → producers emit first-per-NS in `sft/fccm.py` + `ccr/pipeline_adapter.py` → classifier `has_modelled_lgd` widened via OR of is_not_null → `apply_firb_lgd` coalesces the carrier in the otherwise-branch (FIRB inert). Opt-in by data: null carrier routes exactly as before; all 5 strict-xfail anchors flipped green with markers removed (A0-5 flagship pins k≈0.069078 pre-MA, MA≈0.853445, lgd 0.45, M=5/365); A0-5b needed CP_SFT_IRB `annual_revenue=50m` (both-null revenue/assets trips the conservative B31 large-corp default). Recorded non-blocking notes: SA-CCR `.first()` carrier pick is unsorted (heterogeneous multi-trade-NS LGDs = data error, mirrors sibling currency pick); STANDARDISED-mode negative control untested (correct by airb_expr construction). Reviewer pass.) — A-IRB routing for synthetic CCR rows. A `CCR_SFT` / `CCR_DERIVATIVE` row cannot route to A-IRB end-to-end: the classifier A-IRB gate `airb_expr = model_airb_permitted & has_internal_rating & has_modelled_lgd` (`engine/stages/classify/permissions.py:340`) requires a row-level own-modelled `lgd` the FCCM / SA-CCR producers do not emit, so an A-IRB repo falls back to F-IRB (`M = 0.5y`) or SA (also why `CCR-IRB-1`, the derivative golden, is F-IRB). The Art. 162 effective-maturity carrier + IRB rung are already A-IRB-ready (proven at the expression level in `tests/unit/irb/test_ccr_maturity_rung.py`); the 5 A-IRB acceptance anchors in `tests/acceptance/ccr/test_ccr_sft_irb_maturity.py` are `xfail(strict=True)` pending this (so they auto-flip green when it lands). **Capital impact:** A-IRB firms' CCR/SFT exposures receive F-IRB maturity treatment instead of the A-IRB Art. 162(2) carve-out floors (e.g. the 5BD repo floor `M = 5/365`). **Design decision required:** an own-modelled-LGD input field on the SFT/CCR schema + producer emission, OR a defined CCR/SFT A-IRB LGD treatment. **Effort: M** | Ref: CRR Art. 162(2) / Art. 153; `engine/stages/classify/permissions.py:340`

#### Tier 1 — 2026-07-07 CRR / PS1-26 Art. 111–241 compliance audit (WS1–WS11)

> 41 medium + 28 low capital-correctness items from the 2026-07-07 audit (`docs/plans/compliance-audit-crr-111-241-rectification.md` §5). Grouped by workstream so implementers batch by shared files; the 7 high-severity siblings are hoisted to the head of Tier 1 above. Bullets carry the headline gap + citation + one load-bearing evidence pointer — full evidence/fix prose is in §5. Severity tags `(MED)`/`(LOW)`; direction of error noted where it isn't an understatement.
>
> **Schema-enablement first (nullable, conservative default; P2.40a→P2.40b split):** P1.227 (guarantor eligibility), P1.229 (funding currency), P1.239 / P1.240 (third-party-deposit holder ref), P1.259 (implicit-support flag), P1.261 (rating currency) — land the schema PR before the consuming logic.
> **Likely-merge clusters (one code fix, kept as distinct P-codes as the audit filed them from different article angles):** P1.216 ≈ P1.224 ≈ P1.226 (CRR Art. 131 Table 7 short-term branch); P1.233 ≈ P1.236 (corporate-bond normalisation in `_normalize_collateral_type_expr`); P1.239 ≈ P1.240 (third-party-deposit → guarantee substitution, Art. 232); P1.242 ≈ P1.246 (LFSE 1.25× correlation derivation). Implement each cluster as a single change.
> Pure-citation/spec-staleness lows P1.266, P1.269, P1.280, P1.283, P1.295 live in Tier 5 (pointers noted in their workstreams below).

**WS1 — SA short-term ECAI rating treatment** (files: `enrich.py`, `risk_weights.py`, `crr.py`; high sibling P1.216 above)
- **P1.223** [x] FIXED v0.3.16 (obligor short-term ECAI spillover via `_apply_obligor_short_term_spillover`, pinned by `test_p1_223_art_120_3c_obligor_st_spillover.py`) (MED) CRR Art. 120(3)(c) — obligor-level short-term spillover missing: where a short-term issue assessment maps to a *less* favourable RW than the general preferential ST treatment, all of that obligor's unrated ST claims must take the ST assessment's RW; the override is strictly per-exposure, so other ST exposures keep the 20-50% preferential. **Effort: S** | Ref: CRR/PS1-26 Art. 120(3)(c) · Ev: `enrich.py:160-240` · §5 WS1
- **P1.224** [x] FIXED (Unreleased — via P1.216 Table 7 branch) (MED) CRR Art. 120(3) w/ 131 — a short-term issue-specific assessment must take the Art. 131 ST mapping, not the Table 4 general preferential; CRR routes a ≤3m ST-CQS2/3 exposure through Table 4 (20% vs 50%/100%). *Shares the CRR Table 7 branch fix with P1.216/P1.226 — implement as one change.* **Effort: M** | Ref: CRR Art. 120(3) w/ 131 · Ev: `enrich.py:229-238`, `risk_weights.py:805-832` · §5 WS1
- **P1.225** [x] FIXED (Unreleased, 2026-07-16 — two obligor flags `obligor_st_150_contamination`/`obligor_st_50_floor` built in enrich beside the P1.223 spillover from the pristine `_st_assessment_cqs` (`.max().over(counterparty_reference)` + partition_by_nullable), registered on the hierarchy edges, consumed by a shared post-ladder override in `risk_weights.py` (150% force on unrated unsecured incl. LT; max(rw,100%) floor on unrated ST; 150 dominates; ~is_guaranteed excludes substituted legs); regime-invariant, no Feature; Art. 127 defaulted handler runs LAST and overwrites, so provision-based defaulted RW survives contamination — load-bearing ordering now commented; pinned by tests/acceptance/{crr,basel31}/test_p1_225_art_140_2_obligor_st_contamination.py (18 tests: E2 100%→150%, E4 20%→100% incl. B31 SCRA-A base, triggers/controls/guaranteed-leg/independence pins, portfolio 4.2M→5.5M); reviewer pass. RECORDED FOLLOW-UP: add a defaulted-unrated-unsecured-under-contaminated-obligor fixture row pinning that Art. 127's 100% survives (behaviour correct today, untested); P1.264's class gate composes upstream; CO-FIRE FIX 2026-07-17 (1fa5d5da): adversarial review found the Art. 120(3)(c) spillover co-fire defeated the Art. 140(2)(b) floor — the spillover overwrites cqs/has_short_term_ecai so the `cqs null & ~st_ecai` predicate skipped spilled legs (50% kept vs >=100% mandated, both regimes); new per-exposure `has_own_short_term_ecai` flag, target predicate `~own & (cqs null | spilled)`; pinned by tests/acceptance/{crr,basel31}/test_p1_225_cofire_spillover_floor.py (8 tests, LN-B 0.50->1.00); max_engine_module_loc baseline 1675->1684 justified) (MED) CRR/PS1-26 Art. 140(2) — obligor contamination: any ST-rated facility at 150% must force 150% on *all* unrated unsecured exposures to that obligor; a 50% ST facility floors unrated ST exposures at 100%. No obligor-level spillover exists. **Effort: M** | Ref: CRR/PS1-26 Art. 140(2) (CRE21.17-18) · Ev: `enrich.py:160-240` · §5 WS1
- **P1.226** [x] FIXED (Unreleased — via P1.216 Table 7 branch) (MED) CRR Art. 140(1) w/ 131 — CRR ST assessments must use Art. 131 Table 7, not the long-term institution/corporate tables; no Table 7 branch on the CRR path. *Duplicate of the P1.216 fix (Table 7 lookup + gated branch) — collapse into P1.216.* **Effort: M** | Ref: CRR Art. 140(1) w/ 131 · Ev: `risk_weights.py:805-832,1202-1368` · §5 WS1
- **P1.264** [x] FIXED (Unreleased, 2026-07-16 — class gate inside `apply_short_term_rating_override`: signature gains counterparty_lookup + errors; raw `entity_type` joined as scratch, eligible set = pack-sourced ENTITY_TYPES_BY_SA_CLASS institution ∪ corporate (design's `corporate_sme` key doesn't exist — SMEs carry corporate entity types, verified); mis-scoped ST rating → has_short_term_ecai False, counterparty cqs kept, DQ009 WARNING per exposure; ordering gate → P1.223 spillover → P1.225 contamination means both inherit the gate (pinned: the S2B leak — a CQS-4 mis-scope on an unrated sovereign wrongly forced an unrated LT sibling to 150% via the Art. 140(2) broadcast, £500k RWA — reverts to 100%); reviewer revise → resolved: `fill_null("")` guard on the is_in so a null/unknown entity_type or lookup join-miss cleanly rejects + emits DQ009 instead of Kleene-null swallowing the warning (pinned by the N1 null-entity scenario); pinned by tests/acceptance/{crr,basel31}/test_p1_264_art_140_1_st_class_gate.py (20 tests); ratchets +1 collect (DQ emission) +1 fill_null (standard entity_type idiom), documented) (LOW) CRR/PS1-26 Art. 140(1) — the ST override is applied to any scope-matched exposure regardless of class; short-term assessments may only be used for institution/corporate obligors, so a mis-scoped ST rating wrongly replaces e.g. a sovereign row's CQS. Add a class gate / DQ error. **Effort: S** | Ref: CRR/PS1-26 Art. 140(1) · Ev: `enrich.py:229-240` · §5 WS1

**WS2 — Unfunded credit protection (guarantee) mechanics** (files: `guarantees.py`, `collateral.py`, `guarantor_rw.py`; high siblings P1.218/P1.219 above)
- **P1.227** [x] FIXED (Unreleased, 2026-07-17 — NOTE: the "SCHEMA" tag was anticipatory and WRONG: both eligibility signals already existed (`guarantor_cqs` + `guarantor_internal_pd` via `_join_guarantor_ratings`), so this landed as a pure engine change. Gate in `_assign_guarantor_approach`: corporate guarantors (class vocabulary can only say "corporate" — no corporate_sme class value exists) are eligible via ECAI `guarantor_cqs`, or via internal rating ONLY for FIRB/AIRB beneficiaries (Art. 201(2); slotting deliberately excluded — documented); ineligible → `guarantor_approach=""` + `guarantor_exposure_class=""` reusing the established unmapped-guarantor no-substitution path (covered leg reverts to borrower basis) + CRM013 WARNING per leg. Pinned by tests/acceptance/{crr,basel31}/test_p1_227_art_201_guarantor_eligibility.py (18 tests: G1 unrated corp 1.0M→1.5M, G4 internal-only-on-SA 1.0M→1.5M, G2 ECAI control, G3 same-guarantor-IRB-beneficiary control pinned at 6dp, G5 retail probe). Reviewer pass. RECORDED FINDINGS: (a) retail-class guarantors already produce ZERO benefit today (guarantor_rw lookup has no RETAIL entries → null → non-beneficial → skipped; rw_adjustments.py null-guard) — accidental but real safety net, documentation-only follow-up, no new functional item filed; (b) reverted/unmapped class="" legs keep is_guaranteed=True so they escape the P1.225 ST-contamination `is_unsecured` predicate — PRE-EXISTING characteristic of the unmapped path, not a P1.227 regression, noted for a future pass; (c) multi-guarantor-one-ineligible / partial-coverage edges correct by construction, untested — candidate fixture row.) (MED) CRR/PS1-26 Art. 201(1)(g)/(2) — no guarantor-eligibility filter: an unrated corporate guarantor is priced to the unrated-corporate RW (100%) and recognised as protection, but Art. 201 admits corporates only with an ECAI rating (or, IRB/PSM only, an internal rating). Add an eligibility step dropping ineligible providers with a CRM warning. *Schema-enablement first.* **Effort: M** | Ref: CRR/PS1-26 Art. 201(1)(g)/(2) · Ev: `guarantees.py:92-220`, `guarantor_rw.py:523-570` · §5 WS2
- **P1.228** [x] FIXED v0.3.16 (unfloored DD RW vs substitution in `_apply_double_default`, pinned by `test_irb_double_default.py::TestDoubleDefaultReachable`) (MED) CRR Art. 217 / 153(3) — double-default is unreachable: `rw_dd` is floored at `max(rw_dd, guarantor_rw)` then gated `< guarantor_rw`, so the strict `<` is always False and DD never applies; `guarantee_status` can never be DOUBLE_DEFAULT. Compare the *unfloored* DD RW against substitution. CRR-only. **Effort: M** | Ref: CRR Art. 217 / 153(3) · Ev: `engine/irb/guarantee.py:682-710` · §5 WS2
- **P1.229** [x] FIXED (Unreleased, 2026-07-17 — nullable `funding_currency` added to LOAN/FACILITY/CONTINGENTS schemas, plumbed through `stages/hierarchy/unify.py` + `facility_undrawn.py` and declared on the hierarchy-resolved edge (propagates to classifier/CRM exits); new `funding_currency_expr` in `engine/eu_sovereign.py` is null-PERMISSIVE — null falls back to the audit-endorsed denomination proxy (`original_currency`, pre-FX), mirroring the P1.10/Art. 237(2)(a) precedent; `build_domestic_cgcb_guarantor_expr` gains the Art. 235(3) funding AND-limb (funding == guarantee currency, which has already passed the domestic test), threaded from all 3 consumers: SA `rw_adjustments.py`, IRB `guarantee.py`, CRM `guarantees.py`. Pinned by `tests/acceptance/{crr,basel31}/test_p1_229_art_235_3_funding_currency.py` (USD-funded EUR-guaranteed DE-sovereign-CQS3 loan 0%→50%; matched-currency + null-funding controls stay 0%); 3 bug-encoding modules re-pinned (`test_domestic_sovereign_guarantor_end_to_end.py`, `test_guarantor_exposure_class_rw.py`, `test_guarantor_rating_type.py`) with matched-currency 0% controls retained; arch ratchet bumps (+1 fill_null, +3 presence-guards, +4 cites) all attributable. Reviewer pass. RECORDED FOLLOW-UP: the DIRECT Art. 114(4) sovereign path (`build_eu_domestic_currency_expr` at `sa/risk_weights.py:1022`, `stages/classify/approach.py:298`) still has no funding limb — a directly-held EU-sovereign exposure denominated-but-not-funded in domestic currency keeps 0%; same latent defect on a different path, candidate audit item.) (MED) Art. 235(3) — **SCHEMA** — the Art. 114(4)/(7) 0% extension to centrally-guaranteed exposures requires BOTH a domestic-currency guarantee AND the exposure funded in that currency; only guarantor-country vs guarantee-currency is tested, so a USD-funded EUR-guaranteed loan (sovereign CQS3, 50%) wrongly gets 0%. AND in a funding-currency check. *Schema-enablement first.* **Effort: M** | Ref: CRR/PS1-26 Art. 235(3) · Ev: `guarantees.py:361-389`, `eu_sovereign.py:83-111` · §5 WS2
- **P1.230** [x] FIXED 2026-07-12 (commit cee4148b, merged PR #435 — plan tick was outstanding; verified 2026-07-16: slotting branch runs the shared SA RWSM substitution via `engine/slotting/transforms.py::apply_guarantee_substitution` + Art. 235(1A) covered-part EL zeroing via `zero_covered_expected_loss`, gated by pack Feature `slotting_guarantee_substitution` in BOTH regimes (crr.py:336, b31.py:319); aggregator `pre_crm_risk_weight`/`guarantee_benefit_rw` stamp corrected; pinned by `tests/acceptance/basel31/test_slotting_guarantee_rwsm.py`, 14 tests) (MED) PS1/26 Art. 235 (RWSM) / CRR Art. 236 via 153(5) — slotting guarantees give zero CRM benefit: `engine/slotting/**` never reads `guarantor_*`, so a guaranteed slotting exposure gets no substitution (RWA over-statement) and the B31 unified pass stamps a wrong `pre_crm_risk_weight=1.0`. Extend the RWSM row-split to the slotting branch + zero covered-part EL. Cross-ref MEMORY `slotting-guarantee-gap`. **Over-statement. Effort: M** | Ref: PS1/26 Art. 235 / CRR Art. 236 · Ev: `engine/slotting/**`, `sa/rw_adjustments.py:219-283` · §5 WS2
- **P1.231** [x] FIXED (Unreleased, 2026-07-17 — `_apply_maturity_mismatch_to_guarantees` now ZEROES protection ahead of the Art. 239(3) scaling on two gates mirroring the collateral sibling: Art. 237(1) `t_raw < 0.25 & t_raw < T_raw` on PRE-floor residuals (the masked both-floor-to-0.25 case now zeroed; a t≥T protection that outlives a shorter exposure stays recognised), and Art. 162(3)/237(2)(b) `has_one_day_maturity_floor & raw mismatch` (flag already existed on LOAN/FACILITY + hierarchy edge — joined beneficiary→exposure, null/join-miss permissive). Null exposure-T defaults to 5y (`fill_null(5.0)`, audit's third fix component, reviewer-caught on first pass) so null-maturity exposures can't defeat the gates or the scaling; RECORDED twin-divergence: the gate compares RAW T (null→5y, not lower-clipped) per the audit's raw-t<raw-T prose — the collateral twin's 0.25 lower clip would spuriously zero protection outliving a sub-3-month exposure (Art. 238); divergence documented in-code. Pinned by `tests/unit/crm/test_art237_guarantee_ineligibility.py` (17) + `tests/acceptance/{crr,basel31}/test_p1_231_art_237_maturity_gates.py` (13, RWA-level: 1DF-mismatch 781,601→1,000,000; masked 40d/80d 200,000→1,000,000; outlives + 239(3) controls unchanged); no existing pins moved. Reviewer revise→pass.) (MED) CRR/PS1-26 Art. 237(1)/(2)(b) — guarantee maturity gates missing: no zeroing for protection on a one-day-M-floor exposure under any mismatch (237(2)(b)), and the 3-month rule (237(1)) is only implicit via the 0.25 floor. Test raw `t < 0.25 AND t < T` explicitly and zero; join the one-day-floor flag. **Effort: M** | Ref: CRR/PS1-26 Art. 237(1)/(2)(b) · Ev: `guarantees.py:1267-1371`, `haircuts.py:718-748` · §5 WS2
- **P1.232** [x] FIXED (Unreleased, 2026-07-17 — the Art. 237(2)(a) `>=1y original-maturity` pre-filter removed from `_prepare_guarantees`; relocated into `_apply_maturity_mismatch_to_guarantees` as `short_original_gate = raw_mismatch & (orig_maturity < 1.0)`, OR-ed into the P1.231 zero-gate chain (idempotent co-fire with the 237(1) gate). Matched and protection-outlives-exposure short-dated guarantees now recognised (trade-finance case 6m/6m: RWA 1,000,000→200,000 at corp-CQS1 20%); mismatched still zeroed — via a zero-coverage row through the split rather than a dropped row, an already-exercised pattern (239(3) scale-to-0), RWA-neutral for mismatched legs. Null original_maturity stays PERMISSIVE (P1.10); P1.219 residual-vs-original distinction preserved (gate reads original term, scaling reads residual). Chapeau reading verified against the audit prose + the haircuts.py collateral twin (same conditioning). Pinned by `tests/acceptance/{crr,basel31}/test_p1_232_art_237_2a_matched_short_guarantee.py` (8) + 5 unit tests in `test_art237_guarantee_ineligibility.py`; no existing pins moved (P1.124's GUAR_SHORT is mismatched — still zeroed); reporting golden suites green post-change (1015). Reviewer pass.) (MED) CRR/PS1-26 Art. 237(2)(a) — the original-maturity ≥1yr test is an *unconditional* pre-filter, discarding matched short-dated guarantees (no mismatch) — over-statement, systematic for short trade-finance. Move the test into the mismatch branch (mirroring `haircuts.py`). **Over-statement. Effort: M** | Ref: CRR/PS1-26 Art. 237(2)(a) · Ev: `guarantees.py:191-200` · §5 WS2

**WS3 — Financial-collateral eligibility & supervisory haircuts** (files: `haircuts.py`, `collateral.py`, `crr.py`, `b31.py`, `schemas.py`)
- **P1.233** [x] FIXED v0.3.16 (corp/PSE/institution `bond` → `corp_bond` in `_normalize_collateral_type_expr`, pinned by `test_p1_233_art_197_corp_bond_collateral.py`) (MED) CRR/PS1-26 Art. 197(1)(c)/(d) — corporate/PSE `bond` collateral normalises to `other_physical` unless the issuer is sovereign, so the Art. 197 CQS-eligibility zeroing never fires (a CQS-5 corp bond recognised at 60% MV) and it gets 40% flat not the Table 1 1-12%/20%. Route corp/pse/institution bonds to `corp_bond`. *Shares fix with P1.236.* **Effort: S** | Ref: CRR/PS1-26 Art. 197(1)(c)/(d) · Ev: `haircuts.py:634-659,570-578` · §5 WS3
- **P1.234** [x] FIXED (Unreleased, 2026-07-17 — securitisation positions are now eligible financial collateral with the dedicated Art. 224 Table 1 haircut column (previously flat-40% `other_physical` with NO gate — benefit granted to resecs and >100%-RW positions). Schema: COLLATERAL_SCHEMA +`is_resecuritisation` (default False = "not known to be resec", residual risk documented) +`securitisation_position_risk_weight` (nullable, null=CONSERVATIVE ineligible). Pack rows PDF-VERIFIED against the primary sources — orchestrator independently re-extracted both prints after a first-pass "2× corporate" derivation error: CRR Art. 224 Table 1 (crr.pdf p.221) CQS1 2/8/16, CQS2-3 4/12/24; PS1/26 Art. 224 Table 1 (ps126app1.pdf PRA2026/1 p.203, 10-day) CQS1 2/8/8/16/16, CQS2-3 4/12/12/24/24 — B31 securitisation is NOT 2× corp (bands genuinely flattened in print); pinned per-band incl. `test_b31_securitisation_is_not_double_corporate`. Engine: `securitisation` normalisation branch ahead of corp_bond (containment: fires only on securitisation type/issuer strings), CQS+band lookup via bond_types, eligibility gate `is_resec | RW null | RW>1.0 | CQS null | CQS>=4` zeroing value + is_eligible_financial_collateral; haircut_tables.py shim row-specs added (NOTE for future pack-row additions: the rendered table is row-spec-driven, a new pack row is invisible until added there). Acceptance both regimes: eligible CQS1@4y 700,000→540,000 (8% haircut); resec/high-RW/null-RW 700,000→1,000,000 (benefit stripped). SIDE-FINDING: `.claude/skills/basel31/references/crm-changes.md` corp-bond haircut summary is STALE vs the PDF (pack is right) — docs follow-up. Reviewer revise→resolved (B31 print confirmed by orchestrator PDF extraction; stale 2×-corp comments fixed).) (MED) CRR/PS1-26 Art. 197(1)(h) — no securitisation-collateral branch: positions ≤100% RW are eligible with dedicated Table 1 haircuts, resecuritisations ineligible; the packs carry no securitisation rows, so such collateral is mis-priced (corp-bond or `other_physical`). Add the branch + pack rows + RW≤100% gate. **Effort: M** | Ref: CRR/PS1-26 Art. 197(1)(h) · Ev: `haircuts.py:634-659`, `packs/crr.py:756-790`, `packs/b31.py:754-804` · §5 WS3
- **P1.235** [x] FIXED (Unreleased, 2026-07-17 — `is_eligible_irb_collateral` is now the real Art. 199(2) gate on the FIRB FCM non-financial branch of `_apply_collateral_unified` (after `effectively_secured`, before the Art. 231 waterfall — an ineligible row contributes 0 to its category, no cross-category capacity theft): RE/receivables/other-physical zeroed unless attested True, + the Art. 199(5) receivables `original_maturity_years > 1y` limb (strict >, boundary 1.0 eligible; NULL maturity PERMISSIVE via Boolean-fill of the test — recorded deviation, the attestation covers the condition and only contradicting data zeroes). Flag semantics: True = institution's Art. 199 eligibility attestation; False/unset = ineligible (audit's conservative intent — field pre-existed, so the P1.10 new-field-permissive precedent deliberately NOT applied). New CRM014 WARNING per gated row (not-attested vs >1y reasons), errors channel threaded apply_collateral→_apply_collateral_unified→processor. Financial collateral (incl. P1.234 securitisation, separate flag), SA paths, classification untouched. Blast radius handled by option-iii fixture attestation: 38 tests across 4 allocation/LGD-math files re-pinned by setting the flag True (justified per file; gate is new so no pre-existing ineligibility pin existed to neutralize); goldens + oracle byte-identical unregenerated. Pinned by `tests/unit/crm/test_p1_235_firb_fcm_eligibility_gate.py` (8) + `tests/acceptance/{crr,basel31}/test_p1_235_art_199_firb_fcm_eligibility.py` (6; flag alone flips LGD 0.378571↔0.45 CRR, 0.28↔0.40 B31). Reviewer pass incl. adversarial probes. DEFERRED to a schema-enablement item (fields don't exist): Art. 199(5) receivables exclusions (`is_affiliated_party`/`is_sub_participation`/`is_credit_derivative_linked`/`is_securitisation_linked`), Art. 199(3)/(4) RE material-dependence flags, Art. 199(6) other-physical liquid-market/public-price/70%-10% attestations.) (MED) CRR/PS1-26 Art. 199(2)/(5)/(6) — the FIRB FCM waterfall recognises every RE/receivables/other-physical row unconditionally; `is_eligible_irb_collateral` exists (default False) but is never read as a gate. Wire it + receivables ≤1yr / affiliated-party checks. **Effort: M** | Ref: CRR/PS1-26 Art. 199(2)/(5)/(6) · Ev: `collateral.py:687-709`, `schemas.py:496` · §5 WS3
- **P1.236** [x] FIXED v0.3.16 (closed by the same `_normalize_collateral_type_expr` corp_bond branch as P1.233 — cluster sibling) (MED) Art. 224 Table 1 w/ 197(1)(b)/(d) — canonical `bond` collateral with a corporate/pse issuer falls to `other_physical` (40%) and bypasses the Art. 197 CQS gate. Add the `corp_bond` branch. *Duplicate of P1.233 — collapse.* **Effort: S** | Ref: Art. 224 Table 1 w/ 197 · Ev: `haircuts.py:637-659,571-577` · §5 WS3
- **P1.237** [x] FIXED (Unreleased, 2026-07-17 — `haircuts.py` collateral-row default flipped `fill_null(True)`→`fill_null(False)`: null `is_main_index` equity now takes the other-listed haircut (25% CRR / 30% B31, verified against the live packs) not main-index (15%/20%); only attested `is_main_index=True` earns the cheaper band; legacy no-column fallback untouched; schema doc corrected, no Boolean default (null propagates, engine resolves conservatively). Grep-verified the flipped expression is the ONLY `is_main_index`-driven default in the engine (all other sites are table-side join keys / value stores). Blast radius: 2 unit re-pins (the red→green null-default pins), CRR-D3/B31-D3 FTSE-100 fixture row + CRR-D11 helper attested True (factually main-index, intent-preserving; parquet regenerated from `collateral.py` source), and ONE legitimate expected-value correction: B31-G F-IRB provisions EL-excess 606,093.03→605,933.93 (other-listed equity row on a subordinated obligor now takes 30% not the bugged 20%; delta 159.10 reviewer-verified). Goldens+oracle untouched. Reviewer revise→resolved (disclosure — see P1.271).) (MED) Art. 224 Table 3 / 197-198 — `is_main_index.fill_null(True)` treats an unknown-index equity collateral row as main-index (15%/20% not 25%/30%, and general-lending not Art. 198 repo-only). Flip the default to False. Anti-conservative. **Effort: M** | Ref: Art. 224 Table 3 / 197-198 · Ev: `haircuts.py:521` · §5 WS3
- **P1.270** [x] FIXED (Unreleased, 2026-07-17 — nullable `issuer_counterparty_reference` on COLLATERAL_SCHEMA (issuer identity, distinct from `posted_by_counterparty_reference`; null=PERMISSIVE — identity unreported, undetectable); new CRM-stage gate `processor.py::_apply_own_issue_collateral_gate` at Step 3.6 (post-netting-merge, pre-haircut/allocation — ONE check covers FCCM + FIRB FCM; netting synthetics are null-issuer cash so ordering is safe): rows whose issuer == obligor OR shares the obligor's ultimate parent (parent/child/sibling limbs via `CounterpartyLookup.ultimate_parent_mappings`, orphan false-positives guarded by explicit not-null + fill_null(False)) are FILTERED OUT (not value-zeroed — zeroing would trip the `needs_resolve` pledge-percentage re-resolution at processor.py:414-431 and revive the row; recorded design deviation from the audit's "zeroing" wording, same no-benefit outcome) + one CRM015 WARNING each. Obligor resolved at ANY pledge level via a union beneficiary→obligor map (exposure/facility/counterparty), explicitly pinned by TestMultiLevelPledgeResolution. Worked case both regimes: £10m own-issued CQS1 corp bond → no benefit (RWA stays £10m) vs third-party ~£0.8m. Zero blast radius (null field no-op; goldens+oracle 0 movers). Pinned by `tests/unit/crm/test_p1_270_own_issue_collateral.py` (10) + `tests/acceptance/{crr,basel31}/test_p1_270_art_194_own_issue_collateral.py` (8). Reviewer pass; guarantor-issued collateral deliberately out of scope (separate Art. 194(4) angle). SIDE-FIX committed separately (af39fab0): the P1.235 fixture parquets were never registered in generate_all.py — fresh-checkout gap self-reported and closed.) (LOW) CRR/PS1-26 Art. 194(4) — no wrong-way / own-issue collateral check: COLLATERAL_SCHEMA has `issuer_type`/`issuer_cqs` but no issuer identity, so collateral issued by the obligor or its group can't be detected. Add `issuer_counterparty_reference` + DQ zeroing. Audit-trail gap. **Effort: S** | Ref: CRR/PS1-26 Art. 194(4) · Ev: `schemas.py:481-542` · §5 WS3
- **P1.271** [x] FIXED (Unreleased, 2026-07-18 — Art. 198(1)(a) listing-eligibility gate for non-main-index equity, closing the P1.237 deferral. New nullable `COLLATERAL_SCHEMA.is_listed` (no Boolean default; null→conservative not-listed) + `EQUITY_COLLATERAL_TYPES` const. Shared predicate `haircuts.py::non_main_index_equity_ineligible_expr` drives the value gate in `apply_haircuts` (equity neither attested main-index nor listed → value_after_haircut zeroed + is_eligible cleared; `collateral_haircut` LEFT INTACT — eligibility≠valuation; zeroing site is post-`_resolve_pledge_from_joined` and leaves `market_value` untouched, so the P1.270 needs_resolve revival trap cannot fire — reviewer-verified) + one CRM018 WARNING per gated row in `collateral.py::apply_collateral`. Main-index equity bypasses (Art. 197(1)(f) all-methods). Regime-identical → no Feature (reviewer-verified: PS1/26's Foundation-Collateral/SFT-VaR method extension broadens where LISTED equity qualifies, never unlisted, so the unlisted-only gate is safe both regimes incl. the FIRB LGD* leg). SCOPE CALL (reviewer-confirmed vs verbatim Art. 198 text): the audit's "repo/SFT-only" framing is a mischaracterisation — Art. 198(1)(a)/CRE22.38 condition on COMPREHENSIVE method + LISTING, not repo context; the gate enforces the listing condition on the default FCCM path. Worked case both regimes: £1m unrated SA corp + £500k non-main-index equity → listed: CRR 25% haircut EAD/RWA 625k, B31 30% 650k; unlisted: ineligible, RWA 1m + exactly 1 CRM018. Option-iii attestations: COLL_EQ_001/002 + COLL_CRM_D3 (FTSE-100) + TestOtherListedEquityPipeline attested is_listed=True (factually listed, intent-preserving); goldens+oracle 0 movers; full suite green (6087 unit + 2847 acc/int/con/oracle). Pinned by tests/unit/crm/test_p1_271_non_main_index_equity_eligibility.py (6) + tests/acceptance/{crr,basel31} twins (8). Reviewer pass. RECORDED FOLLOW-UPS: (i) FCSM exclusion of listed non-main-index equity deferred (Art. 198 additional collateral is comprehensive-method-only; FCSM rarely elected); (ii) convertible bonds normalise as bonds → Art. 197 CQS gate, excluded from EQUITY_COLLATERAL_TYPES by scoping — track separately if convertible-collateral listing eligibility ever matters.) (LOW) CRR/PS1-26 Art. 197(1)(f)/198(1)(a) — same `is_main_index.fill_null(True)` anti-conservative default (10pp) as P1.237 from the eligibility angle: unreported index membership → all-methods eligibility + the lower haircut. *Shares fix with P1.237.* **Effort: S** | Ref: CRR/PS1-26 Art. 197(1)(f)/198(1)(a) · Ev: `haircuts.py:519-525` · §5 WS3
- **P1.272** [x] FIXED (Unreleased, 2026-07-17 — `lgd_star_expr` now computes on E' = ead_for_crm × he_factor (he_factor = 1 + exposure_volatility_haircut, REUSING the SA E* branch's existing factor): LGD* = [LGDS·min(C,E') + LGDU·max(0,E'−C)]/E', matching PS1/26 Art. 230(1) verbatim (E' in numerator AND denominator — no bare-E overshoot; C=0 identity → LGDU; over-collateralised floors at LGDS) with C = the already-gated post-haircut aggregate (composes after the P1.235 gate: C shaped first, then E grossed). HE is engine-computed per Art. 223(5), non-zero ONLY for SFT rows lending debt securities → he_factor=1 elsewhere, every non-SFT pin bit-stable, goldens+oracle 0 movers. RECORDED scope call (reviewer-verified against the reg): `collateral_coverage_pct` / the 30% FCM coverage threshold stays on bare EAD — the skill confirms "30% of EAD", and the threshold applies to RE/other-physical where HE=0 anyway. Worked case: £10m FIRB SFT lending vs CQS1 gilt (HE=2%×√(5/10)=0.014142), £6m cash: CRR LGD* 0.18→0.183765, B31 0.16→0.163347. Pinned by `tests/unit/crm/test_p1_272_fcm_lgd_star_he_grossup.py` (5, incl. an anti-bare-E pin). Reviewer pass.) (LOW) PS1/26 Art. 230(1) / CRR 228(2) — FCM `lgd_star_expr` omits the `(1+HE)` gross-up on the exposure basis, understating LGD* on FIRB SFT-style rows where HE>0. Multiply the exposure basis (and cap) by `(1 + exposure_volatility_haircut)`. **Effort: S** | Ref: PS1/26 Art. 230(1) / CRR 228(2) · Ev: `collateral.py:1102-1111` · §5 WS3

**WS4 — Third-party cash, on-B/S netting & other funded protection** (files: `collateral.py`, `haircuts.py`, `schemas.py`)
- **P1.238** [x] FIXED (Unreleased, 2026-07-17 — `generate_netting_collateral` pools and the sibling match are now keyed on the triple (netting_agreement_reference, currency, `counterparty_reference`): the agreement stays the set-off boundary, the counterparty is the Art. 195 eligibility boundary — same-counterparty CROSS-FACILITY netting preserved (reciprocal balances with one counterparty), cross-counterparty offsets disallowed. NULL counterparty conservatively excluded (inner join, join_nulls=False — a null-cp deposit can never match any pool, incl. another null; pinned by test_null_counterparty_deposit_excluded). New CRM016 WARNING once per agreement spanning >1 counterparty with a real disallowed offset (deposit + positive loan, different cps), errors threaded from `_merge_netting_collateral`. Absent-column fallback = single counterparty (unit-frame idiom, column always present in production). One legitimate re-pin: `test_cross_counterparty_netting_disallowed` (old pin encoded the Art. 195 violation; POS01 now keeps full £1000) + same-cp £800 control; test-module docstring rewritten (it documented cross-cp netting as intentional — orchestrator fix on reviewer catch). No goldens/oracle movers (2680 green). Reviewer revise→resolved. NOTE: no opt-in attestation for cross-cp netting was added (the audit's option (b)) — follow-up if ever needed; adversarial A£500-deposit/A£300-loan/B£700-loan case verified (£200 excess does NOT leak to B).) (MED) CRR/PS1-26 Art. 195 — on-B/S netting pools group only by (`netting_agreement_reference`, `currency`) with no counterparty-equality constraint, so a deposit from CP A can net a loan to CP B — outside Art. 195 reciprocal-balance eligibility. Constrain the pool join to the same counterparty. **Effort: M** | Ref: CRR/PS1-26 Art. 195 · Ev: `collateral.py:152-260` · §5 WS4
- **P1.239** [x] FIXED (Unreleased, 2026-07-17 — SA leg; closes cluster sibling P1.240. New nullable `held_by_counterparty_reference` on COLLATERAL_SCHEMA (P1.240's `deposit_holder_reference` was the discarded synonym); NULL = own-bank = 0% cash preserved (recorded permissive policy — no reporting-institution identifier exists, so populated=third-party is the semantics). New `engine/crm/third_party_deposit.py`: third-party deposits HARD-PARTITIONED out of the collateral frame pre-apply_collateral (an is_eligible=False shortcut still leaked into LGD* — verified insufficient), so they exit EVERY cash-value channel (SA E*, FIRB LGD* C, netting); Art. 232(2) treatment = covered-part RW blend at the HOLDER institution's RW in `sa/rw_adjustments.py` (life-insurance Art. 232 pattern), BENEFIT-ONLY capped. Holder RW via the SHARED `build_institution_guarantor_rw_expr` from the deposit's `issuer_cqs` (a deposit is a claim on its holder): CRR CQS2 50%→blend 0.80; B31 CQS2 30%→0.72; unrated B31 → SCRA Grade-C 150% CRE20.21 fallback (reviewer-caught: first pass wrongly used ECRA[UNRATED]=40%, £240k anti-conservative) → cap binds → no benefit; non-institution holder gated out entirely (Art. 232(2) requires an INSTITUTION; `INSTITUTION_DEPOSIT_HOLDER_TYPES`) → no benefit + CRM017. FIRB DEFERRED+disclosed: third-party deposits give NO F-IRB benefit (excluded from LGD*, LGD reverts 0.45) + CRM017 recording the pending substitution — follow-up item for the F-IRB analogue. Pinned by 13 tests (unit + `tests/acceptance/{crr,basel31}/` twins, £1m/£400k worked cases incl. NOT-£600k and NOT-£760k negative asserts). Goldens+oracle number-neutral. Reviewer revise→resolved.) (MED) CRR Art. 200(a)/232(2) — **SCHEMA** — third-party cash on deposit with another institution is 'other funded protection' treated as a guarantee by that institution (RW≥20%), but every cash/deposit row gets own-bank 0% cash treatment; no holder field exists. Add `held_by_counterparty_reference` and route to guarantee-equivalent treatment. *Schema-enablement first; overlaps P1.240 (same gap/fix, Art. 232).* **Effort: M** | Ref: CRR Art. 200(a)/232(2) · Ev: `haircuts.py:639-640`, `schemas.py:481-542` · §5 WS4
- **P1.240** [x] FIXED (Unreleased, 2026-07-17 — closed by the P1.239 change (cluster: same gap, Art. 212(1) angle); field name landed as P1.239's `held_by_counterparty_reference`. See the P1.239 annotation for full detail incl. the FIRB deferral.) (MED) CRR Art. 212(1) w/ 200(a)/232(1) — duplicate angle of P1.239: a third-party deposit must be treated as a guarantee by the holding institution, not 0% cash. Add `deposit_holder_reference` + route to guarantee substitution. *Collapse with P1.239.* **Effort: M** | Ref: CRR Art. 212(1) w/ 200(a)/232(1) · Ev: `haircuts.py:639`, `schemas.py:481-526` · §5 WS4
- **P1.241** [x] FIXED (Unreleased, 2026-07-18 — `generate_netting_collateral` now emits the synthetic cash row with the DEPOSIT side's maturity envelope instead of the loan's: min residual `maturity_date` per (agreement, currency, counterparty) pool + derived `residual_maturity_years` (/365.25, day-count ALIGNED to the exposure-side T so equal maturities net exactly — P1.238 crr_same_cp verified exact £800,000.00, no phantom mismatch) + min ORIGINAL maturity `(maturity_date − value_date)/365` as `original_maturity_years`, so ALL THREE existing `apply_maturity_mismatch` gates fire on the netting path: Art. 238-239 (t−0.25)/(T−0.25) scaling, Art. 237(1) <3m-residual zeroing, Art. 237(2)(a) <1y-original zeroing (reviewer-caught in round 1: the original-maturity gate was skipped anti-conservatively — the engine already enforces it for ordinary collateral and the term is derivable; fixed via min-original carry). Pooling convention documented: min residual + min original (conservative; a short deposit drags the pool). Null deposit maturity / absent reporting_date → null residual (permissive 10y fill downstream unchanged). Worked cases (CRR, £1m unrated corp 7y loan, £200k deposit): partial 3y-original/6mo-residual → t=181/365.25, factor 0.0516949, recognised £10,338.99, RWA £989,661.01; 6mo-ORIGINAL → zeroed, RWA £1,000,000 (rel 1e-9); matched 6y/5y → full £200k, RWA £800,000. Number-neutral (no existing netting fixture had a maturity gap); goldens+oracle 0 movers; full suite green. Pinned by unit +4 (`test_collateral_submodules.py` incl. netting-path <3m zeroing) + `tests/acceptance/{crr,basel31}/test_p1_241_art_219_netting_maturity_mismatch.py` (5+4). @cites CRR Art. 238, snapshot regen. Reviewer revise→pass. Companion edit: Art. 219 removed from the P7.5 not-in-scope list below.) (MED) Art. 219 w/ 237-238 — netting-derived synthetic cash collateral carries the LOAN's maturity and null residual, so `apply_maturity_mismatch` fills it to 10y and the deposit-vs-loan mismatch never fires — a 6m deposit netting a 5y loan is recognised unadjusted. Carry the deposit maturity into the pool. Corrects P7.5's stale 'Art. 219 not in scope'. **Effort: M** | Ref: CRR Art. 219 w/ 237-238 · Ev: `collateral.py:230-297`, `haircuts.py:706` · §5 WS4
- **P1.273** [x] FIXED (Unreleased, 2026-07-18 — option (a): lease-as-collateral input convention per Art. 199(7)/211. New nullable `COLLATERAL_SCHEMA.is_lease_collateral_attested` (no Boolean default; null→conservative False): the leased asset is supplied as a real_estate/other_physical collateral row pledged to the lease exposure, and the attestation opens an independent FCM eligibility route in `collateral.py::_apply_collateral_unified` beside the P1.235 `is_eligible_irb_collateral` gate — CATEGORY-GUARDED to {real_estate, other_physical} (reviewer-caught leak: an unscoped OR would have granted a lease-attested receivables row FCM eligibility; pinned by a ≤1y-receivables negative test deliberately shaped to isolate the guard from the Art. 199(5) maturity cap, + a general-flag control). Art. 211(a) subsumption of Art. 208/210 verified defensible (`is_eligible_irb_collateral` is read only at this one gate, so no separate valuation/monitoring check is bypassed). Attested rows dodge CRM014; zero SA-path effect (no SA expression reads the flag, grep-verified). Worked case £10m senior corp F-IRB + £10m attested other_physical: CRR LGD* 0.428571 (Art. 230(2) 40% FCM haircut + 1.4× OC, LGDS 0.40) vs LGDU 0.45; B31 0.31 (LGDS 0.25, no OC) vs 0.40. Reviewer-caught citation sweep: mis-cited "Art. 224" → Art. 230(2) at every introduced site (Art. 224 is financial-collateral haircuts only). Number-neutral; goldens+oracle+parquets 0 movers; full gate green (unit 6101, acc/int/con/oracle 2862). Pinned by tests/unit/crm/test_p1_273_lease_collateral.py (10) + tests/acceptance/{crr,basel31}/test_p1_273_art_211_lease_collateral.py (3+3). @cites Art. 211 ×2 + snapshot regen. Reviewer revise→pass. SCOPE: F-IRB FCM secured-LGD path only (matches the item's 45%/40% framing); SA-side lease input convention stays with sibling P1.279; Art. 211(d) unamortised-vs-market numeric test is an attestation, not a computed check (proportionate for LOW/S).) **Effort: S** | Ref: CRR Art. 211 · Ev: `packs/common.py:726,767`, `risk_weights.py:1186,1358` · §5 WS4
- **P1.274** [x] FIXED (Unreleased, 2026-07-18 — Art. 218 own-issue gate for CLN collateral. New nullable `COLLATERAL_SCHEMA.is_own_issued_cln` (no Boolean default; null→conservative not-own-issued; no reporting-institution identifier exists so an attestation is the P1.239-recorded idiom). Shared predicate `haircuts.py::credit_linked_note_ineligible_expr` in the `ineligible_flags` machinery (P1.271 pattern): a CLN not attested own-issued has value_after_haircut zeroed + is_eligible cleared — reviewer-traced to kill EVERY benefit channel (SA E* via coalesce(value_after_maturity_adj, value_after_haircut), F-IRB effectively_secured, reporting aggregate is is_eligible-filtered; no channel reads raw market_value) — + one CRM019 WARNING per gated row. Fallback = INELIGIBLE, not debt-of-issuer (no CLN→bond route exists and CLN value tracks its reference entity — Art. 194(4) wrong-way; bond treatment would be anti-conservative). Predicate fires on the `credit_linked_note` literal only (normalize maps only that literal to cash; deposits/P1.239 partition untouched). Regime-identical (PS1/26 Art. 218 retained verbatim) → no Feature. Worked case both regimes: £1m unrated corp + £500k CLN → own-issued RWA £500k; third-party/null → RWA £1m + exactly 1 CRM019. Column-absent no-op is unreachable in-pipeline (edges.py:378 seals COLLATERAL_SCHEMA); test_life_insurance.py CLN tests take no gate branch (reviewer-verified not a masked dependency). No existing fixtures carry CLNs; number-neutral; goldens+oracle 0 movers; full gate green (unit 6109, acc/int/con/oracle 2871). Pinned by tests/unit/crm/test_p1_274_credit_linked_note_own_issue.py (8) + tests/acceptance/{crr,basel31}/test_p1_274_art_218_credit_linked_note.py (4+4). @cites Art. 218 + snapshot regen; ratchet +1×4 attributable. Reviewer pass. DEFERRAL: the Art. 218 embedded-CDS-eligibility proviso is folded into the attestation, not computed (conservative — ineligible is stricter).) **Effort: S** | Ref: Art. 218 · Ev: `haircuts.py:639,827-829` · §5 WS4
- **P1.275** [x] FIXED (Unreleased, 2026-07-18 — `compute_life_insurance_columns` rewritten: (1) Art. 233(3) 8% FX reduction via per-policy CUT-THEN-SUM — `li_total` (per-beneficiary sum) + `li_matched` (per beneficiary+policy-currency sum joined on the COMPOUND (beneficiary, exposure-denomination) key), effective = weight×[(1−Hfx)·total + Hfx·matched], so same-currency value stays whole and only the mismatched remainder is cut; deterministic on mixed-currency pools (reviewer-caught round-1 defect: the first cut summed THEN cut on an unsorted `.first()` representative currency — anti-conservative (a £900 USD policy behind a GBP first row escaped the cut) AND plan-order-nondeterministic; pinned now by mixed-pool £100GBP+£900USD→£928 + reversed-order determinism tests); Hfx = REUSED common-pack `fx_haircut` 0.08 (@cites Art. 233(3) at the consumer, no duplicate scalar); null policy currency → never matches the compound join → cut + per-policy CRM020; null exposure denom → all cut (conservative). (2) Pledge join extended exposure→facility/counterparty via CHAINED left-joins onto the single exposures base (concat-of-3-projections blew the crm_exit plan-node ceiling 3981 vs 2600 — chained shape keeps ceilings 2100/2600 untouched), pro-rata by EAD via guarded window (`partition_by_nullable`, div-by-zero guarded), benefit capped `min_horizontal(total_value, ead)`; disjoint-namespace assumption across pledge levels recorded in the module docstring. Pack citation fixed 232(1)→232(3). Worked cases (both regimes, £1m unrated corp, £500k policy, insurer→20%): matched £600k; USD-mismatch £632k; null-ccy £632k+CRM020; counterparty £1m over £600k+£400k → £200k. Number-neutral for existing fixtures (no currency column → absent no-op); goldens+oracle 0 movers; full gate green (unit 6124, acc/int/con/oracle 2879). Pinned by test_life_insurance.py +18 + tests/acceptance/{crr,basel31}/test_p1_275_art_232_life_insurance.py (4+4). Ratchet presence 351→356 + eager 42→43 audited exact. Reviewer revise→pass. DEFERRAL: Art. 233(4) maturity-mismatch scaling of Hfx not applied on this side-channel (no liquidation-period mismatch for surrender value).) **Effort: S** | Ref: Art. 232(2)-(3) · Ev: `life_insurance.py:129-158` · §5 WS4

**WS5 — IRB classification & risk parameters** (files: `subtypes.py`, `formulas.py`, `attributes.py`, `approach.py`, `transforms.py`, `ccf.py`, `crr.py`; high siblings P1.220/P1.221 above)
- **P1.242** [x] FIXED (Unreleased, 2026-07-18 — closes cluster sibling P1.246. `classify_exposure_subtypes` now derives `requires_fi_scalar = apply_fi_scalar OR (cp_is_financial_sector_entity AND cp_total_assets ≥ threshold)` (all operands explicit `.fill_null(False)`, no Kleene hole; the flag is a True-override only and can never suppress a derived True — Art. 153(2) "shall multiply" is mandatory). Threshold via the SME-cap `regulatory_threshold` + fx_derived Feature pattern: CRR EUR 70bn × 0.8732 → GBP 61.124bn; B31 pack fixed 0 → GBP 79bn native (VERIFIED vs ps126app1 IRB-Part glossary, "highest level of consolidation at which audited financial statements are available"; the B31 derivation was previously dead). CRR pack citation corrected Art. 4(1)(146) → Art. 142(1)(4) (value unchanged). Null total_assets on a flagged FSE → scalar NOT applied + CLS009 WARNING (suppressed when the flag is elected) — reviewer-adjudicated vs the P1.215 conservative precedent and UPHELD: it is the audit's own prescribed formula, only a handful of institutions clear the threshold so conservative-apply would overstate the whole FSE population, and the gap is surfaced not silent (recorded direction-of-error: potential under-application). Hand-calc £1m FSE corp PD 0.5%/LGD 45%/M 2.5: R 0.213456→0.266820, CRR RWA £737,884→£965,199, B31 £696,117→£910,565 (+30.8%, ×1.06 CRR scaling reconciled); £65bn mid-case fires CRR-only (> £61.1bn, < £79bn). Zero calculation movers — structurally guaranteed (generate_all invariant 5 forces is_financial_sector_entity=False in all scenario fixtures); only test edit is the B31 threshold pin 0→79bn. End-to-end flag→rwa_final already pinned by P1.159's acceptance test; new coverage = tests/unit/classifier/test_p1_242_lfse_fi_scalar.py (17). @cites ×3 + snapshot regen; ratchet fill_null +2 / eager +1 attributable. Full gate green (unit 6137, acc/int/con/oracle 2880). Reviewer pass. DEFERRED: the Art. 142(1)(5) unregulated-FSE limb (size-independent 1.25×) needs a nullable regulated-status schema field — recorded in the subtypes docstring, firb-calculation.md admonition, and changelog; `apply_fi_scalar` is the interim override for known unregulated FSEs.) **Effort: M** | Ref: CRR Art. 142(1)(4) / 153(2) · Ev: `subtypes.py:181-184`, `formulas.py:668-676`, `crr.py:621` · §5 WS5
- **P1.243** [x] FIXED (Unreleased, 2026-07-19 — Art. 147(5)(a) IRB retail cap made SME-limb-only, both regimes (texts verified identical in structure: natural persons uncapped limb (i), EUR 1m/GBP 880k conditions the SME limb (ii) only; the SA Art. 123/123A individual cap is a SEPARATE rule, grep-verified untouched, `qualifies_as_retail` stays False for over-cap individuals). Natural-person signal = `is_natural_person` OR `cp_entity_type ∈ NATURAL_PERSON_ENTITY_TYPES` (individual/natural_person/retail — `retail` verified an exclusively-natural-person alias in both pack maps; null flag + non-natural type → cap keeps binding, conservative). Restore on the IRB seam: `sync_irb_exposure_class` returns a cap-expelled natural person to RETAIL_OTHER gated on Art. 147(5)(c) `is_managed_as_retail.fill_null(True)` (reviewer-verified BIT-IDENTICAL to the front-door `_build_qualifies_as_retail_expr` null convention); `_align_irb_exposure_class` propagates to `exposure_class` for FIRB/AIRB-routed legs only — restore guard `== CORPORATE` exact, so CORPORATE_SME (SME-typed over-cap), RETAIL_MORTGAGE and QRRE are unreachable; SA-routed rows keep CORPORATE (Phase 7 seal coherence reviewer-traced: reporting_class_origin folds the restored class only where approach agrees; C08.07 Art. 147 origination basis uncorrupted). Direction: restore is RWA-down — the regulatorily-correct correction of a mandatory classification. Hand-calc £2m natural person PD 2%/LGD 45%: retail_other A-IRB RW 0.5799 B31 / 0.6147 CRR (×1.06), both pinned end-to-end through PipelineOrchestrator (the acceptance file carries a crr_result fixture — CRR covered despite living under basel31/). Zero suite movers (no existing fixture had an over-cap natural person); goldens+oracle 0 movers; full gate green (unit 6151, acc/int/con/oracle 2888). Pinned by tests/unit/classifier/test_p1_243 (10) + tests/acceptance/basel31/test_p1_243 (6 incl. CRR twin). Ratchet fill_null +2 attributable; @cites + matrix regen. Reviewer pass (non-blocking notes recorded: no explicit SA-routed negative-control test beyond flag-level; `retail`-typed null-size SME edge accepted by documented-alias design).) **Effort: M** | Ref: CRR/PS1-26 Art. 147(5)(a) · Ev: `attributes.py:566-579`, `subtypes.py:164-169` · §5 WS5
- **P1.244** [x] FIXED (Unreleased, 2026-07-19 — `is_qrre_candidate` now gates on ALL Art. 147(5A)(a)-(b) conditions: (a) individuals via the P1.243 `natural_person_expr()` (null→not-individual, conservative — SME-derived rows can never be QRRE); (b) unsecured via new nullable FACILITY attestation `is_secured` (null→unsecured, PERMISSIVE — reviewer-adjudicated vs the P1.237 precedent and UPHELD: securedness is ordinarily row-evidenced by pledges the pre-CRM classifier cannot see, and null→secured would demote the entire existing QRRE estate; direction disclosed on four surfaces incl. the hierarchy_exit EdgeColumn null_meaning), facility-COUPLED onto BOTH legs via `_FACILITY_QRRE_COUPLED_COLUMNS` (synthetic undrawn: facility_undrawn.py; drawn-loan inheritance: enrich.py — both behaviourally pinned, the drawn leg by TestUnsecuredGateDrawnLeg with a TDD-verified failure mode when the coalesce is neutered); (b) unconditionally-cancellable via the CCF UC signal risk_type∈{LR,low_risk} with the "to the extent undrawn" carve-out (fully-drawn passes trivially; undrawn MR/FR demotes; null→not-cancellable, matching the CCF convention). Demotions land RETAIL_OTHER + CLS010 once per run ((c)-limit demotions unflagged). SCOPE CORRECTION (reviewer-confirmed): CRR Art. 154(4) defines QRRE with identical conditions — gates apply BOTH regimes, no Feature; only the (c) limit differs per pack (CRR EUR 100k / B31 GBP 90k — the GBP 90,000 figure VERIFIED verbatim vs ps126app1.pdf p.91 "largest aggregate nominal exposure to a single individual … is GBP 90,000 or less"; the basel31 SKILL's 100k table entry is WRONG, do not "correct" the pack to it). Hand-calc £50k individual PD 2%/LGD 45%: QRRE R=0.04 → £15,329 CRR/£14,461 B31; demoted RETAIL_OTHER R≈0.0946 → £30,733/£28,993 (+100.5%; correlations cross at PD≈7.3% — demotion is conservative at fixture PDs). Blast radius: p1_191 QRRE control attested MR→LR (intent-preserving); test_p6_26 coupled-tuple +is_secured; zero golden/oracle movement. cites_decorators 254→273 reconciled as a lagging-floor catch-up (snapshot diff = exactly this item's 6 article-lines; actual codebase count verified ≥273) — not drift. Full gate green (unit 6170, acc/int/con/oracle 2896). Pinned by tests/unit/classifier/test_p1_244_qrre_gates.py (19) + tests/acceptance/basel31/test_p1_244_qrre_gates.py (6). Reviewer revise→resolved. DEFERRALS recorded: explicit wage-account derogation flag; classify-time pledge-presence derivation of is_secured; Art. 147(5A)(d)-(e) portfolio-volatility conditions are supervisory attestations, out of per-exposure scope.) **Effort: M** | Ref: PS1/26 Art. 147(5A)(a)-(b) · Ev: `subtypes.py:122-138` · §5 WS5
- **P1.245** [x] FIXED (Unreleased, 2026-07-19 — group revenue roll-up for the Art. 147(4C)(b)(ii) large-corp F-IRB-only gate (verified vs ps126app1 p.89/92: revenue "at the highest level of consolidation … average … over the last three years"; Art. 147A(1)(e)(ii) mandates F-IRB). New `attributes.with_group_annual_revenue`: LAZY self-join of the counterparty lookup on `ultimate_parent_reference` (no exposure fan-out; self-parenting orphans harmless via max(own,own)) → `cp_group_annual_revenue = max_horizontal(own, parent)` — max is the documented conservative proxy for the consolidated figure (max≥own for an F-IRB-forcing test; the parent's annual_revenue = its consolidated audited figure, recorded convention, no new schema field). `_apply_b31_approach_restrictions` reads the group figure; `cp_annual_revenue` untouched for the SME size test / Art. 501; CRR path bit-identical (Feature off, column unread). Null composition reviewer-traced: parent's figure resolves a null-own sub (flip pinned); genuinely both-null (own AND parent) STILL forces the conservative F-IRB default + CLS008 (re-keyed onto the group figure — a sub under a known-SMALL parent now correctly keeps A-IRB, replacing an over-conservative unknown default, disclosed); new CLS011 (once-per-run) flags roll-up-driven flips (own ≤440m ∧ group >440m), mutually exclusive with CLS008. Hand-calc £1m PD 1%/own-LGD 35%/M 3y: B31 A-IRB RW 0.7225/£722,482 → F-IRB LGD 0.40 RW 0.8257/£825,694 (+14.3% = 40/35, reviewer-reconciled). Number-neutral (no fixture had a B31 A-IRB corporate under a revenue-bearing resolved parent); goldens+oracle 0 movers; full gate green (unit 6186, acc/int/con/oracle 2905). Pinned by tests/unit/classifier/test_p1_245_group_revenue_rollup.py (16) + tests/acceptance/basel31/test_p1_245 (8 incl. null-own flip, small-group + standalone controls, CRR twin). cites 273→274. Reviewer pass (cosmetic note: CLS008/011 field_name still "annual_revenue"). DEFERRED: 3-year averaging (schema carries a single point-in-time annual_revenue; most-recent-figure convention recorded in docstring/spec/changelog — future schema enablement).) **Effort: M** | Ref: PS1/26 Art. 147(4C)(b)(ii) w/ 147A(1)(e) · Ev: `approach.py:186-203` · §5 WS5
- **P1.246** [x] FIXED (Unreleased, 2026-07-18 — closed by the P1.242 change (cluster: same code fix, mandatory-treatment angle). The multiplier is now derived (entity flag + size threshold), not an input election; `apply_fi_scalar` survives only as a True-override that cannot suppress a derived True. See the P1.242 annotation for full detail incl. the unregulated-FSE deferral.) **Effort: M** | Ref: CRR/PS1-26 Art. 153(2) · Ev: `subtypes.py:181-184`, `formulas.py:668-676`, `crr.py:621` · §5 WS5
- **P1.247** [x] FIXED (Unreleased, 2026-07-19 — equity IRB-Simple now emits Art. 158(7) EL via new cited pack LookupTable `equity_irb_simple_el` (0.8% ↔ diversified-PE 190% AND exchange-traded 290%; 2.4% ↔ other 370%; 0% central-bank as a documented convention), emission in `_apply_equity_weights_irb_simple` with shared bucket predicates (RW chain guarded bit-identical — every unit test asserts RW alongside EL); EL computes on the NET position (post `_net_short_positions`, consistent with RWA; p2_38 pins undisturbed). PREMISE CORRECTION (reviewer-adjudicated vs primary text, deviating from the audit's prescribed adjustments.py/aggregator edit): UK CRR Art. 159 subtracts only EL "calculated in accordance with Article 158(5), (6) and (10)" and Art. 36(1)(d) (live in the PRA Rulebook Own Funds Part) deducts only that Art. 159 amount — so Art. 158(7)-(9) equity EL has NO capital treatment beyond disclosure (COREP C08 / Pillar 3 IRB EL); `aggregator/_el_summary.py` deliberately UNTOUCHED, corroborated by the repo's recorded SI 2021/1078 facts (DOCS_IMPLEMENTATION_PLAN.md:92). The exclusion is pinned by a non-tautological acceptance guard (corporate pool EL £50k vs provisions £30k → shortfall stays exactly £20,000 when an equity EL £2,400 row is added, through the real aggregator). Scoping: CRR IRB-Simple only via existing `equity_irb_approaches_available` (B31→SA and CRR-SA controls emit nothing; PD/LGD path untouched — it already emitted, likewise unpooled). Hand-calcs: £200k exchange-traded → EL £1,600; £100k other → £2,400. Zero golden/oracle/acceptance movement (architecturally: equity origin is excluded from every EL-bearing template population — CR10 keys slotting-origin, C08/C09 key the IRB approach set; blast-radius mapped exhaustively pre-implementation). Full gate green (unit 6203, acc/int/con/oracle 2911). Pinned by tests/unit/crr/test_p1_247_equity_simple_el.py (17) + tests/acceptance/crr/test_p1_247_art_158_7_equity_simple_el.py (5). cites 274→275. Reviewer pass (notes: "Art. 36 omitted" report phrasing loose — 36(1)(d) moved to PRA Rulebook, still live; no @cites on the emitting function, attribution via pack Citation, defensible since onshored Art. 158 has no CRR index target).) **Effort: M** | Ref: CRR Art. 158(7)-(9) · Ev: `equity/calculator.py:692-734,1033-1043` · §5 WS5
- **P1.248** [ ] (MED) PS1/26 Art. 161(5) — the partially-secured corporate A-IRB LGD floor assigns the FULL secured-type floor to the whole exposure instead of the EAD-weighted blend of 25% unsecured + collateral LGDS; the blend exists but is gated to retail only. Extend the blend to corporate/institution A-IRB. B31. **Effort: M** | Ref: PS1/26 Art. 161(5) (CRE32.17) · Ev: `formulas.py:301-315,402` · §5 WS5
- **P1.249** [ ] (MED) CRR Art. 162(1) — no election to apply the fixed M=2.5y to FIRB non-SFT exposures (Art. 162(1) second sentence); the calculator always uses date-derived M clipped [1,5]. Add a pack Feature/config pinning FIRB non-SFT M=2.5y (repo-style stays 0.5y). CRR. **Effort: M** | Ref: CRR Art. 162(1) · Ev: `transforms.py:889-939` · §5 WS5
- **P1.250** [x] FIXED v0.3.15 (CRR A-IRB `_compute_ccf` branch: null `ccf_modelled` now falls back to the F-IRB supervisory CCF (`_firb_ccf_from_risk_type`, 75% for Art. 166(8)(d) credit lines) not the SA CCF, and modelled CCFs are suppressed for out-of-scope items via `in_166_8_scope = (is_obs_commitment | is_short_term_trade_lc) & ~risk_type in {FR,FRC}`; pinned by `tests/unit/test_ccf.py::TestArt16689CCFFallbackScope`; 3 stale sibling tests re-pinned) (MED) CRR Art. 166(8) — the A-IRB CCF fallback uses SA Art. 111 CCFs (50%/20%) when `ccf_modelled` is null, not the Art. 166(8) supervisory 75%; and modelled CCFs are honoured for any risk_type incl. issued items. Change the fallback to `_firb_ccf_from_risk_type` + scope to Art. 166(8)(a)-(d). CRR. **Understatement. Effort: S** | Ref: CRR Art. 166(8) · Ev: `ccf.py:569` · §5 WS5
- **P1.251** [ ] (MED) PS1/26 Art. 166C(1) w/ 111 Table A1 Row 4(b) — the B31 F-IRB OBS CCF reads `_firb_ccf_from_risk_type`, but the Row 4(b) UK-resi-mortgage 50% override patches only `_sa_ccf_from_risk_type`, so B31 FIRB UK-resi-mortgage commitments miss the 50% CCF. Patch both columns (mirroring `_apply_purchased_receivable_ccf`). B31. **Effort: M** | Ref: PS1/26 Art. 166C(1) · Ev: `ccf.py:510-520,577-578` · §5 WS5
- **P1.276** [ ] (LOW) CRR Art. 147(3)(b)/147(4)(c) — generic (non-named, CQS-rated) `mdb` maps to the CGCB IRB class like `mdb_named`; non-0% MDBs belong in the institutions class. Nil CRR capital effect but COREP class misreport. Map `mdb`→INSTITUTION. **Effort: S** | Ref: CRR Art. 147(3)(b)/147(4)(c) · Ev: `packs/common.py:747` · §5 WS5
- **P1.277** [ ] (LOW) CRR Art. 160(1) — the CRR `pd_floors` includes `sovereign: 0.03%` and the all-equal scalar shortcut floors CGCB IRB rows at 0.03%; CGCB is not subject to the floor under CRR (conservative overstatement). Set the CRR sovereign floor to 0/None + drop the shortcut. **Over-statement. Effort: S** | Ref: CRR Art. 160(1) · Ev: `packs/crr.py:135-147`, `formulas.py:151-153` · §5 WS5
- **P1.278** [ ] (LOW) CRR Art. 160(2)/163(2) — no top-down PD for purchased corporate receivables (PD = EL/LGD; dilution PD = EL); the classifier gates all IRB on `internal_pd` non-null, so such receivables fall to SA. Add `el_estimate` inputs + derivation. **Effort: S** | Ref: CRR Art. 160(2)/163(2) · Ev: `transforms.py:182-196` · §5 WS5
- **P1.279** [ ] (LOW) CRR Art. 166(4)/166A(4) — no discounting of minimum lease payments for the lease exposure value; only the SA `other_residual_lease` path exists. Document the input convention (`drawn_amount` = discounted MLP) + optional marker. **Effort: S** | Ref: CRR Art. 166(4)/166A(4) · Ev: `packs/common.py:726,767`, `risk_weights.py:1186,1358` · §5 WS5
- _P1.280 (WS5, citation): purchased-receivable 'Art. 166E(5)' → 'PS1/26 Art. 166A(5)' rename — **actionable bullet in Tier 5.**_

**WS6 — SA sovereign / quasi-sovereign risk weights** (files: `risk_weights.py`, `common.py`, `guarantor_rw.py`, `schemas.py`; high sibling P1.222 above)
- **P1.252** [ ] (MED) Art. 116(5) (CRR) / 116(3A) (PS1/26) — PSE Table 2/2A and the 20% short-term treatment are applied to every PSE regardless of jurisdiction; third-country PSEs need Treasury equivalence else a flat 100%. Add a jurisdiction-equivalence gate + 100% fallback. **Effort: M** | Ref: Art. 116(5)/116(3A) · Ev: `risk_weights.py:1058-1077,1266-1283` · §5 WS6
- **P1.253** [ ] (MED) CRR Art. 117(1) — non-named MDB *guarantors* are priced from the B31 MDB Table 2B under BOTH regimes; under CRR they must use the institution tables (rated Art. 120 Table 3, unrated Art. 121 Table 5). Gate the MDB guarantor branch on `is_basel_3_1`. CRR. **Effort: S** | Ref: CRR Art. 117(1) · Ev: `guarantor_rw.py:246-254,79` · §5 WS6
- **P1.281** [ ] (LOW) Art. 114(3) / 114(2A) — no ECB 0% branch (an unrated `central_bank` ECB falls to 100%) and no Art. 114(2A) mapping of an unrated central bank onto its sovereign's CQS under B31. Add ECB handling + coalesce `cp_sovereign_cqs` for central_bank rows (B31, Feature-gated). **Effort: S** | Ref: Art. 114(3) / 114(2A) · Ev: `risk_weights.py:1020-1031`, `crr_risk_weight_tables.py:343` · §5 WS6
- **P1.282** [ ] (LOW) Art. 115(2)/(4), 116(4) — RGLAs on the central-government-equivalence list are hardcoded to 0% for GB rather than routed through the Art. 114 CGCB treatment, so they understate on a UK downgrade below CQS1 for non-sterling; non-GB `rgla_sovereign` is unhandled. Re-route `rgla_sovereign` through CGCB. **Effort: S** | Ref: Art. 115(2)/(4), 116(4) · Ev: `risk_weights.py:1079-1084,1284-1290` · §5 WS6
- _P1.283 (WS6, docstring): Art. 118 IO-list `_create_io_df` docstring correction (runtime correct) — **actionable bullet in Tier 5.**_

**WS7 — SA institution / corporate risk weights** (files: `risk_weights.py`, `crr.py`, `common.py`, `schemas.py`)
- **P1.254** [ ] (MED) PS1/26 Art. 121(6) — the SCRA sovereign floor for a non-local-currency unrated-institution exposure never binds when the sovereign CQS is null (unrated); for a confirmed-unrated sovereign Art. 114(1) is 100%, so the floor should bind at 100%. Distinguish 'unrated' from 'missing' + floor at 100%. B31. **Effort: M** | Ref: PS1/26 Art. 121(6) · Ev: `risk_weights.py:1402-1436` · §5 WS7
- **P1.255** [ ] (MED) CRR Art. 122(2) — unrated corporates get a flat 100%, not `max(100%, sovereign-of-incorporation RW)`; a corporate in a CQS6 jurisdiction (sovereign 150%) is under-weighted at 100%. Add a CRR unrated-corporate `max_horizontal(1.00, CGCB lookup)` branch. CRR. **Understatement. Effort: M** | Ref: CRR Art. 122(2) · Ev: `packs/crr.py:938`, `risk_weights.py:1200-1368` · §5 WS7
- **P1.284** [ ] (LOW) CRR Art. 119(2)-(3) — no branch for ≤3m institution exposures denominated+funded in national currency (one category worse than the preferential sovereign RW, 20% floor); such exposures over-weighted. Conservative. CRR (sunsets 2026). **Over-statement. Effort: S** | Ref: CRR Art. 119(2)-(3) · Ev: `risk_weights.py:805-832,1200-1368` · §5 WS7
- **P1.285** [ ] (LOW) CRR Art. 119(4) — no path for intermediated Bank-of-England minimum reserves to be weighted as central-bank exposures. Conservative + permissive ('may'). CRR. **Over-statement. Effort: S** | Ref: CRR Art. 119(4) · Ev: `risk_weights.py` · §5 WS7
- **P1.286** [ ] (LOW) CRR Art. 119(5)-(6) / PS1/26 (not in PRA Rulebook) — `financial_institution` maps to INSTITUTION regime-invariantly; under B31 the Art. 119(5) comparably-regulated-FI rule was not carried over, so non-bank FIs should be corporates. Make the mapping regime-sensitive. **B31 understatement. Effort: S** | Ref: CRR Art. 119(5)-(6) · Ev: `packs/common.py:711,753` · §5 WS7

**WS8 — SA other classes (CIU, defaulted, ECAI seniority/currency)** (files: `risk_weights.py`, `equity/calculator.py`, `enrich.py`, `ratings.py`, `schemas.py`)
- **P1.256** [ ] (MED) PS1/26 Art. 127 vs 133(5) / 112 Table A2 — a defaulted subordinated instrument is re-weighted to the Art. 127 provision-based RW (100% at ≥20% provisions) instead of keeping the Art. 133(5) 150% (Table A2 priority 3 > 5). Extend the defaulted-override carve-out to `seniority=='subordinated'`. B31. **Effort: M** | Ref: PS1/26 Art. 127 / 133(5) · Ev: `risk_weights.py:1044-1052,1526-1533` · §5 WS8
- **P1.257** [ ] (MED) CRR Art. 127(3)-(4) — defaulted RRE/CRE-secured exposures must take a flat 100% regardless of the 20% provision test; the CRR branch applies the 150%/100% provision test to all defaulted rows (no mortgage carve-out). Add the carve-out. **CRR over-statement. Effort: S** | Ref: CRR Art. 127(3)-(4) · Ev: `risk_weights.py:1513-1524` · §5 WS8
- **P1.258** [ ] (MED) PS1/26 Art. 132(4) — the ×1.2 third-party-CIU-calc multiplier is applied only on the mandate-based branch, not the look-through branch, so LTA fund RW from third-party data is recognised 20% too low. Apply the multiplier on look-through too. Both regimes. **Effort: M** | Ref: PS1/26 Art. 132(4) · Ev: `equity/calculator.py:129-143` · §5 WS8
- **P1.259** [ ] (MED) PS1/26 Art. 138(1)(g)/139(6) — **SCHEMA** — no field flags a rating as incorporating implicit government support, so support-inflated bank ratings are accepted; from 1 Jan 2027 they must be excluded for institution obligors (falling to SCRA). Add `rating_incorporates_government_support` + a publicly-sponsored flag. *Schema-enablement first.* B31. **Effort: M** | Ref: PS1/26 Art. 138(1)(g)/139(6) · Ev: `schemas.py:634-665`, `ratings.py:103-140` · §5 WS8
- **P1.260** [ ] (MED) CRR Art. 139(2) — the Art. 138 counterparty CQS is joined onto every exposure unconditionally; an issuer rating may only *lower* an unrated item's RW if it ranks pari passu/senior. Under CRR a subordinated exposure to a CQS1 corporate gets 20% not the unrated 100%. Null the CQS for subordinated rows where the table RW would be lower. CRR. **Understatement. Effort: M** | Ref: CRR Art. 139(2) · Ev: `enrich.py:105-155`, `risk_weights.py:1045-1052` · §5 WS8
- **P1.261** [ ] (MED) CRR/PS1-26 Art. 141 — **SCHEMA** — no rating-denomination-currency field, so a local-currency rating drives FX exposures (non-conservative for the EM-sovereign LC/FC split); PS1/26 requires strict bidirectional matching. Add `rating_currency_scope` + per-exposure selection. *Schema-enablement first.* **Effort: M** | Ref: CRR/PS1-26 Art. 141 · Ev: `schemas.py:634-665` · §5 WS8
- **P1.287** [ ] (LOW) PS1/26 Art. 132(6) — mandate-based (`×1.2`) and look-through CIU RW are not capped at the 1,250% fall-back; a null mandate RW + third-party flag yields 15.0 > 12.5. Clip at `CIU_FALLBACK_RW`. **Over-statement. Effort: S** | Ref: PS1/26 Art. 132(6) · Ev: `equity/calculator.py:133-140` · §5 WS8
- **P1.288** [ ] (LOW) Art. 134(4) — `other_gold` gets 0% unconditionally; gold is 0% only to the extent backed by bullion liabilities, else 100%. Add a backing input + proportion. **Understatement. Effort: S** | Ref: Art. 134(4) · Ev: `risk_weights.py:1176-1180,1348-1352` · §5 WS8
- **P1.289** [ ] (LOW) Art. 134(7) — leased-asset residual RWEA uses fractional `1/t`, not nearest-whole-year `t`; both-direction misstatement. Round to nearest whole year before the division. **Effort: S** | Ref: Art. 134(7) · Ev: `risk_weights.py:1186-1187,1358-1359` · §5 WS8
- **P1.290** [ ] (LOW) Art. 134(6) — no representation of sold nth-to-default basket protection; a firm writing it cannot be capitalised (silent omission). Implement input + aggregation or document the exclusion + a validation error. **Effort: S** | Ref: Art. 134(6) · Ev: `risk_weights.py:1175-1189,1347-1361` · §5 WS8
- **P1.291** [ ] (LOW) CRR/PS1-26 Art. 138(2) — `is_solicited` / `rating_is_inferred` are declared but read nowhere; unsolicited ratings are used unfiltered. Consume the flag (config switch + DQ warning) or remove the dead column. **Effort: S** | Ref: CRR/PS1-26 Art. 138(2) · Ev: `schemas.py:643,661`, `ratings.py:109-119` · §5 WS8

**WS9 — SA retail & real-estate** (files: `risk_weights.py`, `enrich.py`, `attributes.py`, `flagging.py`, `schemas.py`)
- **P1.262** [ ] (MED) PS1/26 Art. 124E — a RE exposure is materially-dependent by DEFAULT (with enumerated exceptions), but the engine defaults `is_income_producing` to False → the preferential 20% loan-split instead of the 30-105% whole-loan tables. Treat null as materially-dependent under B31 unless an engine-verifiable exception fires + DQ warning. **Understatement. Effort: M** | Ref: PS1/26 Art. 124E · Ev: `enrich.py:488,533-535`, `attributes.py:456-487` · §5 WS9
- **P1.263** [ ] (MED) CRR Art. 126 — the 50% CRE weight is granted when `has_income_cover=True` — the OPPOSITE of Art. 126(2)(b) (preferential only where risk does NOT materially depend on the property). Split the semantics; the same column carries inverted meaning across regimes. CRR. **Effort: M** | Ref: CRR Art. 126 · Ev: `risk_weights.py:782-788`, `re_split/flagging.py:157-160` · §5 WS9
- **P1.292** [ ] (LOW) PS1/26 Art. 124A — `is_qualifying_re` null is treated as qualifying, so a firm that never populates the Art. 124A assessment gets 124F/124H preferential weights on every RE row (anti-conservative on missing data). DQ warning / default null → non-qualifying under B31. **Effort: S** | Ref: PS1/26 Art. 124A · Ev: `risk_weights.py:605`, `re_split/flagging.py:165` · §5 WS9
- **P1.293** [ ] (LOW) PS1/26 Art. 123 — the 45% QRRE-transactor weight is applied to ANY class containing 'RETAIL' with raw `is_qrre_transactor`, not gated on the engine-computed RETAIL_QRRE. Gate on `exposure_class == RETAIL_QRRE` + DQ warning. **Effort: S** | Ref: PS1/26 Art. 123 · Ev: `risk_weights.py:577-581` · §5 WS9
- **P1.294** [ ] (LOW) CRR Art. 124/125 — whole-loan RRE excess over the secured portion is blended with a hard-coded 75%, without checking `qualifies_as_retail`; a >EUR 1m individual's excess should be 100%. Make the excess weight conditional (75% if retail else 100%). **Understatement. Effort: S** | Ref: CRR Art. 124(1)/125 · Ev: `risk_weights.py:790-801`, `crr.py:1008-1010` · §5 WS9
- _P1.295 (WS9, spec-stale): Art. 126 'Code Divergence (D3.36)' note in `sa-risk-weights.md` (code now correct) — **actionable bullet in Tier 5.**_

**WS10 — SA exposure value / CCF** (files: `ccf.py`, `common.py`; high sibling P1.217 above)
- **P1.265** [ ] (LOW) PS1/26 Art. 111(1)(c) — the commitment-to-issue lower-of CCF cap fires under `CalculationConfig.crr()` when `underlying_risk_type` is supplied, but CRR Annex I has no SA lower-of rule (e.g. FR-over-LR drops 100%→0%). Gate the cap on the B31 feature or document it as an opt-in B31 election. CRR. **Effort: S** | Ref: PS1/26 Art. 111(1)(c) · Ev: `ccf.py:524-546` · §5 WS10
- _P1.266 (WS10, spec-stale): stale 'Not yet implemented' P2.31/32/33 block in `credit-conversion-factors.md` — **actionable bullet in Tier 5.**_
- **P1.267** [ ] (LOW) CRR Art. 111 Annex I item 1 / PS1/26 Table A1 Row 1(f) — an unrecognised `risk_type` falls to a 50% (MR) default; a direct credit substitute should be 100%. Consider a 100% (FR) fallback + a DQ error. Understatement on malformed input only. **Effort: S** | Ref: CRR Art. 111 Annex I item 1 · Ev: `packs/common.py:151-155`, `ccf.py:207` · §5 WS10
- **P1.268** [ ] (LOW) CRR Art. 113(6)-(7) / PS1/26 113(6) — no representation of the permission-gated intragroup 0% RW; a firm holding the permission can't apply it (all intragroup exposures get the class RW). Add an opt-in flag + config gate + a 0% branch. Conservative. **Over-statement. Effort: S** | Ref: CRR Art. 113(6)-(7) · Ev: `enums.py:764-767`, `risk_weights.py` · §5 WS10

**WS11 — Citations, docstrings & spec staleness**
- **P1.296** [ ] (LOW) Art. 222(3) w/ PS1/26 133 — `fcsm_equity_collateral_rw` is pinned 100% regime-invariant, so FCSM main-index-equity collateral uses 100% instead of the B31 equity RW (250%, transitional 160% in 2027), understating the blended RW under B31. Move the scalar into the regime packs (CRR 1.00; B31 2.50 + transitional). **B31 understatement. Effort: S** | Ref: Art. 222(3) w/ PS1/26 133 · Ev: `packs/common.py:101-105`, `simple_method.py:150-155` · §5 WS11 — *judgment call: kept in Tier 1 (not Tier 5) because it changes a calculation outcome, despite WS11 being the "citations/docs" cluster.*
- _P1.269 (WS11, citation): `@cites("CRR Art. 217")` on the wrong functions (watchfire mis-attribution) — **actionable bullet in Tier 5.**_

#### Tier 1b — Source-code docstring corrections (cosmetic, no calculation impact)

These items are docstring / code-comment / structural-organisation corrections only. The calculations they describe are correct; the bugs are in the prose surrounding them. Tagged Effort: S.

- **P6.15** [~] 3 missing schema fields: `protection_inception_date` (P1.10), `contractual_termination_date` (P1.20 revolving maturity), `liquidation_period` as config (P1.39 dependency). Note: prior plan iteration cross-referenced "P1.186" here, but that ID was used and closed v0.2.13 for an unrelated CRR Art. 224(2)(a) Hfx default fix — the `liquidation_period` config gap is its own residual sub-claim of P1.39 and not currently tracked under any separate P-code. **Effort: S**

### Tier 2 — Test Coverage Gaps (no code changes, but essential for confidence)

- **P5.11** [ ] **NEW** — Missing acceptance tests for secondary SA exposure classes. Covered bonds, PSE, RGLA, MDB, high-risk items, and other items (Art. 134) have 60-120 unit tests each but ZERO end-to-end acceptance tests. Need CRR and B31 acceptance scenarios. **Effort: M**
- **P5.12** [ ] **NEW** — Missing B31 acceptance tests for Art. 129A covered bond changes (SCRA-derived unrated RW). Only unit tests exist. **Effort: S**
- **P5.13** [ ] **NEW** — Missing acceptance tests for Art. 124A-124L RE treatment scenarios under B31 (income-dependent/non-dependent, commercial sub-types, Other RE Art. 124J). Only unit tests. **Effort: S**
- **P5.14** [ ] **NEW** — No integration/acceptance tests for COREP or Pillar III reporting generators. All 663+ COREP tests and 197+ Pillar III tests are unit tests with synthetic data. No test validates full pipeline → reporting output. **Effort: M**
- **P5.16** [ ] **NEW (2026-05-16 audit — test-side companion to P1.30(e))** — No acceptance scenario covering Art. 234 partial protection / tranching (CRR Art. 234, PRA PS1/26 Art. 234). Required once P1.30(e) is implemented; tracking now so the implementer doesn't ship without an end-to-end acceptance pin. Block on P1.30(e). **Effort: M** | Ref: CRR / PRA PS1/26 Art. 234
- **P5.17** [ ] **NEW (2026-05-28 audit — test-side companion to P8.53 + P8.39)** — No end-to-end acceptance scenario exercises the WWR LGD=100% override (P8.53) or the QCCP 2%/4% RW pin (P8.39) **through the orchestrator**. Both engine functions (`apply_wwr_gate`, `apply_ccp_risk_weight`) have thorough unit coverage (`tests/unit/ccr/test_wwr.py` 13 tests; `tests/unit/ccr/test_ccp.py` 8 tests) but those tests call the free functions directly — they do not prove the pipeline routes through them, which is exactly the gap P8.53/P8.39 describe. Track two acceptance scenarios so the implementer who wires the orchestrator call sites cannot ship without an integration pin: (a) **CCR-WWR-1** — a netting set with one `is_specific_wwr=True` trade + one non-WWR trade run through the full `PipelineOrchestrator`, asserting the WWR trade lands in a `<ns>__wwr__<trade>` synthetic NS with `wwr_lgd_override=1.0` while the non-WWR trade stays in the parent NS; (b) **CCR-CCP-1/2** — direct-cleared and client-cleared QCCP trades run end-to-end, asserting `risk_weight == 0.02` / `0.04` overrides the standard SA-Institution ladder (the load-bearing anti-degenerate: RW must NOT equal the CQS-2 institution 50%). Block on P8.53 and P8.39 respectively. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 291(5)(c), Art. 306(1); `tests/acceptance/ccr/`

### Tier 3 — COREP Reporting Completeness

- **P2.1** [~] COREP template rework — see detailed section below.
- **P2.5** [~] COREP missing row structure — OF 02.00 sub-class breakdown rows (0295-0297, 0355-0356, 0382-0385) need `cp_is_fse`/`is_sme` pipeline columns. OF 08.01 revolving row 0017 needs pipeline column. **Effort: M**
- **P2.7** [~] COREP pre-credit-derivative RWEA (row 0310) — approximated as total RWEA. Accurate split requires per-exposure pre/post CD tracking in CRM pipeline. **Effort: M**
- **P2.16** [~] **PARTIAL — (a) FIXED v0.2.41 (batch 20260530-0603); (b) deferred (single-stream)** — Output floor edge cases. (a) GCRA cap now enforced when S-TREA=0: `engine/aggregator/_floor.py::compute_of_adj` dropped the leaky `gcra_capped = min(gcra_amount, gcra_cap) if gcra_cap > 0 else gcra_amount` guard that passed the full uncapped GCRA through at zero S-TREA, replacing it with the unconditional `min(gcra_amount, gcra_cap)` so the 1.25%×S-TREA cap collapses the GCRA add-back to 0.0 (GCRA ≥ 0). Pinned by `tests/unit/test_of_adj.py::TestComputeOfAdj::test_gcra_cap_with_zero_s_trea` (inverted from the prior bug-codifying golden: gcra_capped 500_000→0.0, of_adj 0.0; controls test_gcra_cap_at_1_25_pct_of_s_trea + test_gcra_below_cap_unchanged unchanged). **(b) remaining:** S-TREA fallback to `rwa_final` when `sa_rwa` absent (`aggregator.py:138`) skews the GCRA cap on the audit path — `aggregator.py` is a hard-excluded shared file, so pick (b) SINGLE-STREAM via `/implement-scenario P2.16b`, not in a parallel `/next-items` batch. **Effort: S** | Ref: PRA PS1/26 Art. 92(2A)
- **P2.21** [x] **CLOSED (Phase 4 Slice 8, 2026-06-13)** — Stage data-quality errors now reach `result.errors` verbatim (original code/severity/category + all reference fields) via the unified `STAGE_ERRORS` channel (`engine/orchestrator.py::append_stage_errors`); the lossy PIPELINE_* rewriting and the `CCR_ERRORS` dodge channel are deleted — only stage crashes still mint `PIPELINE_<STAGE>`. Pinned by `tests/unit/test_pipeline.py::TestStageErrorChannel`. — Pipeline error conversion drops ALL `CalculationError` metadata (not just `regulatory_reference`). Three `getattr` blocks in `pipeline.py` (`:510, 512` resolution-error path / `:645, 647` classification-error path / `:683, 684` equity-error path — earlier `:509,511,644,646,682` reference drifted by one line) use `getattr(error, "error_type", "unknown")` and `getattr(error, "context", {})` but `CalculationError` (`contracts/errors.py:47-56`) is `@dataclass(frozen=True)` with only `code/message/severity/category/exposure_reference/counterparty_reference/regulatory_reference/field_name/expected_value/actual_value` — no `error_type` and no `context`. Both `getattr` calls always return the default. Audit trail severely degraded. (Note: the CRM/RE-split paths at `:721, 757` instead use `error.code` directly, which DOES resolve — so this is specifically the three `getattr` sites above.) **Effort: S**
- **P1.143** [ ] **NEW (Phase 9)** — Rule 4.11 unfunded credit protection grandfathering window (1 Jan 2027 – 30 Jun 2028) not honoured. Distinct from P1.10 (which is the underlying Art. 213 eligibility check; P1.143 is the transitional date-gated relaxation that depends on it). **Update 2026-07-16: the P1.10 base gate is now LANDED (Feature `ucp_unilateral_change_ineligible`, flags on GUARANTEE_SCHEMA), so P1.143 is unblocked on that side — remaining prerequisites: a `protection_inception_date` input field (P6.15), then implement Rule 4.11 as a date-gate disapplying ONLY the `is_unilaterally_changeable` limb for pre-1 Jan 2027 contracts (leave the cancellable limb live); consider the CRR Art. 213(1)(c)(ii) cost-increase limb (recorded deferral from P1.10) in the same pass.** **Effort: M** | Ref: PRA PS1/26 Rule 4.11
- **P2.25** [ ] **NEW (Phase 9)** — CR5 special disclosure rules not implemented. `reporting/pillar3/generator.py:325-359` omits: (a) currency mismatch multiplier exposures reported at pre-multiplier RW with post-multiplier RWEA; (b) regulatory RE not-materially-dependent split up to / above 55% LTV; (c) equity transitional reported at end-state RW. **Effort: M** | Ref: PRA PS1/26 Annex XX §CR5
- **P2.27** [ ] **NEW (Phase 9)** — OF 08.01 col 0275 uses IRB EAD not SA-equivalent. `reporting/corep/generator.py:3615`. Annex II §OF 08.01 col 0275 requires SA-equivalent post-CCF / post-CRM EAD. Using IRB EAD overstates the non-modelled exposure denominator for the output floor. **Effort: S** | Ref: COREP Annex II §OF 08.01 col 0275 — **DROPPED at design gate (batch 20260530-0213); re-scope to Effort M–L, NOT a generator one-liner.** A scenario-architect + reviewer pass confirmed there is NO SA-equivalent EAD column anywhere in `src/rwa_calc/`, and the only Effort-S derivation (`sa_equivalent_ead = sa_rwa / sa_risk_weight`) is algebraically circular: `engine/sa/calculator.py:205` builds `sa_rwa = ead_final × sa_risk_weight`, so dividing back out reproduces the IRB `ead_final` col 0275 already sums (a no-op). A correct fix is a dedicated engine batch that recomputes SA-basis post-CCF EAD (SA Table A1 CCFs applied to the IRB population) and re-bases BOTH col 0275 (SA-equivalent EAD) AND col 0276 (SA-equivalent RWA floor numerator — which today silently assumes EAD_SA == EAD_IRB) together. Pick alone / not as a parallel-batch Effort-S item.
- **P2.28** [ ] **NEW (Phase 9)** — CR9 row taxonomy incomplete. `reporting/pillar3/templates.py:565-580` missing AIRB RE 4-way sub-class split (resi SME, resi non-SME, comm SME, comm non-SME) and FIRB "financial / large corporates" sub-row. **Superseded by P2.49** which expresses the full row taxonomy. **Effort: S** | Ref: PRA PS1/26 Annex XXII §CR9
- **P2.37** [ ] **NEW** — Art. 155(4) Internal Models Approach (CRR equity) entirely absent. VaR-based equity capital with PRA-permission gate. Lower priority than P1.153 (PD/LGD approach). **Effort: L** | Ref: CRR Art. 155(4)
- **P2.40** [ ] **NEW (re-scoped again 2026-05-26 — file:line drift)** — model_id precedence chain incomplete. `src/rwa_calc/engine/hierarchy.py:2211-2255` (`_attach_counterparty_rating` body — original 2026-05-16 pointer at `:~2297-2331` is stale, but the substantive claim still holds: `cp_select` at `:2225-2231` only pulls `internal_model_id` from `counterparty_lookup.counterparties`; on `:2250-2252` `model_id` is sourced exclusively from the counterparty's inherited rating row via `pl.col("internal_model_id").alias("model_id")`). Spec specifies an exposure → facility → loan → counterparty → model_permissions default precedence. Original 2026-04 bullet's `1288-1295` pointer pointed at the MOF allocation block (`_allocate_mof_facility_undrawn`) which is unrelated. **Effort: M** | Ref: model_permissions spec — **DROPPED at design gate (batch 20260530-0213); must be split + multi-file, NOT `hierarchy.py`-only.** A scenario-architect audit confirmed there is NO `model_id`/`internal_model_id` source column at facility/loan/exposure grain in any schema (`FACILITY_SCHEMA`/`LOAN_SCHEMA`/`CONTINGENTS_SCHEMA`/`RAW_EXPOSURE_SCHEMA`/`RESOLVED_HIERARCHY_SCHEMA`); `model_id` only enters via `RATINGS_SCHEMA` at counterparty grain (the existing `hierarchy.py:2316-2322` alias). The memory note "model_id on Facility/Loan/Contingent fixture dataclasses" is a test scaffold (`tests/fixtures/p1_126/p1_126.py:187-190`) that pre-propagates onto the exposure frame to *bypass* the production path — not a schema column. A `hierarchy.py`-only coalesce is a no-op without an upstream source. **Split into P2.40a** (schema+loader enablement: add `model_id: ColumnSpec(pl.String, required=False)` to FACILITY/LOAN/CONTINGENTS, thread through RAW_EXPOSURE/RESOLVED_HIERARCHY + the loader exposure-flattening — touches `data/schemas.py` + `engine/loader.py`, does NOT touch the 4 hard-excluded shared files) **then P2.40b** (the `hierarchy.py` `coalesce(pl.col("model_id"), pl.col("internal_model_id"))` precedence). Pick P2.40a first via `/implement-scenario`.
- **P2.45** [ ] **NEW** — Art. 143(6) Overseas Model Approach (OMA) not represented. New B31 IRB permission for retail/SME-corp of equivalent-jurisdiction overseas subs, capped at 7.5% of group RWA / EV pre-output-floor. Need: OMA enum, jurisdiction-equivalence Bool, post-aggregation cap validation, Art. 143(6)(c)-(k) checks. **Effort: M** | Ref: PRA PS1/26 Art. 143(6)/(7)/(8)
- **P2.47** [~] **PARTIAL — Art. 137 OECD MEIP (Basel-3.1 arm) FIXED v0.2.39 (batch 20260530-0426); Art. 136 / 138 / 121 remain** — Art. 137 Table 9 MEIP-score→RW direct sovereign path was already shipped on the **CRR arm** under P1.100; v0.2.39 adds the missing **Basel-3.1 arm**. `engine/sa/risk_weights.py::_apply_b31_risk_weight_overrides` (*was `sa/namespace.py`, retired Phase 4; refreshed 2026-07-07*) gains the `cp_eca_score` branch (mirroring the CRR sibling, ordered below the Art. 114(3)/(4) domestic-currency 0% override and above the unrated fallback) so an unrated B31 sovereign carrying an ECA/MEIP score gets the Art. 137 RW (e.g. score 2 → 20%) instead of the Art. 114 unrated 100% default — reusing the existing `ECA_MEIP_RISK_WEIGHTS` data table via `_eca_meip_rw_expr()` (no new scalar, no schema change), `@cites("CRR Art. 137")` (watchfire WHITELIST synced). Pinned by `tests/acceptance/basel31/test_p2_47_art137_meip_b31.py` (RW 0.20 / RWA 1,000,000 on EAD 5,000,000; anti-confound RW≠1.00). **Remaining sub-claims:** Art. 136 grade-string→CQS map; Art. 138 second-best across ECAI+ECA; Art. 121 sovereign-derived institution propagation. **Effort: M** | Ref: PRA PS1/26 Art. 136-138, 121
- **P2.9** [~] **RE-SCOPED 2026-05-21** — OF 34.07 IRB CCR template now in scope under Tier 8 (P8.50). Becomes a sub-deliverable of P8.50 / P8.51 (COREP CCR template suite) rather than a known gap. Originally tracked as "CCR is out of scope; document as known gap" — now actively planned. **Effort: M** | See Tier 8.
- **P4.20** [ ] C 08.02 PD bands use fixed buckets instead of firm-specific internal rating grades. **Effort: M**

### Tier 4 — Pillar III Disclosure Gaps

- **P3.3** [~] Missing qualitative tables UKB CRD (SA qualitative, Art. 444(a-d)) and UKB CRE (IRB qualitative, Art. 452(a-f)). CR6 AIRB purchased receivables sub-row missing. CR5 rows 18-33 not detailed. **Effort: M**
- **P3.6** [ ] **NEW (Phase 9)** — CR5 / CR9 / OV1 disclosure gaps overlap with P2.25/P2.28/P2.29. Pillar III tables cannot faithfully reproduce Annex XX / XXII rows without: (a) pre-multiplier RW column, (b) equity transitional end-state RW column, (c) AIRB RE 4-way sub-class split, (d) FIRB financial/large-corp sub-rows, (e) OV1 equity sub-approach populations. **Effort: M** | Ref: PRA PS1/26 Annex XX §CR5/OV1, Annex XXII §CR9

### Tier 5 — Documentation & Consistency

- **P4.2** [~] Stale version numbers — `overview.md` says 0.1.37, `prd.md` says 0.1.28; current `pyproject.toml` is **0.2.20**. Plan header re-baselined; docs side still drifted. **Effort: S**
- **P4.3** [~] Stale `docs/plans/implementation-plan.md` — shows items as incomplete that are done. **Effort: S**
- **P4.4** [~] Stale PRD FR statuses. **Effort: S**
- **P4.8** [~] COREP template spec thin in `output-reporting.md` — detailed in `corep-reporting.md` feature doc. Cross-reference needed. **Effort: S**
- **P4.9** [~] Type checker inconsistency — docs disagree with CLAUDE.md (`mypy` vs `ty`). **Effort: S**
- **P4.10** [~] `model_permissions` not in architecture spec. **Effort: S**
- **P4.23** [ ] Stale test counts in `regulatory-compliance.md` (97 CRR / 116 B31; actual 169/212). **Effort: S**
- **P4.24** [ ] Stale NFR metrics in `nfr.md`: 1,844+ tests (actual 5,188); "6 stages" (actual 9 protocols); 8 namespaces (actual 2). **Effort: S**
- **P4.25** [ ] Stale acceptance test counts in `index.md`. **Effort: S**
- **P4.28** [ ] Stale C 07.00 column count in `reporting/corep/templates.py` docstring (says 24/22, actual 27/27). **Effort: S**
- **P4.29** [ ] Stale "Not Yet Implemented" in `docs/user-guide/regulatory/basel31.md:186-187, 196` (currency mismatch, defaulted — both done). **Effort: S**
<!-- P4.30 CLOSED 2026-05-26 (silent fix — see Completed Items): regulatory-compliance.md:80 / :99-101 now have CRR-J, B31-K, B31-L, B31-M; CRR-D enumeration at :73 covers D1–D6. -->

- **P4.31** [ ] Skill ref `slotting-changes.md` shows BCBS pre-op PF weights (80/100/120/350%) as PRA B31; PRA has no separate pre-op table. **Effort: S**
- **P4.32** [ ] Skill reference files (`.claude/skills/.../references/*.md`) need systematic BCBS-vs-PRA audit. **Effort: S**
- **P4.33** [ ] Stale BCBS output-floor phase-in in a docstring: `contracts/bundles.py:1003` says "50% (2027) to 72.5%" — should be 60% (2027) → 72.5% (2030+) per PS1/26 Art. 92(5). Runtime correct. *(Paths refreshed 2026-07-07: the former `ui/marimo/comparison_app.py` was deleted in the UI rearchitecture; `ui/views/comparison.py` and `analysis/transition.py` already read correctly, so `bundles.py:1003` is the only remaining site.)* **Effort: S**
- **P4.47** [ ] Informational: PS1/26 output-floor transitional is 3 years (60/65/70% 2027-2029 per Art. 92(5)); 2030+ → Art. 92(2A) 72.5%. Audit any cite of Art. 92(5)(d) (doesn't exist). **Effort: S**
- **P4.48** [ ] **Likely OBSOLETE — verify & close (2026-07-07)** — targeted a wrong "Art. 161(1B)" covered-bond comment in `data/tables/firb_lgd.py`, but that package was deleted (Phase 5 S13, arch_check ch.12) and a repo-wide grep for `161(1B)` now returns **zero** hits, so the offending comments no longer exist. Confirm the current covered-bond LGD path (`rulebook/packs/{crr,b31}.py` + `engine/sa/*_risk_weight_tables.py`) cites Art. 161(1)(d) with the (correct) 11.25% value, then close. **Effort: S**
<!-- P4.49 CLOSED 2026-05-26 (silent fix — see Completed Items): the cited file `irb/ccf.py` does not exist (consolidated into `engine/ccf.py`), and the live file already uses Art. 166(8) — e.g. `engine/ccf.py:6, 11, 40, 123, 136, 137, 138, 141, 167, 171` all cite Art. 166(8). No occurrence of `Art. 166(9)` remains in `engine/`. -->
- **P4.51** [ ] **File:line + scope re-scoped 2026-05-26** — Two `ExposureClass` docstring typos in `domain/enums.py`: `INSTITUTION` at `:52` cites Art. 112(d) (correct CRR ref is **(f)** — Art. 112(d) is MDBs); `HIGH_RISK` at `:106` (original bullet cited `:76`, file has grown) cites Art. 112(l) (correct CRR ref is **(k)**). The original bullet additionally claimed a third typo at `:91` (which it labelled DEFAULT) — that line is the PSE docstring citing Art. 112(c), which is actually correct CRR; line `:88` (DEFAULT) cites Art. 112(j) which is also CRR-correct (defaults are (j), high-risk is (k), institutions are (f)). Narrowing to the two real typos only. **Effort: S**
- **P4.52** [ ] `ciu_holdings` not in `docs/specifications/architecture.md` data-model section. **Effort: S**
- **P4.53** [ ] Undocumented CalculationConfig fields: `PostModelAdjustmentConfig`, `collect_engine`, `spill_dir`. **Effort: S**
- **P4.54** [ ] fastexcel vs xlsxwriter inconsistency between `overview.md` and `output-reporting.md`. **Effort: S**
- **P4.55** [ ] Document new B3.1 sovereign/institution PD floors (pairs with P2.36) in `firb-calculation.md`/`airb-calculation.md`. **Effort: S**

<!-- 2026-07-07 compliance-audit intake: pure-citation / spec-staleness lows (runtime correct — no calculation impact). Full detail: docs/plans/compliance-audit-crr-111-241-rectification.md §5. Their capital-correctness workstream siblings are in the dated Tier 1 audit block. -->
- **P1.266** [ ] **(WS10)** `docs/specifications/crr/credit-conversion-factors.md:394-404` still carries a 'Not yet implemented' warning block for P2.31/P2.32/P2.33, all of which are shipped (product→risk_type derivation, purchased-receivable 40%/10% CCF, Row 4(b) UK-resi auto-CCF). Rewrite the admonition to the shipped behaviour. **Effort: S** | Ref: CRR/PS1-26 Art. 111 Table A1 · §5 WS10
- **P1.269** [ ] **(WS11)** `@cites("CRR Art. 217")` decorates `_apply_maturity_mismatch_to_guarantees` (which implements Art. 238/239) and `apply_guarantees` (substitution), so `watchfire matrix` attributes Art. 217 to the wrong functions. Move Art. 217 (+ add Art. 202) onto `irb/guarantee.py::_apply_double_default`; replace the `guarantees.py:1267` cite with Art. 238/239. **Effort: S** | Ref: CRR Art. 217 · Ev: `engine/crm/guarantees.py:1267,91` · §5 WS11
- **P1.280** [ ] **(WS5)** 13 code refs cite the non-existent 'Art. 166E(5)' for the purchased-receivable 40%/10% CCF (values correct); global rename to 'PS1/26 Art. 166A(5)' across `ccf.py`/`schemas.py`/`facility_undrawn.py`/`unify.py` + the spec, and refresh the stale P2.31-33 warning. **Effort: S** | Ref: PS1/26 Art. 166A(5) · Ev: `engine/ccf.py:385,589-599`, `data/schemas.py:200-206` · §5 WS5
- **P1.283** [ ] **(WS6)** `_create_io_df` docstring (`crr_risk_weight_tables.py:339-353`) lists 16 IOs (invents 'EFSI', misplaces the ECB, conflates the Art. 117(2) MDB list); Art. 118 UK CRR is a closed 5-entry list (EU, IMF, BIS, EFSF, ESM). Correct the docstring. Runtime 0% is correct. **Effort: S** | Ref: Art. 118 · Ev: `engine/sa/crr_risk_weight_tables.py:339-353` · §5 WS6
- **P1.295** [ ] **(WS9)** `docs/specifications/crr/sa-risk-weights.md:1046-1051` carries a stale 'Code Divergence (D3.36)' note claiming a binary whole-loan Art. 126 treatment; the code now does the proportion split (50% up to 50% MV + Art. 122 residual). Close/rewrite the note. **Effort: S** | Ref: CRR Art. 126(2)(d) · Ev: `docs/specifications/crr/sa-risk-weights.md:1046-1051` · §5 WS9

### Tier 6 — Code Quality

- **P6.15** [~] 3 missing schema fields — see Tier 1 above.
- **P6.17** [ ] **NEW (Phase 9)** — Pre-floor `rwa_final` naming collision. `engine/pipeline.py:487-490`. Consumers reading `sa_results` / `irb_results` see pre-floor values under the label `rwa_final`, creating ambiguity against the post-floor `rwa_final` on the aggregated bundle. **Effort: S**
- **P6.24** [ ] **NEW** — `LazyFrameResult` mutability across stage boundaries. `src/rwa_calc/contracts/errors.py:86`. Not `frozen=True`; exposes `add_error`/`add_errors` mutation methods. Contradicts immutable-bundle architecture principle. **Effort: S**
- **P6.27** [ ] **NEW (batch 20260508-0020 surfacing)** — Pre-existing failures in `tests/unit/crm/test_collateral_sequential_fill.py`: `TestSequentialFillAllCategories::test_all_five_categories_b31` (got 0.15015 vs 0.1595 expected) and `TestPerTypeMinThreshold::test_covered_bonds_no_threshold` (got 0.34595 vs 0.3655 expected). Both reproduce on the unmodified main `src/` per engine-implementer attestation. Numerical drift on multi-collateral sequential-fill blended LGD calculation. **Effort: S** | Ref: `src/rwa_calc/engine/crm/collateral.py`, `tests/unit/crm/test_collateral_sequential_fill.py:733,990`
- **P6.28** [ ] **NEW (batch 20260509-1100 surfacing)** — Pre-existing test-vs-engine drift in `tests/unit/test_sovereign_floor_institutions.py::TestTradeExemption::test_trade_lc_exempt_from_floor`. Test asserts B31 institution SCRA-A short-term trade-LC RW = 40% but engine returns 20% — driven by the SCRA short-term Art. 121(4) trade-finance extension shipped in P1.128 (v0.2.13) which routes `is_short_term_trade_lc=True` through `B31_ECRA_SHORT_TERM_ECAI_RISK_WEIGHTS`/SCRA short-term branch. Resolve by re-deriving the regulatorily correct value (per Art. 121(4) text the SCRA-A short-term ≤6m branch yields 20% — test expectation is stale). **Effort: S** | Ref: `tests/unit/test_sovereign_floor_institutions.py:252-270`
- **P6.29** [ ] **NEW (batch 20260509-1100 surfacing)** — Stress fixture under `tests/acceptance/stress/test_stress_pipeline.py` is stale: 56 errors with `ColumnNotFoundError: unable to find column "has_short_term_ecai"`. Stress fixture data was generated before P1.105 (v0.2.12) added the `has_short_term_ecai` field to `FACILITY_SCHEMA`. Regenerate the stress fixture or have the stress generator default-fill the column. Pre-existing across batches. **Effort: S** | Ref: `tests/acceptance/stress/`, `src/rwa_calc/data/schemas.py` `FACILITY_SCHEMA`, `_b31_append_institution_maturity_branches`
- **P6.31** [ ] **NEW (2026-05-26 audit — companion to P4.53; file:line re-scoped 2026-05-28)** — Two undocumented `CalculationConfig` fields: `audit_cache_dir: Path | None = None` and `audit_cache_max_runs: int | None = None` at `contracts/config.py:958-959` (earlier `:951-952` drifted as the dataclass grew; also surfaced via `crr()` and `basel_3_1()` factory kwargs at `:1021-1022, 1102-1103`). Used by `src/rwa_calc/api/service.py:81-94, 193-194, 203-204` to opt into per-run audit-cache materialisation. Only `docs/specifications/audit-cache.md` documents the feature; `docs/specifications/configuration.md` (the config spec) does not list either field. Pair with P4.53 (which already tracks `PostModelAdjustmentConfig`, `collect_engine`, `spill_dir`). **Effort: S** | Ref: `src/rwa_calc/contracts/config.py:946-952`, `docs/specifications/configuration.md`
- **P6.34** [ ] **NEW (doc-writer SA-CCR batch surfacing — watchfire CRR index gap)** — Five SA-CCR engine functions waive their `@cites(...)` decorator with a NOTE comment because the bundled watchfire CRR rulebook index does not recognise CRR Art. 274 / Art. 275: `src/rwa_calc/engine/ccr/sa_ccr.py:48-52` (`compute_ead` — Art. 274), `src/rwa_calc/engine/ccr/rc.py:28-32` (`compute_rc_unmargined` — Art. 275(1)), `src/rwa_calc/engine/ccr/rc.py:51-52` (`compute_rc_margined` — Art. 275(2)). Same pattern previously surfaced for Art. 280a/b/c (P8.7 fix-commit precedent) and Art. 277/277a (collapsed to parent Art. 277 in `pfe.py:138`). Should be batched into a single watchfire CRR rulebook-index extension that covers Art. 274, 275, 277, 277a, 278, 279a/b/c, and 280a/b/c so the per-function `@cites` decorators can be added and the article→function matrix produced by `uv run watchfire matrix` becomes complete for the SA-CCR chain. Complements partial-coverage tracker P8.48. **Effort: S** | Ref: `docs/development/citation-tracking.md`; watchfire 0.3.1 CRR rulebook_version 2026-05-15; `src/rwa_calc/engine/ccr/sa_ccr.py:48-52`, `src/rwa_calc/engine/ccr/rc.py:28-52`, `src/rwa_calc/engine/ccr/failed_trades.py:72-74`, `src/rwa_calc/engine/ccr/pipeline_adapter.py:127`

- **P6.36** [ ] **NEW (2026-05-28 audit — SFT-FCCM follow-on consistency gap)** — `ccr_method` output column now carries a **third literal** `"fccm_sft"` (emitted at `src/rwa_calc/engine/sft/fccm.py:544` — *was `engine/ccr/sft_fccm.py`, promoted to `engine/sft/` in the SFT/FCCM separation; refreshed 2026-07-07* — and documented at `src/rwa_calc/domain/enums.py:498-499`), introduced by the P8.38 SFT-FCCM branch, but the surrounding contracts still encode the two-/one-literal world: `CCRConfig.method` is typed `Literal["sa_ccr"]` at `src/rwa_calc/contracts/config.py:866` (this is the *EAD method* knob, distinct from the new `sft_method` field, but the comment / Literal no longer reflects that `fccm_sft` is a possible *output* discriminator), and the `CCRCalculator` protocol output-schema docstring at `src/rwa_calc/contracts/protocols.py:253-254` still reads "`ccr_method` (pl.String): method discriminator. First batch returns `"sa_ccr"` for every row". Update the protocol docstring to enumerate `{"sa_ccr", "fccm_sft"}` (and the future `"imm"`), and add a one-line note on `CCRConfig.method` clarifying it is the derivative-EAD method while `sft_method` selects the SFT-EAD method. No calculation impact — typing/docstring consistency only, so a downstream consumer reading the protocol contract isn't surprised by the third value. **Effort: S** | Ref: `src/rwa_calc/contracts/protocols.py:253-254`, `src/rwa_calc/contracts/config.py:866`, `src/rwa_calc/engine/ccr/sft_fccm.py:252`
- **P6.37** [ ] **NEW (2026-05-28 audit — Polars forward-compat)** — 70 `.is_in([list-literal])` call sites across `src/rwa_calc/engine/` and `src/rwa_calc/reporting/` trigger the Polars `DeprecationWarning: "is_in with a collection of the same datatype is ambiguous and deprecated. Please use implode to return to previous behavior."` (observed on every CCR acceptance run, e.g. emitted from `engine/materialise.py:146` collect path). Representative sites: `reporting/corep/generator.py:2300,2348,2628,3980,4133,4156,4164,4166,4180`, `engine/classifier.py:313,316,1841,1910`, `engine/hierarchy.py:2494,2779`, `engine/equity/calculator.py:551`, `engine/ccf.py:300`. The warning is currently benign but a future Polars major will change `is_in([...])` semantics or remove the list-collection form. Sweep all 70 sites to the recommended form (wrap the literal in `pl.Series` / use `.implode()` per the polars guidance, or pin the comparison dtype). Pure forward-compat / code-hygiene — no current calculation or test impact (warnings only). **Effort: M** | Ref: https://github.com/pola-rs/polars/issues/22149; `grep -rn "\\.is_in(\\[" src/rwa_calc/` (70 hits)

- **P6.47** [ ] **NEW (2026-07-07 — durable root-cause fix behind the reconciliation loan-detail 500)** — `ResultsCache` writes every run to a **fixed** `<cache_dir>/last_results.parquet` (`src/rwa_calc/api/results_cache.py:130`), so a later `CreditRiskCalc.calculate()` / `.reconcile()` overwrites the very file that a still-registered `ReconciliationResponse` / `CalculationResponse` re-scans lazily on demand — a torn / stale read raising `polars ComputeError: parquet: File out of specification: The page header reported the wrong page size`. The Unreleased fix mitigated it two ways (reconciliation `loan_detail` + CSV/Excel exports now filter the **memoised** snapshot instead of re-scanning disk; all `ResultsCache` writes are now atomic via temp-file + `os.replace`), but the fixed-filename hazard remains for **any** consumer that re-scans on demand — results-page pagination (`get_page` / `scan_results`), a reconcile `component_reconciliation` / `class_allocation` re-scan, or a future export. Durable fix: give each run a **unique** results filename (`results_<run_id>.parquet`) so every registered response scans an **immutable snapshot**, and re-point `load_cached()` (which currently keys on the fixed name, `:163-187`) at a "latest" pointer — a small manifest JSON, or an atomically-updated copy/symlink. **Caveat:** needs a retention/GC policy (ref-count or TTL) so a snapshot is never deleted while a live response still references it. **Effort: M** | Ref: `src/rwa_calc/api/results_cache.py:130,163-187`, `src/rwa_calc/api/service.py:96-103,227`; `docs/appendix/changelog.md` [Unreleased] → Fixed

**Note on P6.16–P6.23 ID collision:** The Tier 6 bullet entries P6.16–P6.23 added in Phase 9 conflict with pre-existing detailed `### P6.16` through `### P6.21` subsections (now removed from this file along with all detailed sections). The Phase 9 entries are distinct new items but reuse the completed IDs. Documented here for a future renumbering pass.

### Tier 7 — Future / v2.0 (Not Yet Planned)

~~- **P7.1** [ ] Stress testing integration (M4.3)~~ not for this project
~~- **P7.2** [ ] Portfolio-level concentration metrics (M4.4)~~ not for this project
- **P7.3** [ ] REST API (M4.5)
- **P7.4** [partial] Additional exposure classes — phase 1 securitisation FLAG + EXCLUDE shipped (`SecuritisationAllocator` stage + aggregator residual multiplier + per-pool summary + per-exposure audit; CRR Art. 109 / 244-246; PRA PS1/26 Art. 147A(1)(j)). Spec at `docs/specifications/securitisation-pool-allocation.md`. **Still open**: SEC-SA / SEC-IRBA / SEC-ERBA RWA framework (CRR Art. 259-264), significant risk transfer assessment (Art. 244-246 conditions), tranche-level capital, shared-collateral CRM re-allocation; CIU beyond fallback, purchased receivables, dilution risk.
- **P7.5** [ ] **NEW** — Additional CRM methods not in scope: Art. 217 basket credit derivatives, Art. 214 counter-guarantees by sovereigns, Art. 215(2) mutual guarantee schemes, Art. 222(5) derivative cash collateral 0%, Art. 230(3) UK IRB 50% RW property option, Art. 197(4) unrated bond eligibility route. *(Art. 219 on-B/S netting removed from this list 2026-07-18 — implemented incl. maturity-mismatch gates by P1.238/P1.241.)*
- **P7.5** [ ] **NEW** — Art. 150(1A) materiality/immateriality thresholds for IRB firms using SA for immaterial classes. Currently only in COREP templates, not in the engine. **Effort: M**
- **P7.6** [ ] **NEW** — Art. 147B roll-out class tracking in the classification engine (currently only in COREP reporting). **Effort: M**
- **P7.8** [ ] **NEW** — CRR Art. 121(4) trade finance preferential RW for unrated institutions (CRR-only, sunsets 31 Dec 2026). Flat 50% (≤1y) / 20% (≤3m), independent of sovereign CQS ladder. CRR SA routes all unrated institutions through Art. 121(1); CQS 2-6 over-weighted. Add `is_trade_finance` Bool, `CRR_ART_121_4_*_RW` constants, gate in CRR institution branch. **Effort: S** | Ref: CRR Art. 121(4); 162(3); 4(1)(80)

### Tier 8 — Counterparty Credit Risk (CCR) Integration (NEW 2026-05-21; reordered 2026-05-22 for first-batch execution)

**Status reconciliation (2026-06-08 curator pass — verified against live code):** the **orchestrator-wiring trilogy** (P8.19 margined-RC wiring, P8.39 QCCP 2%/4% risk-weight, P8.53 WWR gate) and the **SA-CCR formula coverage** (P8.15 hedging sets, including the credit/equity/commodity add-on extension that P8.15's 2026-05-26 correction had deferred to never-written-out forward-references "P8.34–P8.37") are now **complete and pinned** — prior passes landed the work without ticking the plan. (The "P8.34–P8.37" IDs were forward-references inside the P8.15 correction text, never standalone bullets; the work they described is captured under the now-closed P8.15.) Each was re-verified this pass by reading the cited engine source and running the named acceptance/unit tests green (evidence noted on each bullet). The **genuinely-remaining** CCR engine gaps are, in dependency order: **P8.31** (CCR→IRB routing — unstarted; CCR rows route through SA only today), **P8.49** (default-fund Art. 308/309 — unstarted), **P8.32** CCR PD floor (blocks on P8.31), and the reporting (**P8.50–P8.52**), output-floor (**P8.55**), CVA (**P8.60–P8.63**), test/fixture (**P8.42–P8.48**), and docs (**P8.70–P8.74**) phases. Note: **P8.30** (route SA-CCR EAD through SA risk weights) is **effectively complete** — CCR rows resolve their counterparty class and risk weight end-to-end via `cp_entity_type` rather than an explicit `risk_type=CCR_DERIVATIVE` classifier branch (proven by CCR-A1 + CCR-CCP-1/2). **Addendum (2026-06-19):** a **fourth** instance of the same orchestrator-wiring pattern was surfaced and logged below as **P8.54** — the margined **maturity factor** (`compute_maturity_factor_margined`, implemented + unit-tested under P8.14) is never called; `pipeline_adapter.py:317` applies the unmargined MF to every trade including margined sets. It is the margined-MF wiring follow-up P8.19's body forward-references; SA-CCR **derivatives only** (SFTs route to FCCM and are out of scope — see P8.54's scope note). So the wiring set is now a **quartet** (P8.19 ✅, P8.39 ✅, P8.53 ✅, **P8.54 open**).

Major new epic adding Counterparty Credit Risk to the project. CCR is a separate-but-adjacent risk type to default credit risk: it produces the **EAD** for derivative and SFT (securities financing transaction) exposures, which then flows through the existing SA / IRB risk-weight machinery to produce the counterparty default-risk RWA. CVA (Credit Valuation Adjustment) capital is a related but separate framework and is scoped as a sub-epic (P8.60–P8.69).

**Pipeline shape:** the CCR stage sits between `Loader` and `Classifier` (or between `HierarchyResolver` and `Classifier`, TBD per P8.20). It consumes trade-level and netting-set-level inputs, produces exposure-level rows keyed by counterparty, and merges those rows into the existing `ExposureBundle` so downstream SA / IRB / Aggregator stages need minimal change.

**Execution ordering (2026-05-22 operator-approved):** the **first executable batch** (top of this tier) ships Phase 1 foundations + a single end-to-end vertical slice — **CCR-A1** (one unmargined IR swap → SA-CCR EAD → SA-RW → aggregator → pinned acceptance test). Source plan: `.claude/plans/can-we-start-to-linear-steele.md`. Within Tier 8 the first-batch items are listed at the top in dependency order; items marked **deferred to next batch after CCR-A1** are listed below them; items marked **deferred to v2.0** sit in Phase 10 alongside IMM.

**Scope decisions (operator-confirmed 2026-05-22):**
1. **Simplified SA-CCR (Art. 281) and OEM (Art. 282) — deferred to v2.0 / next-major-epic.** Not in initial epic. Sub-threshold portfolios fall through to full SA-CCR (always conservative). P8.21 and P8.22 reclassified accordingly (moved to Phase 10).
2. **CVA — BA-CVA only initially.** P8.60 (BA-CVA), P8.62 (hedges eligibility), P8.63 (aggregated CVA on bundle) stay in scope as the CVA sub-epic. **P8.61 (SA-CVA) deferred to v2.0** (moved to Phase 10).
3. **IMM — confirmed deferred to v2.0** (no change; P8.90 / P8.91 already there).
4. **SFTs — route through CCR with `risk_type=CCR_SFT`, reuse existing CRM haircut tables in `engine/crm/`.** No duplication of the supervisory haircut table. ~~The dual `risk_type` routing is already encompassed by existing tickets (no new P-code).~~ **Correction (2026-05-28):** the closing sentence proved wrong in practice. The current `engine/ccr/sa_ccr.py::compute_ead` runs the derivative `α·(RC+PFE)` formula on `transaction_type="sft"` rows regardless — only `engine/ccr/maturity_factor.py:121-127` branches on the SFT flag (5-BD margined MF floor). For a £60.7M debt-security SFT booked through the `trades` table this gives ~£4M EAD instead of the ~£65M `E·(1+HE)` the FCCM formula would produce. SFT-specific EAD wiring under SA-CCR per CRR Art. 271(2) is now tracked under the new **P8.38** below. The `CCR_SFT` placeholder comment at `src/rwa_calc/domain/enums.py:457` ("Placeholder pending SFT-specific EAD wiring") is the canonical in-code marker.

**Cross-cutting blockers:**
- Trade-level fixtures are a step-change from the current exposure-level parquet shape. `fixture-builder` agent needs new builders before any acceptance scenario can be pinned (P8.40).
- The `output_floor.py` numerator must include SA-CCR-derived EAD × SA-RW for IMM firms (P8.55).

#### First batch — Foundations + vertical slice CCR-A1 (operator-approved 2026-05-22)

The 16 items below are the first executable batch in dependency order. Source plan: `.claude/plans/can-we-start-to-linear-steele.md`. The batch is "done" when CCR-A1 (one unmargined IR swap → SA-CCR EAD → SA-RW → aggregator → pinned acceptance test) is green end-to-end. P8.20 is single-stream (touches `engine/pipeline.py`); the rest can parallelise up to N=3 under `/next-items`.

- ✅ **All 16 first-batch items complete** — P8.1–P8.7, P8.10, P8.12–P8.14, P8.17, P8.20, P8.40, P8.41 (CCR-A1) closed v0.2.11. See **Completed Items** at the foot of this file.
- **P8.48 (partial)** [ ] **NEW — first-batch (16/16) — partial scope: only the articles exercised in this batch** — `@cites` decorators for CRR Art. 271, 272, 274, 275(1), 279a, 279b, 279c(1), 280 on every new function in the formula path that this batch creates. Stack PS1/26 alongside where the article diverges. `uv run watchfire matrix` must produce a clean article→function index. **`watchfire`'s CRR index may need extending to cover Art. 271-280** — that's a sub-task of this ticket, not a reason to skip the citation. Full coverage of Art. 271-311 (including 277/277a/278/281-311) is **deferred** to the corresponding follow-up batches as those formulas land. **Effort: S** | Ref: `docs/development/citation-tracking.md`

#### Deferred to next batch after CCR-A1 (Phase 2 follow-up — SA-CCR formula coverage)

These items are in scope for this epic but NOT in the first executable batch. They land in the immediate follow-up batch(es) once CCR-A1 is green.

- **P8.53** [x] **FIXED v0.2.26 — `apply_wwr_gate` now wired into `pipeline.py::_run_ccr_stage` between the legal-enforceability gate and `ccr_rows_to_exposures`; specific-WWR trades break out into `<ns>__wwr__<trade>` synthetic NSes tagged `wwr_lgd_override=1.0` (Art. 291(5)(c)), the gate's CCR010/CCR011 diagnostics now reach `result.errors` as raw `CalculationError`s, and the override is surfaced on the synthetic exposure row in `pipeline_adapter.py`. Pinned by `tests/acceptance/ccr/test_ccr_wwr1_orchestrator_gate.py` (CCR-WWR-1). Downstream IRB LGD=100% *consumption* of the override (Change 2b) deferred — blocked on P8.31 CCR→IRB routing (CCR rows route through SA today). | NEW (doc-writer D2.78 SA-CCR batch surfacing 2026-05-28 — Tier 1 calculation correctness; renumbered from P8.38 on 2026-05-28 to resolve ID collision with the shipped SFT-FCCM P8.38)** — `apply_wwr_gate` implemented (P8.27, v0.2.11) but **not wired through the orchestrator**. `src/rwa_calc/engine/ccr/wwr.py::apply_wwr_gate` is fully implemented and unit-tested under `tests/unit/ccr/test_wwr.py` (13 tests covering specific-WWR trade break-out into `<ns_id>__wwr__<trade_id>` synthetic NSes + `wwr_lgd_override=1.0` per Art. 291(5)(c) + general-WWR diagnostic CCR011), but `grep -rn "apply_wwr_gate" src/` returns only the definition site at `wwr.py:87` and its docstring references at `wwr.py:5, 52, 61, 106` plus the public re-export in `engine/ccr/__init__.py`. The orchestrator at `src/rwa_calc/engine/pipeline.py:566` calls `apply_legal_enforceability_gate(data.ccr)` followed directly by `ccr_rows_to_exposures(raw_ccr_gated, ...)` at `:567` — `apply_wwr_gate` is **never invoked** (re-verified 2026-05-28). Consequence: no end-to-end SA-CCR run currently routes through the WWR LGD = 100% override per PRA Rulebook CCR (CRR) Part Art. 291(5)(c), so any trade flagged with `is_specific_wwr=True` is treated as a non-WWR trade for LGD purposes (LGD reverts to the standard F-IRB / A-IRB value instead of 100%), understating capital on the affected netting sets. Same pattern as P8.19 (margined-RC wiring gap) and P8.39 below (QCCP risk-weight wiring gap) — implementation slipped past the orchestrator integration step. Suggested wiring location: between `apply_legal_enforceability_gate` at `pipeline.py:566` and `ccr_rows_to_exposures` at `:567` — `raw_ccr_gated = apply_wwr_gate(apply_legal_enforceability_gate(data.ccr))`. Block on: nothing (apply_wwr_gate already returns a `RawCCRBundle`, drop-in compatible with the existing pipeline shape). Tests already exist; only the orchestrator call site needs adding plus an acceptance test exercising the override end-to-end (split a netting set with one specific-WWR trade + one non-WWR trade, assert the WWR trade lands in a `__wwr__` synthetic NS with `wwr_lgd_override=1.0` while the non-WWR trade stays in the parent NS — see P5.17). **Tier 1 candidate** (regulatory correctness — capital understatement on every netting set with a specific-WWR trade). Cross-ref: D2.78 docs spec (`docs/specifications/crr/ccr/wwr.md`) lands the regulatory citation and the engine status note. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 291(5)(c); CRR Art. 291(5)(c); `src/rwa_calc/engine/ccr/wwr.py:87`, `src/rwa_calc/engine/pipeline.py:566-573`, `tests/unit/ccr/test_wwr.py`
- **P8.19** [x] **FIXED v0.2.26 — margined RC now wired through the orchestrator. `pipeline_adapter.py::ccr_rows_to_exposures` (`:381-385`) calls `compute_rc_unmargined` then `compute_rc_margined` and `coalesce(rc_margined, rc_unmargined)` into a unified `rc` column; `compute_pfe` (`pfe.py:113-114`, `has_unified_rc` check) consumes that unified `rc` in the EAD `α·(rc + pfe_addon)` step, selecting the Art. 275(2) margined form `max(V−C, TH+MTA−NICA, 0)` on margined sets. Pinned end-to-end by `tests/acceptance/ccr/test_ccr_a13_margined_rc.py` (CCR-A13, `rc_margined == 2_250_000`) — re-ran green 2026-06-08 (6 passed). Verified against live code this pass.** ~~NEW (doc-writer SA-CCR batch surfacing — Tier 1 calculation correctness)~~ — Margined RC implemented (P8.11) but **not wired through the orchestrator**. `compute_rc_margined` at `src/rwa_calc/engine/ccr/rc.py:53-79` is fully implemented and unit-tested under `tests/unit/ccr/test_rc_margined.py`, but `src/rwa_calc/engine/ccr/pipeline_adapter.py::ccr_rows_to_exposures` (`:240` `ns_with_ead = compute_pfe(ns_frame, config_ccr)`) only calls `compute_pfe`, which internally inlines the *unmargined* RC at `engine/ccr/pfe.py:110`. The synthetic exposure row emitted at `pipeline_adapter.py:246-271` therefore carries only `rc_unmargined` and never an `rc_margined` column, so the COREP-reconciliation surface and any downstream consumer get the unmargined RC for **every** netting set — including margined ones where the threshold-floor branch `max(V−C, TH+MTA−NICA, 0)` should bind. New docs page `docs/specifications/crr/ccr/rc-calculation.md:370-371` already flags the gap explicitly: "compute_rc_margined ... is not yet routed through the orchestrator pending the margined-netting-set acceptance batch". Per Art. 275(2) the threshold floor can be materially positive for negative MTM with a low / zero collateral threshold, producing an RC understatement on every margined netting set that hits the floor. **Tier 1 candidate** (regulatory correctness — different formula per Art. 275(2), capital understatement on margined netting sets). Implementation: extend `pipeline_adapter.py` to call `compute_rc_margined` on the NS frame (joining MTA / TH / NICA from `raw_ccr.margin_agreements`), then `coalesce(rc_margined, rc_unmargined)` into a unified `rc` column that `compute_pfe` and the synthetic-row selection both consume; the lazy plan stays single-pass. Block on CCR margined-NS acceptance scenarios (pairs with P8.14 margined MF sub-piece — also a CCR-A* acceptance follow-up). **Effort: M** | Ref: PRA PS1/26 Art. 275(2); CRR Art. 275(2); Art. 285 (MPOR floors); `src/rwa_calc/engine/ccr/pipeline_adapter.py:240`, `src/rwa_calc/engine/ccr/rc.py:53-79`, `docs/specifications/crr/ccr/rc-calculation.md:366-395`
- **P8.54** [x] **FIXED (Unreleased — 2026-06-19): margined MF wired into `pipeline_adapter._derivative_rows_to_exposures` via the new `_attach_mpor_cascade_inputs` join + an internal `is_margined` gate on `compute_maturity_factor_margined` + a trade-grain `coalesce(maturity_factor_margined, maturity_factor_unmargined)` before the PFE add-on (mirrors the P8.19 RC coalesce); `mpor_days_input.fill_null(10)` null-safety (arch ratchet 441→442, justified load-bearing). CCR-A13 golden re-pinned to MF=0.30 (daily remargin) and a new CCR-A14 long-remargin acceptance scenario added (`remargining_frequency_days=126` → MPOR_eff=135 → MF≈1.10227, EAD £6.46M→£6.97M — the "EAD-rises" branch). Full acceptance suite green (1261 passed); only CCR-A13/A14 shifted (the only margined scenarios). SA-CCR derivatives only — SFTs route to FCCM, untouched; the all-SFT 5-BD MF branch flagged documented-but-inert.** ~~NEW (orchestrator-wiring gap surfaced 2026-06-19 — fourth instance of the P8.19 / P8.39 / P8.53 "implemented but never wired" pattern; the margined-MF wiring follow-up P8.19's body forward-references as "pairs with P8.14 margined MF sub-piece")~~ — Margined SA-CCR maturity factor implemented but **never wired through the orchestrator**. `compute_maturity_factor_margined` (`src/rwa_calc/engine/ccr/maturity_factor.py:88-174`) implements the Art. 279c(2) margined formula `MF = 1.5·√(MPOR_eff/250)` with the full Art. 285 MPOR cascade (base 5-BD repo / 10-BD OTC → 20-BD large-or-illiquid → ×2 dispute-doubling → `+ remargining_frequency_days − 1` → floored by `mpor_days`) and is unit-tested under `tests/unit/ccr/test_maturity_factor_margined.py` (P8.14), but has **zero production callers** (`grep -rn compute_maturity_factor_margined src/` → only the def + the `engine/ccr/__init__.py:40,66` re-export). The live derivative chain `_derivative_rows_to_exposures` calls `compute_maturity_factor_unmargined` at `pipeline_adapter.py:317` for **every** trade regardless of `is_margined` — so a margined netting set gets the longer-horizon unmargined `MF = √(min(M,1y)/1y)` (≤1.0) in its PFE add-on while only its RC reflects margining (P8.19), leaving the two halves of Art. 279c inconsistent. **Capital impact runs both directions:** for a long-remargin set (e.g. CSA remargin frequency 126d → `MPOR_eff = 10 + 126 − 1 = 135` → `MF = 1.5·√(135/250) ≈ 1.10 > 1.0`) the unmargined MF **understates** PFE→EAD (the motivating symptom: ~£60.7M vs expected ~£65.1M, ≈£4.4M low); for a normal daily-remargined set (`MPOR_eff = 10` → `MF = 0.30 < 1.0`) it **overstates** EAD — both corrected by wiring. **NOT pure wiring — needs a data join that does not exist yet.** `remargining_frequency_days` and `dispute_count_qtr` live only on `MARGIN_AGREEMENT_SCHEMA`, and `is_margined` / `number_of_trades` / `has_illiquid…` / `mpor_days` live on `NETTING_SET_SCHEMA`; none are joined onto `trades_enriched` before line 317. The denormalising trades × netting_sets × margin_agreements join (with the alias renames `has_illiquid` ← `has_illiquid_collateral_or_hard_to_replace_otc`, `mpor_days_input` ← `mpor_days`) currently exists only as the unit-test helper `_build_denormalised_lf` (`tests/unit/ccr/test_maturity_factor_margined.py:57-99`) and must be ported into the adapter. **Implementation (mirror P8.19 exactly):** port the join before the MF step; add an internal `pl.col("is_margined")` gate to `compute_maturity_factor_margined` so it emits null for unmargined rows (exactly like `compute_rc_margined`, `rc.py:69`); rename its output to `maturity_factor_margined` and `coalesce(maturity_factor_margined, maturity_factor_unmargined)` at **trade grain, before** the per-(NS, asset-class) add-on aggregation at `pipeline_adapter.py:321` (mirrors the `coalesce(rc_margined, rc_unmargined)` at `:437-441`); `is_margined.fill_null(False)` and `mpor_days_input.fill_null(10)` for backward-compat + null-safety (a null `mpor_days_input` would null the MF through `max_horizontal`); keep `remargining_frequency_days` in business-day semantics — do not conflate with the calendar-day `years_to_maturity` at `:292`. **Regression — re-pins the one margined golden.** `tests/expected_outputs/ccr/CCR-A13.json` is the only end-to-end margined-NS golden and is currently baked at the unmargined `MF=1.0` (its `addon_aggregate=3,914,298.23` is byte-identical to unmargined CCR-A1); A13 is daily-remargined, so wiring collapses its add-on to `MF=0.30` (~70%), shifting `pfe_multiplier` / `pfe_addon` / `ead_final` / `rwa_final` — **re-derive CCR-A13.json by hand-calc, do NOT bulk-regenerate** (migration do-not-do register). Add a **new long-remargin acceptance fixture** (the 126d repro, EAD rises) since every in-tree `remargining_frequency_days` is currently 1 — the "EAD-rises" branch is untested today. **Scope — SA-CCR derivatives only.** SFTs are split out on `transaction_type=="sft"` at `pipeline_adapter.py:174` and routed to FCCM (`sft_fccm.py`, Art. 271(2)); they never reach the maturity factor, so this item does **not** touch SFT/repo remargining (FCCM-SFT remargining = the separate Art. 223/224/226 haircut-scaling path — correct + acceptance-tested on the general-CRM `crm/haircuts.py:188-200` route, deliberately deferred-and-currently-unreachable on `sft_fccm.py`). Consequently the all-SFT 5-BD branch inside `compute_maturity_factor_margined` (`maturity_factor.py:129-138`) is **documented-but-inert** — structurally dead even post-fix because `_derivative_rows_to_exposures` only ever receives the derivative sub-bundle (`all_sft_in_ns` is always False), same inert-flag category as P8.23 `is_long_settlement`; flag it as such in the docstring. **Tier 1 candidate** (regulatory correctness — capital understatement on long-remargin margined netting sets; over-statement on daily-remargin sets). `@cites` not added (CRR Art. 285 absent from watchfire's bundled index — keep attribution in docstrings, consistent with `rc.py` / `pfe.py` / `sa_ccr.py`). Block on: nothing (the compute fn + its unit tests already exist; only the adapter join + coalesce + the CCR-A13 hand-calc re-pin + a long-remargin fixture remain). **Effort: M** | Ref: CRR Art. 279c(2), Art. 285; PRA PS1/26 CCR (CRR) Part Art. 279c; `src/rwa_calc/engine/ccr/maturity_factor.py:88-174`, `src/rwa_calc/engine/ccr/pipeline_adapter.py:317` / `:321` / `:437-441`, `tests/unit/ccr/test_maturity_factor_margined.py:57-99`, `tests/expected_outputs/ccr/CCR-A13.json`, `tests/acceptance/ccr/test_ccr_a13_margined_rc.py`
- **P8.15** [x] **FIXED (verified complete 2026-06-08, no commit — prior-pass) — the credit / equity / commodity extension that was deferred to P8.34–P8.37 has now landed, so the full hedging-set partition and per-asset-class add-on aggregation are complete. `assign_hedging_set` (`engine/ccr/hedging_sets.py:176-188`) now dispatches all five asset classes — IR (maturity-bucket keyed), FX (currency-pair keyed), credit (`CR-<ns>`), equity (`EQ-<ns>`), commodity (`CO-<ns>-<commodity_type>`) — not just IR/FX; `compute_addon_per_asset_class` (`engine/ccr/pfe.py:177-181`) dispatches all five `_compute_addon_{ir,fx,credit,equity,commodity}`. Pinned by `tests/unit/ccr/test_pfe_{credit,equity,commodity}_addon.py` + acceptance CCR-A3..A10 — re-ran green 2026-06-08 (90 passed). Supersedes the 2026-05-26 "IR+FX only" correction below — the "P8.34/P8.35/P8.36/P8.37" forward-references in that correction were never written as standalone bullets; their scope (credit/equity/commodity hedging sets + add-ons) is fully delivered and is closed here under P8.15.** ~~PARTIAL — closing claim overstated; IR + FX shipped only (batch 20260523-1500); credit / equity / commodity remain open under P8.34~~ — SA-CCR Hedging sets and asset-class add-on aggregation. `engine/ccr/hedging_sets.py` + `engine/ccr/pfe.py::compute_addon_per_asset_class`. **Correction (2026-05-26 audit, similar to P8.20's "Option B placement" and P8.27's "plan bullet was wrong on three counts" self-corrections):** the original closing summary stated the hedging-set partition covered "credit by reference entity; equity by issuer; commodity by sub-type" — that was incorrect. `assign_hedging_set` at `engine/ccr/hedging_sets.py:140-147` only emits a non-null `hedging_set_id` for `asset_class in {"interest_rate", "fx"}`; all other asset classes fall through to `pl.lit(None, dtype=pl.Utf8)`. `compute_addon_per_asset_class` at `engine/ccr/pfe.py:127-170` correspondingly only dispatches `_compute_addon_ir` and `_compute_addon_fx`; credit / equity / commodity rows return null `asset_class_addon` by design. The shipped IR three-bucket aggregation per Art. 277a(1)(a) and FX per-currency-pair aggregation per Art. 277(3)(a) + CRE52.55 are both correct and pinned. **Remaining work** for credit (single-name + index hedging sets per Art. 277(2)(c) + Art. 277a / CRE52.60-64), equity (single-name + index hedging sets per Art. 277(2)(d) + CRE52.65-66), and commodity (5-bucket partition per Art. 277(3)(b) — electricity / oil_gas / metals / agriculture / other — per CRE52.67-69) is now tracked under the new **P8.34** below (hedging-set extension) and the per-asset-class adjusted-notional + PFE add-on items **P8.35** (credit), **P8.36** (equity), **P8.37** (commodity). Aggregation formulas per Art. 277a using supervisory correlations from P8.7. **Effort: L (originally); residual under P8.34/35/36/37** | Ref: CRR Art. 277, 277a, 280; BCBS CRE52.60-72

#### Phase 3 — Alternative methods, CCP, and edge cases (in scope of epic; later batches)

- **P8.23** [x] **CLOSED v0.2.26 — DOCUMENTED NO-OP (premise was false). Primary-source CRR verified via legislation.gov.uk (orchestrator, 2026-06-09): the plan's "bespoke MPOR floor" for long-settlement transactions does NOT exist under SA-CCR. Art. 271 — long-settlement transactions *may* be calculated under Chapter 6 (SA-CCR) instead of Chapter 4 (no special treatment). Art. 272(2)/(9) — definitions only. Art. 285 — does NOT mention long-settlement transactions; the MPOR floors (5/10/20 BD) key off the netting set's margining type, not the `is_long_settlement` flag. Under SA-CCR a long-settlement trade is an ordinary trade whose maturity factor is set by Art. 279c(1) (unmargined) / 279c(2) (margined) exactly like any other trade — so the `is_long_settlement` flag is INERT (no MF/EAD consequence). The plan's framing conflated SA-CCR with the IMM / pre-SA-CCR treatment. **Resolution (operator-approved):** no engine change; the schema flag stays as a marker. Pinned by `tests/acceptance/ccr/test_ccr_ls_long_settlement_inert.py` (4 tests, green-on-arrival regression guard: a long-settlement trade routes through SA-CCR and produces EAD/RWA identical to an economically-identical non-long-settlement control — would go RED if a spurious `is_long_settlement` MF branch were ever introduced). Fixture `tests/fixtures/ccr/p823_ls_builder.py::build_p823_bundle(is_long_settlement)`. No `src/` change. Follow-up: a one-line `docs/specifications/crr/ccr/maturity-factor.md` note (Art. 271 permits SA-CCR; no LS-specific MPOR floor) is a small `doc-writer` item for `DOCS_IMPLEMENTATION_PLAN.md`. | ~~NEW; starting-point refreshed 2026-06-08 — SCHEMA STUB ONLY~~** Long-settlement transactions Art. 271(2). **Effort: S** | Ref: CRR Art. 271, Art. 272(2)/(9), Art. 285 (verified — no LS-specific MPOR floor); `tests/acceptance/ccr/test_ccr_ls_long_settlement_inert.py`
- **P8.39** [x] **FIXED v0.2.26 — QCCP 2%/4% trade-exposure pin now correct end-to-end through the orchestrator. Implementation corrected the plan's premise: a CCP risk-weight branch already existed in the SA calculator (`namespace.py` `cp_entity_type=="ccp"` → 0.02/0.04), so QCCP-proprietary already resolved to 2% (not the 50% the plan assumed) — but two real bugs remained: (a) the trade-level `is_client_cleared` flag never reached the synthetic CCR row, so client-cleared QCCP trades wrongly got 2% instead of 4%; (b) the branch keyed on `entity_type=="ccp"` without a QCCP gate, so a non-QCCP CCP wrongly got 2% instead of the institution ladder. Fix (Path B — single live mechanism, no duplication): thread `is_client_cleared` NS-collapsed via `any()` → `cp_is_ccp_client_cleared` (`pipeline_adapter.py`), surface `is_qccp` → `cp_is_qccp` (`classifier.py` + new `cp_is_qccp` ColumnSpec in `schemas.py` HIERARCHY_OUTPUT_SCHEMA), gate both `namespace.py` CCP branches on `cp_is_qccp.fill_null(True)` (absent flag = qualifying, preserving legacy rows), and extend the MDB-style `cqs` coalesce so a demoted non-QCCP CCP lifts `cp_institution_cqs` into `cqs` and resolves to the Art. 120 Table 3 institution ladder (CQS-2 → 50%) per Art. 107(2)(a) rather than the unrated-100% fallback. Result: proprietary QCCP → 2% (Art. 306(1)(a)), client-cleared → 4% (Art. 306(1)(c)), non-QCCP CCP → institution ladder. Pinned by `tests/acceptance/ccr/test_ccr_ccp_orchestrator_pin.py` (CCR-CCP-1/2 + a 2-counterparty keyed-join fan-out guard; 12 tests). `apply_ccp_risk_weight` (`engine/ccr/ccp.py`) deliberately left **unwired** — the SA-calculator branch is the single source of truth; the free function remains library-only (its 8 unit tests unchanged). Unblocks P5.17 (CCR-CCP-1/2). Follow-up debt: the unwired `apply_ccp_risk_weight` + its unit tests are now redundant and a curator pass should decide remove-vs-adopt. | NEW (doc-writer D2.79 SA-CCR batch surfacing 2026-05-28 — Tier 1 calculation correctness)** — `apply_ccp_risk_weight` implemented (P8.25, v0.2.11) but **not wired through the orchestrator**. `src/rwa_calc/engine/ccr/ccp.py::apply_ccp_risk_weight` is fully implemented and unit-tested under `tests/unit/ccr/test_ccp.py` (8 tests covering QCCP proprietary 2% / client-cleared 4% / non-QCCP pass-through across CCR-B1a/b/c, plus EAD-invariance assertions), but `grep -rn "from rwa_calc.engine.ccr.ccp"` returns only test imports (no consumer in `src/`). The orchestrator at `src/rwa_calc/engine/pipeline.py:561-573` calls `apply_legal_enforceability_gate` then `ccr_rows_to_exposures` and exits the CCR stage — `apply_ccp_risk_weight` is **never invoked**. Consequence: every QCCP trade is currently routed through the standard SA institution risk-weight ladder (CRR Art. 120 Table 3 by CQS) instead of the regulatory 2% / 4% pin per PRA Rulebook CCR (CRR) Part Art. 306(1). For a typical CQS-2 institution counterparty the standard SA RW is 50%, so a QCCP-proprietary trade gets 50% instead of 2% and a client-cleared trade gets 50% instead of 4%: a ~25x over-statement of QCCP RWA and ~12.5x for client-cleared. **Conservative mis-pricing (over-states capital) but still regulatorily wrong** — firms over-allocate capital to QCCP exposures. Same pattern as P8.19 (margined-RC wiring) and P8.53 (WWR-gate wiring) — implementation slipped past the orchestrator integration step. Suggested wiring location: after `ccr_rows_to_exposures` returns the synthetic exposure rows in `pipeline.py:582-584`, call `apply_ccp_risk_weight(ccr_exposure_rows, resolved.counterparty_lookup.counterparties, data.ccr.trades)` to overlay the regulatory 2%/4% on QCCP rows (non-QCCP rows get NULL `risk_weight` which the classifier then resolves via the standard SA ladder — the function is already designed for this pass-through pattern). Production wiring needs keyed joins on `counterparty_reference` / `trade_id` (the existing test fixture uses cross-join because it's keyless). Block on: nothing in principle, but pairs with P8.30 (general SA routing for `risk_type=CCR_DERIVATIVE`) — the QCCP pin must dispatch BEFORE the standard SA classifier so the 2%/4% takes precedence. Tests already exist; only the orchestrator call site needs adding plus an acceptance test exercising the override end-to-end (direct-cleared and client-cleared QCCP scenarios under both CRR and B3.1). **Tier 1 candidate** (regulatory correctness — material RWA over-statement on every QCCP exposure). Cross-ref: D2.79 docs spec (`docs/specifications/crr/ccr/ccp-exposures.md`) lands the regulatory citation and the engine status note. **Effort: S** | Ref: PRA Rulebook CCR (CRR) Part Art. 306(1); CRR Art. 306(1); `src/rwa_calc/engine/ccr/ccp.py:48`, `src/rwa_calc/engine/pipeline.py:561-584`, `tests/unit/ccr/test_ccp.py`
- **P8.49** [x] **FIXED v0.3.3 (batch 20260619-0936)** — default-fund-contribution capital stack (CRR Art. 308/309) shipped end-to-end. New `engine/ccr/default_fund.py::compute_dfc_capital` computes `K_CM = K_CCP·(DF_i/DF_CM)` (Art. 308(2) clearing-member allocation) and `RWEA = K_CM × 12.5`, reading the 12.5 own-funds→RWA factor from the rulepack pack `own_funds_to_rwa_factor` (CRR Art. 92(3)(ca)); covers pre-funded QCCP (Art. 308(3)) and non-QCCP / unfunded (Art. 309(2)) via a three-way `regulatory_band`. New `default_fund_contributions: pl.LazyFrame | None` field on `RawCCRBundle` + `DF_CONTRIBUTION_SCHEMA` (7 cols); synthetic SA exposure rows (`exposure_reference='dfc__'+id`, `risk_type='CCR_DEFAULT_FUND'`, `drawn_amount=k_cm`, RW pinned 12.5 on both `engine/sa/risk_weights.py` override chains) threaded via `engine/stages/ccr.py` (mirrors the P8.43 failed-trade pattern); new `rwa_ccr_default_fund` roll-up on `AggregatedResultBundle`; `regulatory_band` audit column carried to the aggregated frame via `CCR_EXIT_EDGE` + `_calc_output_common_columns`. **PREMISE CORRECTION:** the bullet's `data/tables/default_fund.py` instruction is STALE — the `data/tables/` package was removed (Phase 5 S13) and is hard-banned by arch_check check 12 (no allowlist); the 12.5 is read from the rulepack pack instead. No `@cites` (Art. 308/309 outside watchfire's bundled index → docstring attribution). `arch_metrics.json` baselines bumped (`engine_fill_null_sites` 441→442, `max_engine_module_loc` 1526→1531). Pinned by `tests/acceptance/ccr/test_ccr_b2_b4_default_fund.py` (CCR-B2 QCCP 12,500,000 / CCR-B3 non-QCCP 9,375,000 / CCR-B4 unfunded 5,000,000; portfolio delta 26,875,000; 9 tests). | ~~NEW (doc-writer D2.79 SA-CCR batch surfacing 2026-05-28 — Phase 3 CCP; SUPERSEDES P8.26 with concrete scope; premise re-verified accurate 2026-06-08 — genuinely UNSTARTED)~~ — Art. 308 / 309 default-fund-contribution stack not implemented in any form. **Re-verified 2026-06-08:** `grep -rn "default_fund\|K_CCP\|K_CM\|dfc_capital" src/` returns zero hits. **Engine gap survey**: no `K_CCP` / `K_CM` allocation logic anywhere in `src/rwa_calc/engine/ccr/`. The single module `ccp.py` covers Art. 306 trade-exposure RW only (the new `apply_ccp_risk_weight` shipped under P8.25 in v0.2.11); there is no companion module for the default-fund-contribution treatment. `grep -rn "default_fund\|K_CCP\|K_CM\|dfc_capital" src/` returns zero hits. No `default_fund_contributions: pl.LazyFrame | None` field on `RawCCRBundle` (`contracts/bundles.py`); no `DF_CONTRIBUTION_SCHEMA` in `data/schemas.py`; no `compute_dfc_capital` referenced by P8.26's bullet text. Firms with pre-funded default-fund contributions cannot compute that RWA in-system today — the calculator silently omits the contribution from the RWA total. **Required deliverable**: new module `src/rwa_calc/engine/ccr/default_fund.py` implementing (a) the **K_CCP hypothetical-capital alternative formula** per PRA Rulebook CCR (CRR) Part Art. 308(2) (or institution-published K_CCP input — CCP loss-mutualisation simulation is out-of-scope per `IMPLEMENTATION_PLAN.md:394` explicit non-goal), (b) the **K_CM clearing-member allocation** `K_CCP × DF_i / DF_CM`, (c) the **12.5× multiplier** per Art. 308(3) for pre-funded QCCP and Art. 309(2) for non-QCCP (`RWEA = K × 12.5`). New schema additions to `RawCCRBundle`: `default_fund_contributions: pl.LazyFrame | None` (columns: `contribution_id`, `ccp_reference`, `is_qccp_ccp`, `df_i_contribution_amount`, `df_cm_total_contributions`, `k_ccp_published`, optional `is_unfunded_commitment` per Art. 309 unfunded-contributions branch). New `data/tables/default_fund.py` for the `DF_RWA_MULTIPLIER = Decimal("12.5")` constant (matches the existing `engine/ccr/failed_trades.py` 12.5 own-funds-to-RWA factor precedent — keep in data layer per arch_check checks 5/6). Aggregator extension: route the new `rwa_default_fund` column into `AggregatedResultBundle.rwa_ccr_default_fund` (already enumerated in P8.52). Pinned by CCR-B2..B4 acceptance scenarios (direct-cleared QCCP DF contribution + non-QCCP DF contribution + unfunded non-QCCP contribution under Art. 309). Cross-ref: D2.79 docs spec already flags this as pending and forward-cites the new module path. **Note on Art. 307**: the user-supplied finding text referenced "Art. 307 / 308(3) / 309(2)" — PS1/26 Annex R Section 9 confirms the operative articles are **308** (pre-funded QCCP) and **309** (non-QCCP / unfunded), with Art. 307 covering the trade-exposure value definition (not default-fund allocation). The user's K_CCP / K_CM formula maps to Art. 308(2)-(3) and the 12.5× multiplier appears in both Art. 308(3) and Art. 309(2) per `docs/assets/ps126app1.pdf` page 457. **Effort: M** (new module + schema columns + data table + 3 acceptance scenarios + aggregator wiring) | Ref: PRA Rulebook CCR (CRR) Part Art. 308(2)/(3), Art. 309(1)/(2); CRR Art. 308, 309; `docs/assets/ps126app1.pdf` page 457; `src/rwa_calc/engine/ccr/ccp.py`, `src/rwa_calc/contracts/bundles.py`, `src/rwa_calc/data/schemas.py`
- **P8.26** [ ] **SUPERSEDED BY P8.49 (2026-05-28)** — Default fund contributions to QCCP. `engine/ccr/ccp.py::compute_dfc_capital`. Per Art. 308 alternative formula `K_CCP × DF_i / DF_CM`. Non-QCCP default fund contributions: 1,250% RW (Art. 309). P8.49 below carries the load-bearing description (new `default_fund.py` module, schema columns, data-table constants, acceptance scenarios CCR-B2..B4). Note: the original P8.26 reference to "1,250% RW (Art. 309)" was a simplification — Art. 309(2) actually applies the 12.5× multiplier to the K capital requirement (same mechanism as Art. 308(3) for QCCPs), not a flat 1,250% RW on the contribution amount. **Effort: M** | Ref: CRR Art. 308, 309 — see P8.49
- **P8.28** [x] **FIXED v0.2.26 — per-counterparty α=1.0 carve-out now wired end-to-end (CRR Art. 274(2) second sub-paragraph). New `counterparty_type` column on `COUNTERPARTY_SCHEMA` (default `"financial"` → α=1.4; `non_financial`/`pension_scheme`/`pension_default_comp` → α=1.0) with `VALID_CCR_COUNTERPARTY_TYPES` + carve-out subset `CCR_ALPHA_CARVE_OUT_COUNTERPARTY_TYPES` (engine membership-tests the subset, keeping category strings out of engine scope) registered in `COLUMN_VALUE_CONSTRAINTS`. α scalars `SA_CCR_ALPHA=1.4` / `SA_CCR_ALPHA_CARVE_OUT=1.0` added to `data/tables/sa_ccr_factors.py` (regulatory scalars in the data layer per arch_check 5/6). `pipeline_adapter._attach_alpha_applied` keyed-joins `counterparty_type` on `counterparty_reference` (left join + `unique(subset)`, no fan-out) and reduces to a per-NS `alpha_applied` scalar (surfaced on the synthetic row for COREP audit); `compute_pfe` / `compute_ead` read the per-row `alpha_applied` when present, else fall back to the scalar `config.alpha`/1.4. `pipeline._run_ccr_stage` threads `data.counterparties` through. Backward-compatible via three defensive layers (counterparties None / column absent / null → α=1.4) — all 283 existing CCR tests stay α=1.4 green. Pinned by `tests/acceptance/ccr/test_ccr_alpha_carveout.py` (CCR-ALPHA-1/2/3 + 2-CP keyed-join guard; 14 tests). **Premise corrections:** the original "~28% understatement" was the wrong direction — the engine OVER-states EAD by ×1.4 for in-scope counterparties (correct is 28.6% LOWER; conservative error). The spec's `499,829`/`699,760` figures are an obsolete 1-year example; the live CCR-A1 anchors are `pfe_addon=3,914,298.228`, `ead(α=1.4)=5,480,017.519`. `@cites` not added (CRR Art. 274 absent from watchfire's bundled index — would be a fatal gate failure; attribution in docstrings/comments, consistent with existing `sa_ccr.py`/`pfe.py`). Unblocks P8.29 (transitional add-on defined in terms of the EAD(α=1) branch). | NEW (doc-writer SA-CCR batch surfacing — Tier 1 calculation correctness; premise re-verified accurate + line refs refreshed 2026-06-08 — genuinely UNSTARTED)** — Art. 274(2) α = 1 carve-out **not engine-wired**. **Verified 2026-06-08:** a single uniform `alpha_value = float(config.alpha) if config is not None else 1.4` is read at `src/rwa_calc/engine/ccr/sa_ccr.py:77` (applied at `:79`) and at `src/rwa_calc/engine/ccr/pfe.py:95` (applied at `:123`); `grep counterparty_type src/rwa_calc/` returns zero hits — there is no per-counterparty alpha dispatch anywhere. `src/rwa_calc/engine/ccr/sa_ccr.py::compute_ead` and `src/rwa_calc/engine/ccr/pfe.py::compute_pfe` both apply the same scalar uniformly to every netting set. PRA PS1/26 Art. 274(2) (and CRR Art. 274(2)) carves out **α = 1** for trades whose counterparty is (i) a **non-financial counterparty** as defined in EMIR Art. 2(9), (ii) a **pension scheme arrangement** as defined in EMIR Art. 2(10), or (iii) a **pension-default compensation entity**. The per-counterparty dispatch is entirely absent: `RawCCRBundle` (`contracts/bundles.py`) has no `counterparty_type` field, `CCRConfig` (`contracts/config.py`) has no per-counterparty alpha override, and the pipeline adapter passes a single scalar `config.alpha` regardless. Capital impact: ~28% understatement of EAD on any in-scope counterparty whose true α should be 1.0 rather than 1.4 (`(1.4 − 1.0) / 1.4 ≈ 28.6%`). The new docs page `docs/specifications/crr/ccr/ead-composition.md:74-93, 310-323, 332-341` already documents the gap with a side-by-side α=1 vs α=1.4 worked example for CCR-A1 (`499,829` vs `699,760`). Implementation: extend `NETTING_SET_SCHEMA` or `COUNTERPARTY_SCHEMA` with a `counterparty_type: pl.String` (or equivalent enum) sourced from EMIR Art. 2(9)/(10) classifications; join into the NS frame in `pipeline_adapter.py` between the v_net/c_net aggregation and `compute_pfe`; replace the scalar `alpha_value` with a per-row `pl.when(counterparty_type.is_in(NON_FIN_OR_PENSION)).then(1.0).otherwise(1.4)` expression; emit a per-NS `alpha_applied` column for COREP reconciliation. **Tier 1 candidate** (regulatory correctness — material EAD understatement on in-scope counterparty types). **Effort: M** (schema extension + dispatch + acceptance scenarios for each carve-out type) | Ref: PRA PS1/26 Art. 274(2); CRR Art. 274(2); EMIR Art. 2(9)-(10); `docs/assets/ps126app1.pdf` page 456; `src/rwa_calc/engine/ccr/sa_ccr.py:77-80`, `src/rwa_calc/engine/ccr/pfe.py:94-115`, `src/rwa_calc/engine/ccr/pipeline_adapter.py:240`
- **P8.29** [x] **FIXED v0.2.26 — transitional alpha add-on wired end-to-end (PRA PS1/26 Art. 274(2A)-(2B)). Primary source VERIFIED via `docs/assets/ps126app1.pdf` p.456 (orchestrator, 2026-06-09): the "alpha add-on" is defined as EAD(NS,α=1.4 as at 1 Jan 2027) − EAD(NS,α=1) = 0.4·(RC+PFE); Art. 274(2A) adds 60% (2027) / 40% (2028) / 20% (2029) / 0% (2030+) of it to the NS exposure value for trades entered prior to 1 Jan 2027 with a CVA Risk Part 7.1(1)(a)/(b) counterparty; Art. 274(2B) excludes it from the leverage-ratio EAD. Implementation (builds on P8.28): new `is_legacy_cva_exempt` flag on `TRADE_SCHEMA` (firm-supplied conjunction: pre-2027 entry AND CVA-exempt counterparty); new `SA_CCR_TRANSITIONAL_ADDON_PHASE = {2027:0.60, 2028:0.40, 2029:0.20}` in `data/tables/sa_ccr_factors.py`; `pipeline_adapter._attach_transitional_add_on` collapses the flag to per-NS via `any()`, computes `add_on = phase·0.4·(rc+pfe)` gated on `is_legacy_cva_exempt AND alpha_applied==1.0` (coherence — an α=1.4 financial CP gets nothing) AND Basel-3.1 AND reporting-year∈{2027,2028,2029}, folds it into `ead_ccr`, and surfaces a `transitional_add_on` audit column; `pipeline._run_ccr_stage` threads `is_basel_3_1=config.is_basel_3_1`. Phase resolved at build time → under CRR / 2030+ the add-on is a constant 0.0 and `ead_ccr` is byte-identical to pre-P8.29 (all 307 CCR tests stay green). Pinned by `tests/acceptance/ccr/test_ccr_alpha_addon_transitional.py` (CCR-ALPHA-ADDON-1 four-year phasing 1.24×/1.16×/1.08×/1.00× + non-legacy / legacy-financial / CRR-framework controls + 2-NS fan-out guard; 20 tests, expected EAD derived dynamically from the non-legacy baseline since Basel-3.1 PFE differs from CRR). **Scope notes:** Art. 274(2B) leverage exclusion is **moot** (no leverage-ratio EAD path exists); the "as at 1 Jan 2027" snapshot uses the current-reporting-date proxy (exact for static economics — a frozen-snapshot input is a documented follow-up). `@cites` not added (Art. 274 absent from watchfire index). `TRADE_SCHEMA` contract count bumped 29→30. | NEW (doc-writer SA-CCR batch surfacing — transitional regulatory feature)** — Art. 274(2A)–(2B) **transitional alpha add-on not implemented**. PRA PS1/26 Art. 274(2A) defines a phased add-on `transitional_add_on = phase_factor × [EAD(α=1.4) − EAD(α=1)]` for **legacy CVA-exempt trades** (trades entered into prior to 1 January 2027), phased in at **60% (2027) → 40% (2028) → 20% (2029) → 0% (2030+)**; Art. 274(2B) excludes this add-on from the leverage-ratio EAD numerator. Nothing in `src/rwa_calc/engine/ccr/` implements this transitional: no `is_legacy_cva_exempt: pl.Boolean` flag on `TRADE_SCHEMA`, no `trade_inception_date`-vs-2027-cutoff gate, no `transitional_add_on` column on the synthetic exposure row, no leverage-ratio-EAD vs default-EAD bifurcation. Documented forward-visibility in the new docs page `docs/specifications/crr/ccr/ead-composition.md:83-94, 338-345, 352-355`: "the engine implements only the steady-state EAD per Art. 274(2); the transitional add-on is a documented gap and will be re-routed once Art. 274(2A) is wired". Should be its own engine batch and a follow-on docs page `docs/specifications/crr/ccr/alpha-add-on-transitional.md` (recommended cross-file move to `DOCS_IMPLEMENTATION_PLAN.md` once this lands). Block on P8.28 (Art. 274(2) carve-out gate — the add-on is defined in terms of the carve-out's `EAD(α=1)` branch). **Tier 8 candidate** (CCR transitional regulatory feature; date-gated 2027-01-01 to 2030-01-01). **Effort: M** (per-trade legacy-vs-new flag + per-year phase-factor table + leverage-ratio bifurcation + acceptance scenarios across the four years) | Ref: PRA PS1/26 Art. 274(2A)–(2B); `docs/assets/ps126app1.pdf` page 456; `src/rwa_calc/engine/ccr/sa_ccr.py`, `src/rwa_calc/data/schemas.py` `TRADE_SCHEMA`

#### Phase 4 — Counterparty default-risk RWA routing (SA + IRB)

- **P8.30** [x] **FIXED (verified complete 2026-06-08, no commit — prior-pass; premise re-scoped) — SA-CCR EAD now routes through SA risk weights end-to-end, but via a different mechanism than the original bullet assumed. The bullet expected `engine/classifier.py` to learn a literal `risk_type=CCR_DERIVATIVE` branch; in the as-built design there is no `CCR_DERIVATIVE` branch in `classifier.py` (`grep CCR_DERIVATIVE src/rwa_calc/engine/classifier.py` = 0 hits). Instead `pipeline_adapter.py::ccr_rows_to_exposures` emits synthetic exposure rows carrying `drawn_amount = ead_ccr` (so CRM `_initialize_ead` yields `ead_pre_crm = ead_ccr` with no CCF/collateral/guarantee), and the classifier resolves the counterparty class + risk weight from `cp_entity_type` / `cp_institution_cqs` / `cp_is_qccp` (institution ladder, QCCP 2%/4%, etc.) exactly as for any other SA exposure. Proven end-to-end by CCR-A1 (`tests/acceptance/ccr/test_ccr_a1_unmargined_ir_swap.py`) and CCR-CCP-1/2 (`tests/acceptance/ccr/test_ccr_ccp_orchestrator_pin.py`) — re-ran green 2026-06-08 (17 passed). The intent (CCR EAD → SA-RW machinery, no `engine/sa/` changes) is satisfied; the `risk_type=CCR_DERIVATIVE` literal is carried on the synthetic row for reporting (`pipeline_adapter.py:406`) but is not a classifier dispatch key.** ~~NEW — Route SA-CCR EAD through SA risk weights. `engine/classifier.py` learns `risk_type=CCR_DERIVATIVE` and looks up counterparty class (Institution, Corporate, Sovereign, Retail-derivative). EAD from P8.17 replaces drawn-amount / CCF flow. No changes needed in `engine/sa/`.~~ **Effort: S**
- **P8.31** [x] **FIXED v0.2.26 — SA-CCR derivative exposures now route through IRB (F-IRB / A-IRB) for IRB-permissioned counterparties (scenario CCR-IRB-1).** Root cause: synthetic CCR rows reach `engine/classifier.py` with `model_id = null` (the rating-inheritance attach that renames `internal_model_id → model_id` runs only over hierarchy-resolved lending rows, before CCR rows are appended), so `_resolve_model_permissions` hit its `null_model_id` diagnostic and fell back to SA even for an IRB-permissioned counterparty. Fix (classifier-only, no shared-file edit): `_add_counterparty_attributes` surfaces the counterparty's resolved `internal_model_id` as `cp_internal_model_id`, and `_resolve_model_permissions` coalesces it into `model_id` — so the CCR derivative exposure resolves its permission and routes through the existing PD/LGD/M machinery using the SA-CCR EAD `α·(RC+PFE)` as the IRB EAD input (`ead_final == ead_ccr`). The architect's proposed `pipeline_adapter.py` adapter-local join was infeasible (the raw `data.counterparties` frame carries neither `internal_model_id` nor `internal_pd`; only the resolved `counterparty_lookup` does, and that is reachable in `classifier.py` but not the adapter without touching the hard-excluded `pipeline.py`). **Plan-premise correction:** the derivative effective maturity is **CRR Art. 162(2)(b)** (derivatives under a master netting agreement: residual maturity, 1y floor / 5y cap) — NOT the bullet's "Art. 162(2)(g)-(i) weighted MTM-positive cash flows", which are the IMM / CVA-internal-model maturity paths (out of scope for an SA-CCR firm; IMM=P8.90, SA-CVA=P8.61). For a single-trade NS the existing `maturity_date`-clipped derivation already yields the Art. 162(2)(b) result, so no new maturity formula was needed. Coalesce is a strict no-op for lending rows with a populated `model_id` (verified across 1,600+ tests; CCR-A1 still SA, QCCP 2%/4% + α-carve-out pins intact). Pinned by `tests/acceptance/ccr/test_ccr_irb1_routing.py` (6 tests; `approach_applied="foundation_irb"`, F-IRB RW ≈ 145.9%) + golden `tests/expected_outputs/ccr/CCR-IRB-1.json`. Multi-trade weighted-average `M`, WWR-LGD=100% *consumption* (P8.53 Change 2b) and the 0.05% CCR PD floor (P8.32 — still blocked on this item but the routing path now exists) remain deferred/un-asserted. | ~~NEW; premise re-verified accurate 2026-06-08 — genuinely UNSTARTED.~~ Route SA-CCR EAD through IRB (F-IRB / A-IRB). **Effort: M** | Ref: CRR Art. 153(1), Art. 162(2)(b), Art. 161(1)(a), Art. 163; `src/rwa_calc/engine/classifier.py`, `tests/acceptance/ccr/test_ccr_irb1_routing.py`
- **P8.32** [x] **CLOSED v0.3.2 (batch 20260619-0822) — ALREADY IMPLEMENTED; premise + citation corrected, no code change.** Scenario-architect + orchestrator verification found NO separate FSE-specific IRB PD floor exists in CRR/PS1/26: the B3.1 corporate/sovereign/institution IRB PD floor is a flat **0.05%** and is **already wired** in the b31 rulepack (`rulebook/packs/b31.py:118-121 = Decimal("0.0005")`; CRR baseline `crr.py:115-118 = Decimal("0.0003")`), read by `engine/irb/formulas.py::_pd_floor_expression`. An unrated large-FI CCR-IRB exposure (routed via P8.31) therefore already floors at 0.05% under B3.1 → green-on-arrival, no fail-first test possible, no engine change required. **Citation correction:** the original "Art. 153(1)(iii)" is the 1.06 IRB capital-formula limb, NOT a PD-floor article — the IRB PD floor is **Art. 160(1)**. The `apply_fi_scalar`/FSE machinery drives the 1.25× correlation uplift (Art. 153(2)), not a PD floor. ~~NEW — CCR-specific PD floor application. Per Art. 153(1)(iii) PD floor of 0.03% replaced by 0.05% for unrated large financial sector entities under B3.1. Ref: PRA PS1/26 Art. 153(1)~~ **Effort: S**

#### Phase 5 — CVA capital sub-epic (separable)

- **P8.60** [x] **FIXED v0.3.5 (batch 20260619-1112)** — BA-CVA (Basic Approach) **reduced-K** vertical slice (scenario CVA-A1) shipped end-to-end. New `engine/cva/ba_cva.py::compute_ba_cva_rwa` computes `cva_rwa = DS_BA_CVA(0.65)·K_reduced·12.5`, where `K_reduced = sqrt[(ρ·ΣSCVA)² + (1−ρ²)·ΣSCVA²]` (ρ=0.5, collapses to `SCVA_c` for a single counterparty) and `SCVA_c = (1/α)·RW_c·Σ_NS[M_NS·EAD_NS·DF_NS]` with α=1.4, `DF_NS=(1−e^(−0.05·M))/(0.05·M)`, reading EAD_NS off the SA-CCR synthetic `ccr__*` rows on the aggregated frame (joined to CVA counterparties on `counterparty_reference`). All CVA scalars + the s.4.4 sector×IG/HY-NR supervisory RW table live in `rulebook/packs/b31.py` (each cited): `ds_ba_cva`=0.65, `cva_ba_supervisory_correlation`=0.5, `cva_ba_supervisory_discount_rate`=0.05, `cva_ba_supervisory_risk_weights` DecisionTable; α reuses `sa_ccr_alpha`, 12.5 reuses the own-funds→RWA source. Gated by the new `cva_ba_cva` pack **Feature** (Basel-3.1-only; no `is_basel_3_1`/`is_crr` branch — arch_check 17). New `RawDataBundle.cva_counterparties: pl.LazyFrame|None` input (+ `CVA_COUNTERPARTY_SCHEMA` + loader edge) and `AggregatedResultBundle.cva_rwa: float|None` output, **both default `None` → clean no-op for every non-CVA run** (6509-test sweep stayed green). Roll-up folded into the aggregator stage (`engine/stages/aggregate.py`, double-gated on `cva_counterparties is not None` AND the pack Feature) rather than a new `engine/registry.py` `StageSpec`, keeping the pinned 9-stage `test_stage_order_is_pinned` green — mirrors the P8.49 `rwa_ccr_default_fund` precedent. **PREMISE CORRECTION:** source-verification against `docs/assets/ps126app1.pdf` (p399 §4.2) surfaced a **mandatory PRA `DS_BA_CVA = 0.65` discount scalar** the architect/plan had omitted → `cva_rwa = 0.65·K_reduced·12.5`, not `K_reduced·12.5`. **Scope:** reduced-K single-counterparty slice only; `K_full`/hedge recognition (Art. 386 → P8.62), aggregated-bundle CVA fields (P8.63), SA-CVA (P8.61, deferred v2.0) and multi-counterparty diversification remain open. No `@cites` (CVA Part articles outside watchfire's bundled index → docstring attribution). Pinned by `tests/acceptance/ccr/test_ccr_ba_cva_a1.py` (CVA-A1: golden `cva_rwa=1,554,155.29` derived dynamically from the pipeline `ead_ccr=1,922,528.69`; 2 tests). `arch_metrics.json` baselines bumped (`engine_fill_null_sites` 442→444, `engine_eager_collect_sites` 33→34). | ~~NEW (sub-epic)~~ — BA-CVA (Basic Approach). `engine/cva/ba_cva.py`. Per PRA PS1/26 Art. 383a-c (post-B3.1). Compute `K_reduced` and `K_full` with hedge recognition, aggregate per Art. 383b formula. Output `cva_rwa` field on `AggregatedResultBundle`. **Effort: L** | Ref: PRA PS1/26 Art. 383, 383a-c
- **P8.62** [x] **FIXED v0.3.6 (batch 20260619-1215)** — full BA-CVA eligible-hedge recognition shipped end-to-end. `K_full = β·K_reduced + (1−β)·K_hedged` (β hedging-disallowance 0.25, PRA PS1/26 CVA Part §4.5) extends the P8.60 reduced version. New firm-supplied `cva_hedges` input (`CVA_HEDGE_SCHEMA` — single-name/index CDS with counterparty attribution, supervisory correlation band, sector/rating RW keys, residual maturity, notional, eligibility flag) wired as an optional `RawDataBundle` field + loader edge mirroring `cva_counterparties`; new cited `packs/b31.py` entries `cva_ba_beta` (§4.5), `cva_ba_single_name_hedge_correlation` (r_hc {IDENTICAL 1.00 / LEGALLY_RELATED 0.80 / SAME_SECTOR_REGION 0.50}, §4.10), `cva_ba_index_diversification_factor` (0.70, §4.8). **Source-verified vs `docs/assets/ps126app1.pdf`:** `SNH_c = Σ_h(r_hc·RW_h·M_h·B_h·DF_h)` carries **NO** `(1/α)` factor (§4.7), unlike `SCVA_c`; HMA (§4.9) and index-hedge (IH) terms implemented in full. No-hedge path is byte-identical to the reduced version (back-compat — P8.60/CVA suite stays green). Pinned by `tests/acceptance/ccr/test_ccr_cva_hedge_a1.py` (CVA-HEDGE-A1: a perfect single-name hedge, notional = EAD/α → `K_hedged`=0, collapses the charge to `cva_rwa_full / cva_rwa_reduced == 0.25`, derived from the live SA-CCR EAD). **Effort: M** | Ref: PRA PS1/26 Art. 386; CVA Part §4.5–4.10
- **P8.63** [x] **FIXED v0.3.7 (batch 20260619-1305)** — Aggregated CVA surface on `AggregatedResultBundle` completed. `cva_rwa` already existed (P8.60); this adds the two descriptive fields `cva_method` (`"BA-CVA"` for both reduced and full Basic Approach; `None` out of scope) and `cva_hedges_recognised` (`True` when ≥1 eligible hedge fed the full-`K` path, `False` reduced, `None` out of scope), populated in the single `engine/stages/aggregate.py::_ba_cva_roll_up` path. `compute_ba_cva_rwa` now returns a typed `BaCvaResult(rwea, hedges_recognised)` so the recognition flag reuses the existing `cva_hedge_eligible` discriminator (single source of truth — no parallel CVA computation); `engine/cva/__init__.py` re-exports it. Portfolio total composes additively as default-risk RWA + CVA RWA (SA-CCR `ccr__*` rows in `Σ rwa_final`; `cva_rwa` adds on top, no double-count) — `engine/aggregator/aggregator.py` untouched, the composition is pinned at test level. Docstring attribution to PRA PS1/26 App.1 CVA Part §4.2–4.10 (CRR2 Art. 384) + Own Funds Part §4(b); no `@cites` (CVA Part outside watchfire's bundled CRR index). Pinned by `tests/acceptance/ccr/test_ccr_cva_aggregated_p8_63.py` (CVA-AGG-A1; 9 tests). [batch 20260619-1305] | ~~**NEW (sub-epic)** — Aggregated CVA on `AggregatedResultBundle`. New fields: `cva_rwa`, `cva_method`, `cva_hedges_recognised`. Update aggregator to compose default-risk RWA + CVA RWA.~~ **Effort: S**

#### Phase 6 — Reporting (COREP + Pillar III)

- **P8.50** [x] **FIXED v0.3.9** — implemented **C 34.01/02/04/08** (analysis-by-approach roll-up, SA-CCR EAD per netting set, BA-CVA capital [Basel 3.1 only — None under CRR], CCP exposures QCCP 2%/4%/non-QCCP/default-fund) as a reshape of the `AggregatedResultBundle` CCR roll-up columns via new `COREPTemplateBundle.c34_*` fields + `_generate_c34_*` in `reporting/corep/`; portfolio `cva_rwa` surfaced as an optional column on `AGGREGATOR_EXIT_EDGE` (`contracts/edges.py`) + broadcast in `engine/stages/aggregate.py` so C 34.04 reads it from the results LazyFrame. QCCP discriminator byte-mirrors the aggregator. **Deferred structure-only** (no engine support yet): C 34.03 (IMM — v2.0), C 34.07/34.11 (IRB-CCR — all CCR fixtures route SA), C 34.05 (collateral — no roll-up cols), C 34.06 (top-10), C 34.09/34.10 (RWEA flow — no prior-period). Pinned by `tests/unit/test_corep_ccr.py` (17 tests, QCCP/CVA invariants over golden p839/cva_a1 fixtures). [batch 20260619-1411] — COREP CCR template suite. `reporting/corep/templates.py` + `generator.py`. New templates: **C 34.01** (CCR analysis by approach), **C 34.02** (SA-CCR), **C 34.03** (IMM, placeholder for v2.0), **C 34.04** (CVA), **C 34.05** (composition of collateral), **C 34.06** (top 10 counterparties), **C 34.07** (IRB CCR — supersedes P2.9), **C 34.08** (exposures to CCPs), **C 34.09** (RWEA flow CCR), **C 34.10** (RWEA flow CVA), **C 34.11** (CCR IRB). **Effort: L** | Ref: COREP Annex II §C 34.01-11; supersedes P2.9
- **P8.51** [x] **FIXED v0.3.9** — implemented **CCR1/CCR2/CCR3/CCR8** (analysis-by-approach, BA-CVA capital charge, SA-CCR EAD by risk-weight band, CCP exposures QCCP/non-QCCP) as a reshape of the `AggregatedResultBundle` CCR roll-up columns via new `Pillar3TemplateBundle.ccr*` fields + `_generate_ccr*` in `reporting/pillar3/`; CRR uses UK prefix, Basel 3.1 uses UKB; CCR3 reuses the existing CR5 risk-weight bands; CCR2 reads the shared `cva_rwa` broadcast (unified single-source with P8.50, no duplicate column). QCCP discriminator byte-mirrors the aggregator. **Deferred structure-only** (no engine support yet): CCR4 (IRB EAD by PD — all CCR fixtures route SA), CCR5 (collateral composition), CCR6 (credit derivatives), CCR7 (RWEA flow under IMM — v2.0). Pinned by `tests/unit/test_pillar3_ccr.py` (16 tests, roll-up invariants over golden CCR-A1/p839/cva_a1 fixtures). [batch 20260619-1411] — Pillar III CCR disclosure templates. `reporting/pillar3/templates.py` + `generator.py`. New tables: **CCR1** (CCR analysis by approach), **CCR2** (CVA capital), **CCR3** (SA EAD by RW), **CCR4** (IRB EAD by PD scale), **CCR5** (collateral composition), **CCR6** (credit derivatives), **CCR7** (RWEA flow under IMM — placeholder), **CCR8** (CCP exposures). **Effort: L** | Ref: PRA PS1/26 Annex XXII §CCR1-8
- **P8.52** [x] **FIXED v0.3.8 — `ead_ccr_total`, `rwa_ccr_default`, `rwa_ccr_qccp_trade`, `failed_trades_rwa` added to `AggregatedResultBundle` as filtered sums over the aggregator `combined_df` (`rwa_ccr_default_fund`/`cva_rwa` already existed); the QCCP/non-QCCP partition reconciles exactly to the full `ccr__` `rwa_final` sum; all four default to `None` on a CCR-free portfolio (per-row `rwa_final`/TREA untouched); unblocks the bundle reads for P8.50/P8.51. Pinned by `tests/acceptance/ccr/test_ccr_p852_reporting_rollups.py` (16 tests). CRR Art. 274(2)/107(2)(a)/306(1)/(4)/378-380. NEW** — `AggregatedResultBundle` extension for CCR reporting columns. Add: `ead_ccr_total`, `rwa_ccr_default`, `rwa_ccr_qccp_trade`, `rwa_ccr_default_fund`, `cva_rwa`, `failed_trades_rwa`. **Effort: S**

#### Phase 7 — Output floor

- **P8.55** [x] **FIXED v0.3.2** — SA-CCR-equivalent EAD × SA-RW now contributes to the output-floor S-TREA/U-TREA numerators (PRA PS1/26 Art. 92(3A) — SA-CCR is NOT on the S-TREA exclusion list). Root cause: SA-routed CCR rows carried `approach_applied='standardised'`, which is excluded from `FLOOR_ELIGIBLE_APPROACHES`, so a CCR-only portfolio yielded `s_trea=u_trea=0` (floor numerator silently omitted SA-CCR). Fix (CCR-specific, shared-seam, **no `aggregator.py` edit**): added `SA_CCR_APPROACH='standardised_ccr'` to `FLOOR_ELIGIBLE_APPROACHES` (NOT to `SA_APPROACHES`) in `engine/aggregator/_schemas.py`, and re-tagged CCR rows' `approach_applied` in `engine/stages/calc.py` (output-floor branch, gated `risk_type==CCR_DERIVATIVE`); both `_floor.py:204` and `aggregator.py:233` read the same constant so the `s_trea_pre` GCRA-cap site stays consistent automatically. Anti-double-count preserved (label not in `SA_APPROACHES` → leaves `sa_rwa_total`; `total_rwa_post_floor` unchanged). Pinned by `tests/acceptance/ccr/test_ccr_floor1_output_floor.py` (B31-CCR-FLOOR-1: `s_trea=u_trea=4,220,395.77`; `floor_threshold=3,059,786.94` @2030; non-binding). PREMISE CORRECTION: the "4-year transitional 2027-2030" framing is wrong — Art. 92(5) has only 3 transitional steps (60/65/70% for 2027/28/29) then 72.5% steady-state from 2030. [batch 20260619-0822] | ~~NEW — Output floor S-TREA / U-TREA inclusion~~ **Effort: S** | Ref: PRA PS1/26 Art. 92(2A)/(3A); BCBS CRE52 floor commentary

#### Phase 8 — Fixtures, tests, and documentation

- **P8.42** [x] **FIXED v0.3.4 (batch 20260619-1022)** — Acceptance scenario **CCR-B5** added: a non-QCCP CCP trade exposure routed through the institution SA ladder. Pins the demotion path CRR Art. 107(2)(a) → Art. 120(1) Table 3 CQS-1 → **20% RW**, with EAD asserted invariant against the CCR-A1 golden (5,480,017.519, loaded from `CCR-A1.json` not transcribed) and anti-degenerate guards (RW ∉ {0.02, 0.04, 0.50, 0.40, 1.0, 12.5}). **Delta-scoped:** CCR-B1 (QCCP 2%/4%, Art. 306) and CCR-B2..B4 (default-fund Art. 308/309) were already pinned by `test_ccr_ccp_orchestrator_pin.py` (P8.39) and `test_ccr_b2_b4_default_fund.py` (P8.49) — deliberately not re-created; the bullet's "non-QCCP 1,250%" applies only to default-fund contributions (out of scope for a trade exposure). Green-on-arrival regression guard — **no `src/` change**. Pinned by `tests/acceptance/ccr/test_ccr_b1_b5_ccp.py` (5 tests) + `tests/fixtures/ccr/p842_nonqccp_b5_builder.py`. [batch 20260619-1022] | ~~NEW — Acceptance scenarios CCR-B1..B5 (CCP exposures). QCCP 2% RW; non-QCCP 1,250%; default-fund alternative formula.~~ **Effort: M**
- **P8.43** [x] **FIXED v0.3.2** — Acceptance scenarios **CCR-C1..C3** (failed/unsettled DvP trades, CRR Art. 378 multiplier ladder) wired end-to-end. `compute_failed_trade_rwa` (implemented under P8.24 but never called in `src/`) is now invoked in `engine/stages/ccr.py`, emitting synthetic SA exposure rows (`exposure_reference='ft__'+failed_trade_id`, `risk_type='SETTLEMENT_FAILED_TRADE'`, `drawn_amount=own_funds_requirement`); a `risk_weight=12.5` override (`own_funds_to_rwa_factor` read from the common pack, Art. 92(3)(ca)) is added early in BOTH `_apply_crr_risk_weight_overrides` and `_apply_b31_risk_weight_overrides` so it wins over the CQS path. Pinned by `tests/acceptance/ccr/test_ccr_c1_c3_failed_trades.py` (C1 t+6→8%→RWA 100,000; C2 t+35→75%→RWA 7,500,000; C3 t+46→100%→RWA 6,250,000; firm-total delta vs `failed_trades=None` = 13,850,000). No hard-excluded files; `@cites` omitted (Art. 378/379 outside watchfire's index, docstring attribution). [batch 20260619-0822] | ~~NEW — Acceptance scenarios CCR-C1..C3 (failed trades). DvP escalating multiplier across t+5..t+45.~~ **Effort: S**
- **P8.44** [x] **FIXED v0.3.4 (batch 20260619-1022)** — Acceptance scenarios **CCR-D1..D3** added as Simplified-SA-CCR (CRR Art. 281) / OEM (Art. 282) **fall-through regression guards**: a sub-threshold portfolio is shown to receive FULL SA-CCR, never a simplified/OEM branch (neither is implemented — both deferred to v2.0 per P8.21/P8.22; full SA-CCR is always conservative). Load-bearing pin is CCR-D3 (margined OTM IR, MtM −4m): `pfe_multiplier == 0.6048083569079303` (rel 1e-9) AND `ead_final == 6,464,360.391383706 != 8,629,617.519` (the value Art. 281's forced multiplier=1.0 would produce). D1/D2 anchor CCR-A1/CCR-A2. Green-on-arrival — **no `src/` change** (verified there is no `ccr_method`/threshold/simplified branch in `engine/ccr/`). Pinned by `tests/acceptance/ccr/test_ccr_d1_d3_simplified_oem_fallthrough.py` (18 tests) + `tests/fixtures/ccr/golden_ccr_d1_d3.py`. [batch 20260619-1022] | ~~NEW — Acceptance scenarios CCR-D1..D3 (Simplified SA-CCR + OEM). Threshold-gated portfolios.~~ **Effort: S**
- **P8.45** [x] **FIXED v0.3.4 (batch 20260619-1022)** — Acceptance scenarios **CCR-E1..E5** added: one SA-CCR EAD routed to the correct SA risk weight per counterparty class × framework — E1 CRR institution CQS2 **50%**, E2 CRR corporate CQS3 **100%**, E3 CRR sovereign (foreign BR, CQS3) **50%**, E4 B3.1 institution ECRA CQS2 **30%**, E5 B3.1 corporate CQS3 **75%**. Cross-checks: EAD-invariance across counterparty classes (within-CRR E1=E2=E3 rel 1e-9; within-B3.1 E4=E5; cross-framework E1≈E4 rel 1e-3 — B3.1 2027 reporting dates shift the Art. 279b supervisory-duration bucket ~0.02%), `rwa==ead*rw` derived from the pipeline EAD, and the CRR↔B3.1 RW deltas (0.50≠0.30; 1.00≠0.75). Confirms the architect-flagged sovereign-CQS-inheritance routing already works (foreign sovereign resolves 50%, not the unrated 100% fallback). Green-on-arrival — **no `src/` change**. Pinned by `tests/acceptance/ccr/test_ccr_e1_e5_default_risk_routing.py` (20 tests) + `tests/fixtures/ccr/p845_e1_e5_builder.py`. [batch 20260619-1022] | ~~NEW — Acceptance scenarios CCR-E1..E5 (default-risk RWA integration). SA-CCR EAD → Institution / Corporate / Sovereign / Retail-derivative routing under both CRR and B3.1. Cross-check that EAD is consistent and RW lookup uses existing tables.~~ **Effort: M**
- **P8.46** [ ] **PARTIAL — CVA-A3 landed v0.3.11 (batch 20260619-1550); CVA-A2 v0.3.10 (batch 20260619-1521)** — BA-CVA two-counterparty diversification reduced-K acceptance pin (PRA PS1/26 App.1 CVA Part §4.2, ρ=0.5 systematic cross-term — the first scenario where `K_reduced = √[(ρ·ΣSCVA)² + (1−ρ²)·ΣSCVA²]` does not collapse to the single-CP `SCVA_c`). Green-on-arrival regression guard, **no engine change**; golden derived dynamically from the live SA-CCR netting-set EADs + an EAD-robust invariant `√(SCVA₁²+SCVA₂²) < K_reduced < SCVA₁+SCVA₂`. Pinned by `tests/acceptance/ccr/test_ccr_ba_cva_a2.py` (5 tests). CVA-A3 (one-CP/two-NS `SCVA_c` cross-NS additivity `SCVA_c = SCVA_NS1 + SCVA_NS2` + single-CP collapse `K_reduced = SCVA_c`) landed v0.3.11, pinned by `tests/acceptance/ccr/test_ccr_ba_cva_a3.py` (7 tests; green-on-arrival, no engine change). Already-pinned BA-CVA scenarios: CVA-A1 (P8.60), CVA-A2 (P8.46), CVA-A3 (P8.46), CVA-HEDGE-A1 (P8.62), CVA-AGG-A1 (P8.63). **REMAINING (BA-CVA, future batches):** A4 (sector × IG/HY_NR supervisory-RW-table coverage), A5 (multi-CP single-name-hedge K_full blend), A6 (maturity-discount-factor sensitivity). **BLOCKED:** SA-CVA scenarios depend on P8.61 (DEFERRED v2.0) — cannot ship until the SA-CVA epic lands. — Acceptance scenarios **CVA-A1..A10** (BA-CVA + SA-CVA). Block on P8.60 / P8.61. **Effort: L**
- **P8.47** [ ] **NEW** — Performance benchmarks for CCR pipeline. `tests/benchmarks/test_ccr_perf.py`. 100k trades / 10k netting sets target wall-clock. Profile group-by-netting-set; consider streaming if memory pressure. **Effort: S**
- **P8.48** [ ] **NEW — residual after first-batch partial** — `@cites` decorators for the **remaining** CRR Art. 271-311 articles + PRA PS1/26 CCR articles that the first batch's partial scope (Art. 271, 272, 274, 275(1), 279a/b/c(1), 280) did not cover: Art. 275(2), 277, 277a, 278, 279a (option-delta sub-branch), 279c(2), 281, 282, 283-294 (IMM — deferred), 295-297, 306-310, 378-380, 386, 291. `watchfire matrix` must produce a clean article→function index for the new module across all batches. **Effort: S** | Ref: `docs/development/citation-tracking.md`

#### Phase 9 — Documentation

- **P8.70** [ ] **NEW** — `docs/specifications/ccr.md` — canonical specification page. Covers: scope, SA-CCR formula chain, simplified / OEM thresholds, CCP treatment, integration into existing pipeline, configuration knobs, CVA scope. **Effort: M** | Owner: `doc-writer` (post-implementation) — tracked in `DOCS_IMPLEMENTATION_PLAN.md`
- **P8.71** [ ] **NEW** — `docs/user-guide/regulatory/ccr.md` — user-facing guide with worked examples. **Effort: M**
- **P8.72** [ ] **NEW** — Update `docs/specifications/architecture.md` data-model section with the four new bundles (`TradeBundle`, `NettingSetBundle`, `MarginAgreementBundle`, `CCRCollateralBundle`). **Effort: S**
- **P8.73** [ ] **NEW** — Update `docs/plans/implementation-plan.md` (the published Zensical narrative) with the CCR roadmap once Phase 1 (P8.1-P8.7) is closed. **Effort: S**
- **P8.74** [ ] **NEW** — `.claude/skills/basel31/references/ccr-changes.md` + `.claude/skills/crr/references/ccr.md` — skill reference files so the `crr` and `basel31` skills can answer SA-CCR queries. **Effort: M**

#### Phase 10 — Deferred (v2.0 / next-major-epic)

- **P8.21** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 1)** — Simplified SA-CCR. `engine/ccr/simplified.py`. Per Art. 281: same PFE structure but **PFE multiplier fixed at 1** and **MF simplification**. Available only for firms below derivative-portfolio threshold (Art. 273a(1)) — config-gated. **Not in initial epic** — sub-threshold portfolios fall through to full SA-CCR (always conservative; no capital understatement risk). **Effort: M** | Ref: CRR Art. 281, 273a(1)
- **P8.22** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 1)** — Original Exposure Method (OEM). `engine/ccr/oem.py`. Per Art. 282: `EAD = (α × notional) × supervisory factor by residual maturity`. Available only for small derivative portfolios (Art. 273a(2)). **Not in initial epic** — sub-threshold portfolios fall through to full SA-CCR. **Effort: S** | Ref: CRR Art. 282, 273a(2)
- **P8.61** [ ] **DEFERRED v2.0 (re-classified 2026-05-22 per operator scope decision 2 — CVA sub-epic ships BA-CVA only initially)** — SA-CVA (Standardised Approach). `engine/cva/sa_cva.py`. PRA permission gated. Sensitivity-based: delta + vega risk across six risk classes (IR, FX, counterparty credit spread, reference credit spread, equity, commodity). 3-4 months on its own. **Effort: L (3-4 months)** | Ref: PRA PS1/26 Art. 383d-w
- **P8.90** [ ] **DEFERRED v2.0** — Internal Model Method (IMM). CRR Art. 283-294. Large scope: EPE/EEPE simulation engine, alpha calibration, model-validation governance, stressed-EPE add-on, PRA permission process. Recommended only after SA-CCR + CVA are stable in production. **Effort: XL (6-9 months)** | Ref: CRR Art. 283-294; PRA SS12/13 IMM
- **P8.91** [ ] **DEFERRED v2.0** — IMM-specific output-floor S-TREA computation per P8.55. Block on P8.90. **Effort: M**

The bullet list above is the canonical work-queue. The items below have substantive sub-issue structure that doesn't compress cleanly into a single bullet, so they are kept in long-form.

### Tier 9 — CRR / Basel 3.1 Conformance Audit (workflow 2026-06-01)

Generated by the regulatory-conformance-audit workflow on 2026-06-01; each item independently reviewer-confirmed. Promote capital-impacting items into Tier 1 on the next curator pass.

#### Capital-impacting

- **P1.191** [x] **FIXED (batch 20260601-1947): QRRE qualification now uses the per-individual aggregate nominal test (per-`counterparty_reference` sum of `facility_limit` over the QRRE sub-portfolio, replacing the per-row `facility_limit <= qrre_max_limit` check; `classifier.py` `_classify_exposure_subtypes`). CRR Art. 154(4)(c) / PS1/26 Art. 147(5A)(c). Pinned by `tests/unit/classifier/test_p1_191_qrre_aggregate_nominal.py` (8 tests).** Original gap:** — `classifier.py:872-877` (`_combine_classifications`) sets `is_qrre` via a per-row `facility_limit <= qrre_max_limit` check; CRR Art. 154(4)(c) / PS1/26 Art. 147(5A)(c) require the largest per-individual *aggregate* nominal exposure across the QRRE sub-portfolio to be ≤ EUR 100k (CRR) / GBP 90k (B31). Reuse the obligor-aggregate at `classifier.py:2182-2219` and compare per-individual aggregate to `qrre_max_limit` (threshold at `config.py:639/658/687`). Affects RETAIL_QRRE R=0.04 (`formulas.py:894`) + QRRE PD/LGD floors. Scalars already correct. **Effort: M** | Ref: CRR Art. 154(4)(c) (crr.pdf p.152); PS1/26 Art. 147(5A)(c) (ps126app1.pdf p.91)
- **P1.192** [x] **FIXED (batch 20260601-2131): CRR EU-domestic-currency sovereign no longer hard-forced to SA — the `is_eu_domestic_sovereign` branch in `classifier.py` `_build_approach_expr` is now AND-ed with `pl.lit(config.is_basel_3_1)`, so under CRR a CGCB holding IRB permission routes to IRB (Art. 150(1)(a)/(d) PPU election) while B31 forced-SA (Art. 147A(1)(a)) is preserved. Pinned by `tests/unit/test_classifier.py::TestApproachAssignment::test_crr_eu_domestic_sovereign_with_irb_permission_routes_to_airb` (+ B31 regression guard + unchanged foreign-ccy test).** Original gap:** CRR path forces EU-domestic-currency sovereigns to SA, pre-empting CGCB IRB permission — `classifier.py:1920-1924/1941-1942` (`_build_approach_expr`): the `is_eu_domestic_sovereign` branch routes any CENTRAL_GOVT_CENTRAL_BANK in an EU member-state domestic currency to `ApproachType.SA` with no `config.is_basel_3_1` guard (unlike the adjacent Art. 147A slotting branch at `:1927-1931`). Under CRR, Art. 114(4) only grants a 0% SA *risk weight*; sovereign-to-SA *approach* routing is the Art. 150(1)(a)/(d) PPU permission, not a hard override — forcing SA pre-empts a CRR firm electing CGCB IRB (0% RW vs positive IRB RWA, anti-conservative). Gate the branch on `config.is_basel_3_1` (forced-SA stays correct under B31 via Art. 147A(1)(a)); split the test pins at `test_classifier.py:1613-1668` and `:1670-1729`. **Effort: S** | Ref: CRR Art. 114(1)/(4), Art. 150(1)(a)/(d) (crr.pdf p.111, pp.145-146); contrast PS1/26 Art. 147A(1)(a)
- **P1.193** [x] **FIXED (batch 20260601-1947): B31 corporate-SME 85% override now gated on `cqs.is_null() | (cqs <= 0)` (mirroring the SA-SL gate in `sa/namespace.py` `_apply_b31_risk_weight_overrides`); rated SMEs (CQS 1-6) now keep their Art. 122(2) Table 6 weight (20/50/75/100/150/150), unrated SMEs still 85%. PS1/26 Art. 122(11). Pinned by `tests/acceptance/basel31/test_p1_193_b31_rated_sme_corporate_cqs_weights.py` (10 tests).** Original gap:** — `sa/namespace.py:1293-1295` (B31 override) `.when(uc.contains('CORPORATE') & uc.contains('SME')).then(85%)` lacks a CQS gate, so a rated CORPORATE_SME (CQS 1-6) is forced to 85%, overwriting the Table-6 weight the rw_table join set at `:1130`. PS1/26 Art. 122(11) restricts 85% to SMEs without a nominated-ECAI assessment; Art. 122(2)+Table 6 require rated SMEs to use 20/50/75/100/150/150%. Gate the SME branch on `pl.col('cqs').is_null() | (pl.col('cqs') <= 0)`, mirroring the SA-SL branch at `:1285-1291`. Under-weights rated SMEs at CQS 4-6. **Effort: S** | Ref: PS1/26 Art. 122(11), 122(2)+Table 6 (ps126app1.pdf pp.44-45)
- **P1.194** [x] **FIXED (batch 20260601-2131): B31 CRE loan-split (Art. 124H(1)) residual now uses the Art. 124L counterparty-type table — `b31_commercial_rw_expr` builds a `cp_rw_for_cre` (natural person / retail-qualifying SME 75% via `B31_RRE_RESIDUAL_RW_NATURAL_PERSON`, other SME 85% via `B31_RRE_RESIDUAL_RW_OTHER_SME`, CQS-independent) used in the `loan_split_rw` leg only; Art. 124H(3) + income legs keep raw CQS `cp_rw`. Natural-person high-LTV residual RW now 0.64 (was 0.70667). Pinned by `tests/unit/test_b31_sa_risk_weights.py::TestB31CommercialREGeneral::test_high_ltv_loan_split_blended` (+ re-pinned sibling `test_other_vs_natural_person_comparison`).** Original gap:** B31 CRE loan-split (Art. 124H(1)) residual uses raw CQS RW, not the Art. 124L 75/75/85 counterparty-type table — `data/tables/b31_risk_weights.py:587` (`b31_commercial_rw_expr` loan-split leg) computes `loan_split_rw = 0.60*secured_share + cp_rw*(1-secured_share)` with `cp_rw = _cqs_risk_weight` at `:559`, applying raw CQS RW to the Art. 124H(1) residual. Art. 124H(1)(b) defers the residual to Art. 124L, which fixes natural-person 75% / retail-qualifying SME 75% / other SME 85% regardless of CQS. Build a `cp_rw_for_cre` mirroring the residential `cp_rw_for_rre` at `:491-499` (124H(1) reaches only counterparty types a/b/c). Re-pin `test_b31_sa_risk_weights.py::TestB31CommercialREGeneral::test_high_ltv_loan_split_blended` (`:340-372`). **Effort: S** | Ref: PS1/26 Art. 124H(1)(b), Art. 124L(1)(a)-(c) (ps126app1.pdf internal pp.56,58)
- **P1.195** [x] **FIXED (batch 20260601-2211): B31 Other RE (Art. 124J) non-income residual now uses the Art. 124L counterparty-type table — `b31_other_re_rw_expr` builds a full FOUR-limb `cp_rw_for_re` (natural person / retail-qualifying SME 75%, other SME 85%, social-housing `max(75%, unsecured)`, otherwise raw counterparty RW; `B31_RRE_RESIDUAL_RW_*` scalars) mirroring the residential `cp_rw_for_rre`, fed into both the Art. 124J(2) RESI residual leg and the Art. 124J(3)(b) CRE `max(0.60, ·)` leg; income-dependent 1.50 leg unchanged. Unlike P1.194's CRE-loan-split 2-limb subset, the social-housing/other limbs ARE included because Art. 124J has no counterparty re-routing gate. Natural-person residual now 0.75 (was raw 1.00). Pinned by `tests/unit/test_b31_other_re.py` (7 cases A-G + 2 SA-level). Missing-column tolerance via `pl.coalesce(cs.by_name(..., require_all=False), lit(default))` keeps legacy bare-expr tests green.** Original gap:** B31 Other RE (Art. 124J) non-income branch ignores Art. 124L counterparty-type table — `data/tables/b31_risk_weights.py:643-655` (`b31_other_re_rw_expr`) resolves the Art. 124J(2) RESI-non-dependent leg (returns `cp_rw`) and Art. 124J(3)(b) CRE-non-dependent leg (returns `max(0.60, cp_rw)`) from `cp_rw = pl.col('_cqs_risk_weight').fill_null(1.0)` (raw CQS, 100% unrated default) rather than the Art. 124L(1)(a)-(e) table 124J(2)/(3) require. Replace `cp_rw` with the 124L routing identical to `b31_residential_rw_expr`'s `cp_rw_for_rre` (`:491-499`): natural/retail-SME 75%, other SME 85%, social housing `max(75%, unsecured)`. Re-pin `test_b31_other_re.py:249-270`. Call site `sa/namespace.py:580`. **Effort: S** | Ref: PS1/26 Art. 124J(2)/(3)(b), Art. 124L (ps126app1.pdf internal pp.57,58)
- **P1.196** [x] **FIXED (batch 20260601-2131): CRR rated corporate-SME now respects the Art. 122 Table 6 CQS weight — the corporate-SME 100% override in `_apply_crr_risk_weight_overrides` (`sa/namespace.py`) is gated on `pl.col('cqs').is_null() | (pl.col('cqs') <= 0)` (mirroring the B31 P1.193 fix), so a rated SME (CQS 1-6) keeps its Art. 122 weight (CQS1 → 20%) and only unrated SMEs get 100%; Art. 501 SME relief stays the separate supporting factor. Pinned by `tests/acceptance/crr/test_p1_196_crr_rated_sme_corporate_cqs_weight.py` (7 tests; RW 0.20, RWA 200,000 pre-factor, final 152,380 with SF 0.7619).** Original gap:** CRR Art. 122 CQS table ignored for rated corporate SMEs (ungated 100% override) — `sa/namespace.py:1378-1379` (`_apply_crr_risk_weight_overrides`) CRR SME override `.when(uc.contains('CORPORATE') & uc.contains('SME')).then(1.00)` has no CQS predicate, overwriting the base Art. 122 CQS weight (joined `:1130`; CORPORATE_SME→`_lookup_class='CORPORATE'` at `:1113`) for rated SMEs. Art. 122(1) Table 6 gives rated corporates 20/50/100/100/150/150 — a CQS1 SME should be 20% but gets 100%, CQS5/6 should be 150% (understatement). SME relief is the separate Art. 501 supporting factor. Gate on `pl.col('cqs').is_null() | (pl.col('cqs') <= 0)`. Sibling B31 arm is P1.193. **Effort: S** | Ref: CRR Art. 122(1) Table 6, Art. 501 (crr.pdf pp.119-120)
- **P1.197** [x] **FIXED (batch 20260601-1947): CRR slotting OBS EAD now routes to the F-IRB CCF via a CRR-gated `(approach == SLOTTING) & (not is_b31)` branch in `engine/ccf.py` `_compute_ccf` (→ `_firb_ccf_from_risk_type`, e.g. MR 75%); B31 slotting stays on SA CCFs per Art. 166C. CRR Art. 166(8)(d)/(10), 147(8), 151(5)/(8). Pinned by `tests/acceptance/crr/test_p1_197_crr_slotting_obs_ead_firb_ccf.py` (7 tests).** Original gap:** — `engine/ccf.py:438-446` (`_compute_ccf` final when-chain) matches only AIRB and FIRB; `ApproachType.SLOTTING` falls to `.otherwise()` → `_sa_ccf_from_risk_type` (SA CCF). Under CRR, SL slotting is a corporate exposure (Art. 147(8)) whose EAD is Art. 166(8)/(10) (via Art. 151(8)), so a CRR slotting MR/OC commitment must get F-IRB 75% (`data/tables/ccf.py:133`), not SA 50% — understates EAD/RWA on every CRR slotting OBS exposure. Add a CRR-only branch routing `approach=='slotting'` to `_firb_ccf_from_risk_type`; leave B31 on SA CCFs (Art. 166C correct). EAD consumption at `slotting/namespace.py:447-448`. **Effort: S** | Ref: CRR Art. 166(8)(d)/(10), 147(8), 151(5)/(8), 153(5) (crr.pdf pp.143/148/165); contrast PS1/26 Art. 166C
- **P1.198** [ ] **DISPUTED — likely false-positive audit finding; do NOT implement without authoritative PS1/26 PDF confirmation (investigated batch 20260601-2211, operator-confirmed drop).** The plan premise (PS1/26 Art. 230(1)(b)/231 adds an 8% Hfx on **funded non-financial** FCM collateral when currencies differ) directly contradicts the SSOT spec `docs/specifications/crr/credit-risk-mitigation.md:257-260` and the `basel31` skill, which both state `HFX = 0` for funded non-financial collateral under BOTH CRR and B31 (the 8% Hfx applies only to **financial** collateral via Art. 224 FCCM, and to **unfunded** protection via Art. 233(3) — the latter is P1.200's territory; currency mismatch on non-financial collateral is handled by upstream spot rebasing in `FXConverter`). The conformance-audit appears to have conflated the Art. 233 unfunded-protection FX haircut with funded non-financial collateral. Implementing the "fix" would INTRODUCE an erroneous over-charge. The CRR side is already correct (HFX=0). **Re-open only if a direct read of `ps126app1.pdf` Art. 230(1)(b)/231 verbatim text confirms a non-financial Hfx; otherwise close as not-a-bug.** ~~**B31 FCM LGD* omits the Hfx currency-mismatch haircut on non-financial collateral (Art. 230(1)(b))**~~ — `crm/haircuts.py:221-229` gates the FX haircut to financial collateral only (`is_financial = ~collateral_type.is_in(NON_FINANCIAL_COLLATERAL_TYPES)`), but PS1/26 Art. 230(1)(b)/231 Ci(b) require an explicit Hfx (8% @10-day) on ES for receivables/immovable/other physical when currencies differ. The missing term flows `value_after_haircut` → `effectively_secured` (`collateral.py:638-641`) → `lgd_star_expr` (`collateral.py:1052-1061`), overstating the secured portion. Fix must be `is_basel_3_1`-conditional (keep financial-only for CRR; allow non-financial Hfx under B31). Constant `schemas.py:1275`. **Effort: M** | Ref: PS1/26 Art. 230(1)(b), 231 Ci(b)/231(2)(a) (ps126app1.pdf PDF idx 208-210); contrast CRR Art. 230 (crr.pdf pp.226-227, no Hfx)
- **P1.199** [ ] **CIU/fund units as collateral mis-bucket to other_physical (flat 40% HC) instead of Art. 224(5) look-through volatility adjustment** — `crm/haircuts.py:592-616` (`_normalize_collateral_type_expr`) has no CIU/fund branch, so eligible CIU collateral (Art. 197(5)/(6)) falls to `.otherwise(other_physical)` and the financial-haircut join misses → `:576 .fill_null(0.40)`. CIU also absent from `COLLATERAL_TYPE_CATEGORY` (`schemas.py:1252`), giving other_physical LGDS (40% CRR / 25% B31, `crm_supervisory.py:53,67`) not financial 0%. Recognise eligible CIU as financial with a volatility adjustment = weighted-average of underlying-asset haircuts (or highest where holdings unknown) per Art. 224(5); no Art. 224(5) collateral look-through helper exists in `engine/crm/` (the `equity/calculator.py` CIU code is the unrelated Art. 132 equity path). **Effort: L** | Ref: CRR Art. 224(5) (crr.pdf p.221), Art. 197(5)-(6) (crr.pdf p.194); B31/FCCM retention via comparison PDF p.28
- **P1.200** [x] **FIXED (batch 20260601-2211): B31 guarantee/CDS maturity-mismatch (Art. 239(3)) scaling now runs under `basel_3_1()` — dropped the `if config.is_crr:` guard at `crm/guarantees.py:200-201` so the sole call to `_apply_maturity_mismatch_to_guarantees` runs unconditionally for both frameworks (mirroring the already-unconditional `collateral.py:397`). The function is framework-agnostic (Art. 239(3) `GA = G*·(t−0.25)/(T−0.25)` is identical CRR/B31), so CRR is byte-for-byte unchanged and B31 short-dated unfunded protection (t<T) is no longer over-recognised. Pinned by `tests/acceptance/basel31/test_p1_200_b31_guarantee_maturity_mismatch.py` (B31 total RWA 626,666.67, was 200,000; + CRR no-regression pin). No sibling regressions (full tests/unit/crm 713 + tests/acceptance/basel31 565 green).** Original gap:** B31 guarantee/CDS maturity-mismatch (t-0.25)/(T-0.25) scaling skipped — gated on `config.is_crr` — `crm/guarantees.py:200-201` gates the only call to `_apply_maturity_mismatch_to_guarantees` on `if config.is_crr:`, so under `basel_3_1()` the Art. 239(3) GA = G*·(t−0.25)/(T−0.25) scaling for unfunded protection never runs — a short-dated guarantee/CDS (t<T, ≥1y so it passes the Art. 237(2)(a) filter at `:184`) over-recognises protection and understates B31 RWA. PS1/26 Art. 235(1)/236(1) require GA "further adjusted ... as laid down in Articles 237 to 239"; Art. 239(3) is identical to CRR. Drop the `is_crr` guard (mirroring the already-unconditional `collateral.py:397`). **Effort: S** | Ref: PS1/26 Art. 235(1), 236(1), 239(3) (ps126app1.pdf pp.214-215,219)
- **P1.201** [ ] **Art. 233(2)(b) 60%-of-exposure cap missing for over-covering no-restructuring CDS** — `crm/guarantees.py:1077-1086` (`_apply_restructuring_haircut_to_guarantees`) applies `amount_covered*(1-0.40)` for both limbs of Art. 233(2) but never caps G* at 60% of exposure value when G > E. For G=200, EAD=100 the only downstream cap (cap-at-EAD pro-rata at `:627-631`) yields recognised protection=100 instead of 0.60×100=60 — over-recognition up to 40% of EAD, anti-conservative. Apply `min(amount_covered, 0.60*exposure_value)` for the no-restructuring branch when `amount_covered > exposure_value`. The unused twin `_apply_restructuring_exclusion_haircut` (`:1313-1365`) shares the defect. Closely related to P1.202 (same function). **Effort: S** | Ref: CRR Art. 233(2)(a)/(b) (crr.pdf p.230); PS1/26 Art. 233(2) (no override)
- **P1.202** [ ] **Restructuring-exclusion 40% haircut skips `percentage_covered` credit derivatives** — `crm/guarantees.py:1071-1086` (`_apply_restructuring_haircut_to_guarantees`, called `:193`) gates on `amount_covered>0` and rewrites only `amount_covered`, so a no-restructuring CDS supplied via `percentage_covered` (amount null/0) gets no 40% reduction and is recognised at full G (resolved `:893`). Mirror the FX sibling's percentage branch (`:1026-1033`): add a `percentage_covered` output expr and broaden the gate to `has_coverage` (amount OR percentage>0). Dead correct-shape variant at `:1313`. Add an acceptance test for the percentage-CDS restructuring path. Same function as P1.201. **Effort: S** | Ref: CRR Art. 233(2)(a)-(b), 233(1), 216 (crr.pdf pp.~229-230); PS1/26 carries Art. 233(2)
- **P1.203** [ ] **Double-default eligibility admits ineligible guarantor classes (sovereign/MDB/corporate) and omits Art. 202(b)-(d) CQS/PD gates** — `irb/guarantee.py:622-629` (`_apply_double_default`, `_is_eligible_guarantor_type`) flags ['institution','mdb','central_govt_central_bank'] as Art. 153(3) DD-eligible with no CQS/PD gate, plus corporates at CQS≤2. Art. 202 limits eligible DD providers to institutions, investment firms, insurers/reinsurers and ECAs meeting (a) expertise, (b) regulated-equivalent OR ECAI CQS≤3, (c) internal PD≤CQS2, (d) internal PD≤CQS3 — sovereigns, MDBs, corporates are NOT eligible. Restrict to institution (+investment_firm/insurer/eca if modelled), drop govt/mdb/corporate, apply CQS≤3 + internal-PD gates. CRR-only, gated behind `config.enable_double_default` (default False). **Effort: M** | Ref: CRR Art. 202(a)-(d) (crr.pdf p.201)
- **P1.204** [ ] **B31 slotting col A/C short-maturity discount applied mandatorily; Art. 153(5)(d) makes it an optional "may" election over the B/D default** — the B31 branch of `lookup_rw` (`slotting/transforms.py` — *paths refreshed 2026-07-07: was `slotting/namespace.py:377-387`, retired Phase 4*) unconditionally selects the col-A/C short tables (pack `slotting_rw_short` / `slotting_rw_hvcre` in `rulebook/packs/b31.py` — *was `b31_slotting.py`, moved to packs Phase 5*; non-HVCRE 50/70; HVCRE 70/95) whenever `is_short_maturity`, with no opt-in. Art. 153(5)(c) makes col B/D (70/90; 95/120) the mandatory default and 153(5)(d) makes col A/C a firm election, so the engine understates RWA vs the default for short-dated Strong/Good SL. Add an election flag (input column or `CalculationConfig` field, mirroring `look_through_election`) gating col A/C, defaulting to B/D; CRR path stays auto. Also fix stale "Not Yet Implemented" at `docs/specifications/basel31/slotting-approach.md:249-267` (docs-side — see cross-file note). `is_short` derivation now in `slotting/transforms.py`. **Effort: M** | Ref: PS1/26 Art. 153(5)(c)-(d) Table A (ps126app1.pdf pp.102-103); contrast CRR Art. 153(5) Table 1 (crr.pdf p.150)
- **P1.205** [ ] **CRR Art. 165(1)(a)/(b) 0.09% equity PD floors defined but unreachable — no input column to select long-term-relationship / regular-cashflow sub-types** — `equity/calculator.py:857-869` (PD/LGD branch) reads only `EQUITY_PD_FLOORS['exchange_traded']` (0.40%) and `['other']` (1.25%); `pd_floored` has two outcomes. The two 0.09% keys `exchange_traded_long_term` / `non_exchange_regular_cashflow` (`crr_equity_pd_lgd.py:38-39`) have zero consumers and no input column selects them (`VALID_EQUITY_TYPES` `schemas.py:1304-1316`, `EQUITY_EXPOSURES` `:605-629`). Result: exchange-traded long-term floored at 0.40% not 0.09% (165(1)(a)); non-exchange cashflow-based at 1.25% not 0.09% (165(1)(b)). Conservative but diverges. Add an equity sub-type flag + four-way `pd_floored`; add CRR acceptance tests. **Effort: M** | Ref: CRR Art. 165(1)(a)-(d) (crr.pdf p.164)
- **P1.206** [ ] **PD/LGD equity LGD (Art. 165(2)) keys 65% diversified-PE on type-string only, ignoring `is_diversified_portfolio` the IRB-Simple branch honours** — `equity/calculator.py:871-876` assigns 65% only when `equity_type=='private_equity_diversified'`, whereas the IRB-Simple branch at `:731-738` also recognises `equity_type=='private_equity' & is_diversified_portfolio==True`. No step normalises the flag into the type string. An identical diversified PE exposure encoded via the boolean gets 90% LGD under PD/LGD but the diversified treatment under IRB-Simple — inconsistent Art. 165(2). Broaden the `:872` `when` to OR in `(eq_type=='private_equity') & is_diversified_portfolio.fill_null(False)`. **Effort: S** | Ref: CRR Art. 165(2) (crr.pdf p.164; gated by Art. 155(3))
- **P1.207** [ ] **B31 `is_speculative` flag short-circuits to 400% bypassing the unlisted-AND-business<5y higher-risk test (Art. 133(4))** — `equity/calculator.py:692-695` (`_apply_b31_equity_weights_sa`) assigns 400% whenever `is_speculative==True` OR `equity_type=='speculative'`, evaluated independent of `is_unlisted` (`:665`) and `business_age` (`:670-680`), so a LISTED or ≥5y-business equity flagged speculative wrongly gets 400% instead of 250% (Art. 133(3)). PS1/26 glossary defines higher-risk equity solely as unlisted AND business<5y — no speculative tier. Same ungated OR repeats in the transitional floor at `:968+991`. Feed `is_speculative` into the unlisted/business-age determination rather than letting it force 400%, in both branches. **Effort: S** | Ref: PS1/26 Glossary "higher risk equity exposure" (ps126app1.pdf p.5), Art. 133(3)/(4)
- **P1.208** [ ] **SA provision deduction sums all provision rows — GCRA wrongly reduce SA exposure value (Art. 111(1) is SCRA-only)** — `crm/provisions.py:166-273` (`_resolve_provisions_multi_level`) aggregates `pl.col('amount')` across all PROVISION_SCHEMA rows into `provision_allocated` with no `provision_type` filter; the SA drawn-first branch (`:92-117`) deducts the entire `provision_allocated` from exposure value. CRR Art. 111(1) reduces SA exposure by SPECIFIC CRA only — general CRA flows to Tier 2 via Art. 110(1)/62(c). Filter the SA deduction to `provision_type=='scra'` (null/legacy as scra), exclude `gcra` and route it to the unused `gcra_provision_amount` output (`schemas.py:2054`) / Tier 2 path. Shares the no-`provision_type`-split root cause with P1.210. **Effort: M** | Ref: CRR Art. 111(1) with Art. 110(1)/62(c) (crr.pdf pp.109-110); PS1/26 Art. 111 unchanged — *2026-07-07 compliance audit re-confirmed (art111-gcra-included-in-sa-provision-deduction, §6).*
- **P1.209** [ ] **Art. 158(6A) post-model EL gross-up bypasses the Art. 159 shortfall/excess test (`el_after_adjustment` ignored)** — `irb/adjustments.py:255-264` (`apply_post_model_adjustments`) writes the grossed-up EL to `el_after_adjustment` but leaves `expected_loss` unchanged; `compute_el_shortfall_excess` (`:344`, called at `calculator.py:242` right after PMA at `:241`) reads the pre-PMA `expected_loss`, so `el_shortfall/el_excess` (summed in `_el_summary.py:100-105`) ignore the mandatory EL increase. Feed `el_after_adjustment` into `compute_el_shortfall_excess` on the B31 path. Latent until a non-zero `pma_el_scalar` is configured (B31 default 0.0, `config.py:540/574`); when set it understates the CET1 shortfall / overstates T2 excess. **Effort: S** | Ref: PS1/26 Art. 158(6A) with Art. 159(1), 146(3)(c) (provisions.md:93-102,250,362-364)
- **P1.210** [ ] **Art. 159(3) defaulted pool (Pool D) wrongly includes GCRA — `provision_type` never split GCRA/SCRA upstream** — `crm/provisions.py:_resolve_provisions_multi_level` (`:169/200/239/270`) sums provision amounts with no `provision_type` filter, so `provision_allocated = GCRA+SCRA` undifferentiated; `irb/adjustments.py:362,367` builds per-exposure `el_excess` from this combined pool_b — for a defaulted obligor any attached GCRA inflates `el_excess`. `_el_summary.py:200-201` sums into `d_excess`; the Art. 159(3) split branch (`:217,226`) sets `effective_excess = d_excess` feeding `t2_credit` (`:237`). Pool D is SPECIFIC-CRA-on-defaulted only; general CRA must go to non-defaulted Pool B. Split `provision_allocated` by `provision_type` so GCRA always lands in pool_b. Shares root cause with P1.208. **Effort: M** | Ref: CRR Art. 159(3) (provisions.md:154-159,179-198); PS1/26 retained Art. 159
- **P1.211** [ ] **Infrastructure supporting factor (0.75) lacks corporate/SL class gate AND defaulted-exposure exclusion (Art. 501a(1)(a))** *(merged: this item subsumes the narrower defaulted-only finding)* — `supporting_factors.py:422-429` (`apply_factors`, `infra_factor_expr`) and `:174` (`get_effective_factor`) apply the 0.75 factor gated solely on `is_infrastructure`, which `classifier.py:758` derives only from `product_type` containing 'INFRASTRUCTURE' (no class condition, survives default rerouting at `:748-758`). Art. 501a(1)(a) requires the exposure be in corporate OR specialised-lending class AND excludes defaulted exposures. Gate so 0.75 applies only when `exposure_class` is CORPORATE/SPECIALISED_LENDING and `~is_defaulted` (the SME branch already excludes defaults at `:387/406`); else 1.0. Add a defaulted-infrastructure regression test. **Effort: S** | Ref: CRR Art. 501a(1)(a) (crr.pdf PDF idx 418)
- **P1.212** [ ] **GCRA cap base uses floor-eligible S-TREA, not full standardised TREA (Art. 92(3A))** — `aggregator/aggregator.py:185-192` computes the GCRA cap base (`s_trea_pre`) by filtering `combined` to FLOOR_ELIGIBLE_APPROACHES (IRB+slotting only), excluding pure-SA and equity rows present in the frame. Art. 92(2A) caps GCRA at 1.25% of paragraph-3A S-TREA = SA-equivalent RWA across ALL business activities, not just the modelled subset. Sum `sa_rwa` over FLOOR_ELIGIBLE + SA + EQUITY approaches (whole `combined` frame) before `compute_of_adj` (`:196`); cap applied at `_floor.py:89-90`. For mixed firms the too-small base over-tightens the cap, inflating OF-ADJ (over-conservative). Add a mixed-approach test (`test_of_adj.py` uses IRB-only). **Effort: S** | Ref: PS1/26 Art. 92(2A), 92(3A), 92(3)(a) (ps126app1.pdf pp.13,15)
- **P1.213** [ ] **SA-CCR option supervisory delta uses original tenor for Black-Scholes T instead of remaining time-to-expiry from reporting_date** — `ccr/supervisory_delta.py:136-138` sets `t_years=(maturity_date-start_date)/365` and feeds d1 (`:144`); the signature (`:85`) takes no `reporting_date`. For any seasoned option (reporting_date>start_date) T is overstated, so d1, Φ(d1), delta, PFE add-on and EAD are wrong. Thread `reporting_date` in and compute `T=max((maturity_date-reporting_date)/yearfrac, 0)`, reusing the reporting-relative `years_to_maturity` at `pipeline_adapter.py:245-246` (called `:269`), matching the S/E convention in `adjusted_notional.py:90-91`. **Effort: S** | Ref: BCBS CRE52.42 / CRE52.40; CRR Art. 279a(2) (onshored under SA-CCR / PS1/26 CRR Part)
- **P1.214** [x] **FIXED (Unreleased): unmargined MF now floors residual maturity at 10 BD — `MF = sqrt(min(max(BD, 10), 250)/250)`, via a new regime-invariant `mf_unmargined_floor_days = 10` rulepack `IntParam` (cited CRR Art. 279c(1) / CRE52.47-52.48 fn.13). A _dedicated_ scalar, NOT a reuse of the Art. 279b `sa_ccr_start_floor_years` start-floor — the residual-maturity `M` floor and the start-date `S` floor are distinct provisions that share the 10-BD value (an adversarial 3-source verification workflow confirmed the floor is CRE52.47-52.48 fn.13, not the CRE52.50 margined-MPOR list). Implemented on the business-day basis (`pl.max_horizontal(business_days_to_maturity, 10)` before the 250-BD cap), superseding this bullet's calendar-year `max_horizontal(M, 0.04y)` suggestion (the unmargined MF moved to the BD basis the same cycle). No existing golden moves (all CCR-A scenarios ≥ 10 BD); pinned by sub-10-BD + date-driven 5-BD cases in `tests/unit/ccr/test_mf_business_day_basis.py` and a pack-value test in `tests/unit/data/tables/test_sa_ccr_factors.py`.** ~~Unmargined SA-CCR maturity factor omits the 10-business-day (0.04y) floor on M, dropping MF below sqrt(10/250)=0.20~~ — `ccr/maturity_factor.py:62-72` (`compute_maturity_factor_unmargined`) computes `MF=sqrt(min(M,1)/1)` with no lower floor; its producer `pipeline_adapter.py:245` builds `years_to_maturity=(maturity_date−reporting_date)/365.25` with no 10-BD floor. Per CRR Art. 279c(1) / CRE52.48 (+BIS FAQ d438) M must be floored at 10 business days (10/250=0.04y), so MF never drops below 0.20. Apply `pl.max_horizontal(M, SA_CCR_START_FLOOR_YEARS)` before `min(M,1)` (the 0.04 constant exists at `sa_ccr_factors.py:181`). Add an acceptance test for a sub-10-BD unmargined trade (MF=0.20). Anti-conservative. **Effort: S** | Ref: CRR Art. 279c(1) / BCBS CRE52.48 + BIS SA-CCR FAQ d438; PS1/26 CCR (CRR) Part Art. 279c

#### Reporting & docs

- **P2.50** [ ] **B31 IRB retail PD floor: 0.10% applied to all residential-mortgage retail; Art. 163(1)(b) limits 0.10% to UK-located RRE, non-UK RRE retail should floor at 0.05% (Art. 163(1)(c))** — `irb/formulas.py:128` (`_pd_floor_expression`) routes every `exposure_class` matching 'MORTGAGE'|'RESIDENTIAL' to `floors.retail_mortgage=0.0010` (`config.py:109`) with no property-location gating (`namespace.py:543` calls it). PS1/26 Art. 163(1)(b) restricts 0.10% to retail secured by RRE "located in the UK"; (c) puts all-other-retail (incl. non-UK RRE) at 0.05%. No property-country column exists, so a fix needs a new UK-property input flag + a gated branch + a `retail_mortgage_non_uk`=0.0005 floor. Divergence is conservative (overstates capital), edge case for UK retail. *(confidence: medium)* **Effort: M** | Ref: PS1/26 Art. 163(1)(b)/(c) (ps126app1.pdf PDF idx 114)
- **P2.51** [ ] **CIU equity-underlying Rule 4.8(3) floor uses a flat 370% instead of the underlying's per-type Art. 155(2) weight (190/290/370)** — `equity/calculator.py:552-553` (`_equity_holding_higher_of_rw`) returns `max(_IRB_RW[OTHER]=370%, transitional_rw)` as a single scalar applied via `pl.lit()` (`:439`) to ALL equity-class CIU look-through underlyings. Rule 4.8(3) requires the per-underlying Art. 155(2) weight: 190% diversified PE / 290% exchange-traded-listed / 370% other. Over-states RW for ET/diversified-PE underlyings (over-capital). Needs `CIU_HOLDINGS_SCHEMA` (`schemas.py:647-653`) extended with the underlying equity-type/listing/diversification field, plumbed through `_resolve_look_through_rw` (`:434-441`), then branch the floor per underlying. **Effort: M** | Ref: PS1/26 App 1 Rule 4.8(3) (ps126app1.pdf pp.21-22); CRR Art. 155(2) 190/290/370 (`crr_equity_rw.py:57-72`)
- **P2.52** [ ] **CRR Art. 159 purchased-in-default discount (Art. 166(1)) not treated as SCRA in EL-vs-provisions Pool B/D** — Art. 159 requires discounts on balance-sheet exposures purchased when in default (Art. 166(1)) to be treated as SCRA on the provisions side of the EL comparison. Pool B is built only as `provision + ava_amount + other_own_funds_reductions` at `irb/adjustments.py:361-362` and `_el_summary.py:211` — no discount term — and there is no input column (PROVISION/CRM_OUTPUT/IRB-result schema) to carry it. Add a purchased-in-default discount column and include it in Pool B/D; distinguish the B31 path, where the renumbered Art. 166A(2) defaulted-discount is instead EXCLUDED per `docs/specifications/basel31/provisions.md:328`. *(confidence: medium)* **Effort: M** | Ref: CRR Art. 159 with Art. 166(1) (crr.pdf page label 156); B31 contrast PS1/26 Art. 166A(2)

#### Code-quality

- **P6.38** [ ] **`_compute_ead` implements PS1/26 Art. 166D(5) A-IRB EAD floors with no `@cites` decorator** — `engine/ccf.py:634` (`CCFCalculator._compute_ead` — *was `ccr/ccf.py:494`, consolidated into `engine/ccf.py`; refreshed 2026-07-07*) applies the Art. 166D(5)(b) facility-level EAD floor (pack `airb_obs_floor_b_multiplier`) and 166D(5)(c) fully-drawn floor, gated on the `airb_ead_floor_applies` pack Feature, yet carries no `@cites` — so `watchfire matrix --instrument PS` indexes zero sites for Art. 166D while every sibling CCF helper is decorated. Stack `@cites("PS1/26, paragraph 166D.5")` (canonical numeric-paragraph form, matching the `_apply_purchased_receivable_ccf` "166.5" precedent — the literal "Art. 166D(5)" form is not watchfire-valid). Constant now pack `airb_obs_floor_b_multiplier` (`rulebook/packs/b31.py:569`, moved from `airb_floors.py` in Phase 5); add WHITELIST row at `tests/contracts/test_watchfire_coverage.py:109-113`. CRR Art. 111(2) already cited at `provisions.py::resolve_provisions` — do not double-track. Metadata-only. **Effort: S** | Ref: PS1/26 Art. 166D(5)(b)/(c) → `PS1/26, paragraph 166D.5`
- **P6.39** [ ] **`overcollateralisation_ratio_expr` missing outer `@cites("CRR Art. 230")` for its CRR C** divisor branch** — `crm/expressions.py:86` decorates the fn with only `@cites("PS1/26 Art. 230(1)")`. Its default `is_basel_3_1=False` else-branch (`:106-117`) is the single source of truth for the CRR Art. 230 Table 5 C** divisors (receivables 1.25, RE/other 1.40; from `firb_lgd.py:104-110`), consumed at `collateral.py:623,638-642`. Per citation-tracking.md, watchfire's matrix surfaces only the OUTER decorator, so CRR Art. 230 never maps here. Add outer `@cites("CRR Art. 230")` above the existing decorator (and correct the inner to canonical `"PS1/26, paragraph 230"`). Metadata-only. **Effort: S** | Ref: CRR Art. 230 Table 5
- **P6.40** [ ] **Stale PD-floor values in the `apply_pd_floor` docstring** *(paths refreshed 2026-07-07: `IRBLazyFrame` retired Phase 4; `apply_pd_floor` is now the plain function at `irb/transforms.py:278` — re-verify the stale docstring survived the migration before actioning, it may already be fixed)* — the old `irb/namespace.py:531-534` docstring listed wrong B31 PD floors: "Retail mortgage: 0.05%" (should be 0.10% per Art. 163(1)(b) UK RRE) and "transactors: 0.03%" (should be 0.05% per Art. 163(1)(c)). Runtime is correct (`config.basel_3_1()` sets `retail_mortgage=0.0010`, `retail_qrre_transactor=0.0005`; live path uses `_pd_floor_expression` whose docstring at `formulas.py:67-69` is already correct). Align the `apply_pd_floor` docstring with `formulas.py:66-71`. Documentation-only. **Effort: S** | Ref: PS1/26 Art. 163(1); basel31 skill irb-changes.md:15-18
- **P6.41** [ ] **Stack PS1/26 paragraph 153.5 citation on the B31 slotting risk-weight path** *(paths refreshed 2026-07-07: `SlottingLazyFrame` retired Phase 4 → plain functions in `slotting/transforms.py`; `b31_slotting.py` tables moved to `rulebook/packs/b31.py`)* — `apply_slotting_weights` (`slotting/transforms.py:157`) and `SlottingCalculator.calculate_branch` (`slotting/calculator.py:83`) carry only `@cites("CRR Art. 153(5)")`. The `is_crr==False` branch of `lookup_rw` (`slotting/transforms.py:335`) implements the PS1/26 Art. 153(5) Table A structure (distinct HVCRE table + col-A/C short-maturity subgrades from the pack) which has no UK-CRR analogue, but no PS1/26 citation is stacked, so watchfire matrix omits the B31 Art. 153(5) mapping. Stack `@cites("PS1/26, paragraph 153.5")` inner under the existing decorator on both functions; add WHITELIST rows at `test_watchfire_coverage.py:122-126`. Index-only. **Effort: S** | Ref: PS1/26, paragraph 153.5 (Art. 153(5) Table A)
- **P6.42** [ ] **Slotting EL path (Art. 158(6) Table B) carries no `@cites` decorator on either framework** *(paths refreshed 2026-07-07: `slotting/namespace.py` retired Phase 4 → `slotting/transforms.py`; `crr_slotting.py`/`b31_slotting.py` tables moved to `rulebook/packs/`)* — `apply_el_rates` (`slotting/transforms.py:192`) and `lookup_el_rate` (`:380`) implement the Art. 158(6) Table B slotting EL ladder for CRR and B31 but carry no `@cites`, so the mapping is absent from `watchfire matrix`. The symmetric RW method `apply_slotting_weights` (`:157`) is cited CRR Art. 153(5); the EL path is an asymmetric omission. Add `@cites("CRR Art. 158(6)")` (and an inner PS1/26 citation where the B31 path differs) to both; add WHITELIST rows. Index-only. **Effort: S** | Ref: CRR Art. 158(6) Table B; PS1/26 Art. 158(6) Table B
- **P6.43** [ ] **Stack a PS1/26 IRB-equity-removal `@cites` on `EquityCalculator._determine_approach`** — `equity/calculator.py:309` is decorated only with `@cites('CRR Art. 155(3)')`, but `:333` (`if config.is_basel_3_1: return EquityApproach.SA`) implements the B31 IRB-equity removal mapping onto PS1/26 Art. 147A(1)(h). Per citation-tracking.md (dual CRR-outer / PS1/26-inner), add `@cites('PS1/26, paragraph 147A.1')` as the inner decorator (NOT the literal 'Art. 147A(1)(h)' form, which fails the strict watchfire gate). Mirror the classifier's existing `@cites('PS1/26, paragraph 147A.1')` (`classifier.py:1689`). Optionally add to the `test_watchfire_coverage.py` WHITELIST. **Effort: S** | Ref: PS1/26, paragraph 147A.1 (Art. 147A(1)(h) read with Art. 147(2)(e))
- **P6.44** [ ] **Wrong CRR citation for SA provision deduction: docstrings/comments cite Art. 111(2) (derivatives) instead of Art. 111(1)(a)-(b)** — SA drawn-first provision deduction is cited as 'CRR Art. 111(2)' in 6 source locations, but Art. 111(2) governs derivative exposure values; the asset/OBS value-adjustment reduction is Art. 111(1)(a)-(b) (spec already corrected at `docs/specifications/crr/provisions.md:38-41`). Fix all six: `crm/provisions.py:9` and `:48`; `data/schemas.py:92` and `:1583`; `engine/ccf.py:502`; `crm/processor.py:732`. The generic `@cites('CRR Art. 111')` at `provisions.py:31` is fine. Cosmetic / no calculation impact. **Effort: S** | Ref: CRR Art. 111(1)(a)-(b) (crr.pdf pp.109-110)
- **P6.45** [ ] **Add `@cites` decorators for Art. 158/159/62(d)/36(1)(d) on EL shortfall/excess + portfolio-summary functions** — `compute_el_shortfall_excess` (`irb/adjustments.py:282`) and `compute_el_portfolio_summary` (`aggregator/_el_summary.py:151`) implement the Art. 158-159 EL-vs-Pool-B comparison, the Art. 159(3) two-branch rule, the Art. 62(d) T2 0.6% cap, and the Art. 36(1)(d) CET1 deduction, but carry no `@cites` (citations only in docstrings) and neither file imports `from watchfire import cites`. `watchfire matrix` shows zero Art. 158/159/62/36 entries. Add the import and stack `@cites('CRR Art. 158')`/`@cites('CRR Art. 159')` on the shortfall fn and `@cites('CRR Art. 159')`/`@cites('CRR Art. 62')`/`@cites('CRR Art. 36')` on the summary fn, then re-run watchfire via `scripts/arch_check.py`. Index-only. **Effort: S** | Ref: CRR Art. 158, 159, 62(d), 36(1)(d)
- **P6.46** [ ] **`apply_factors` applies the Art. 501a infrastructure factor but is uncited for 501a; watchfire index lists only a dead helper** — `supporting_factors.py:181` `apply_factors` (the only production supporting-factor path — called by `irb/calculator.py:288`, `slotting/calculator.py:166`, `sa/factors_output.py:83` — *`sa/namespace.py` retired Phase 4; refreshed 2026-07-07*) applies both the SME and infrastructure factors (`infra_factor_expr` `:422-429`) but is decorated only `@cites('CRR Art. 501')` (`:180`). The sole 501a-cited site, `calculate_infrastructure_factor:125`, is reached only via `get_effective_factor:175` which is never called. Stack `@cites('CRR Art. 501a')` inner under the existing decorator on `apply_factors`, and update the WHITELIST row at `test_watchfire_coverage.py:87-88` from `('CRR Art. 501',)` to `('CRR Art. 501', 'CRR Art. 501a')`. Index-only. **Effort: S** | Ref: CRR Art. 501a

### P1.9 Output Floor — OF-ADJ, portfolio-level application, U-TREA/S-TREA

- **Status:** [~] Partial — sub-items (a) OF-ADJ, (b) portfolio-level, (d) skip-transitional all FIXED; only (c) remains.
- **Remaining (c):** U-TREA/S-TREA COREP export. `OutputFloorSummary` is on `AggregatedResultBundle`, but full `OF 02.01` COREP template wiring (4-column comparison) not yet done — tracked under P2.1.
- **File:Line:** `engine/aggregator/_floor.py`, `_schemas.py`, `aggregator.py`, `contracts/bundles.py`
- **Spec ref:** `docs/specifications/output-reporting.md`, PRA PS1/26 Art. 92(2A)/(3A)/(5)
- **Tests:** 80 unit tests across `test_portfolio_level_floor.py`, `test_of_adj.py`, `test_output_floor_skip_transitional.py`.

### P2.1 COREP template rework — structure alignment

- **Status:** [~] Needs rework
- **Impact:** Current COREP generator (`reporting/corep/generator.py`) uses simplified column sets and one-row-per-class structure. Only C 07.00, C 08.01, C 08.02 (and OF variants) are implemented. Full-width CRR/B31 column definitions exist in `templates.py` (lines 1-651) but generator uses backward-compatibility aliases. Specific sub-gaps:
  - C 08.01 col `0120` ("Of which: off balance sheet") still null — needs off-BS EAD pipeline column
  - B31 OF 08.02 missing columns `0001` and `0101-0105` (per-grade CCF breakdown)
  - B31 C 08.01 off-BS CCF sub-rows `0031-0035` always null
  - C 07.00 B31 CIU sub-rows `0284/0285` defined but never populated by generator
  - B31 slotting FCCM cols `0101-0104` in C 08.01 still null — pipeline FCCM for slotting not yet wired
  - **From PDF comparison:** OF 07.00 missing cols 0230/0235/0240 (ECAI breakdown). OF 09.02 missing cols. OF 08.01 missing cols 0254/0265/0282; col 0280 renamed. OF 02.00 needs rows 0034/0035/0036 (floor indicator/multiplier/OF-ADJ). OF 08.06 col 0070 removed in B3.1; col 0031 is a deduction. OF 08.07 cols 0160-0180 consolidated-basis-only. OF 09.01 missing col 0061.
- **File:Line:** `reporting/corep/generator.py`, `reporting/corep/templates.py`
- **Fix:** Migrate generator to use full template definitions. Rework row/column logic. Add missing pipeline columns for equity transitional and currency mismatch reporting. Remove dead alias objects.
- **Tests needed:** Rewrite COREP tests (~250 tests in `tests/unit/test_corep.py`).

---
