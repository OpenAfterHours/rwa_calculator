"""Regulation-derived property and invariant suite (independent validation C2).

Every other layer in this estate asserts a value someone recorded from engine
output. This package asserts statements taken from the regulation itself, which
must hold for **any** portfolio — so a property can fail without anyone having
derived an expected number, and a generated portfolio nobody thought to build
can be the thing that breaks it.

See ``docs/plans/independent-validation-system.md`` §C2.
"""
